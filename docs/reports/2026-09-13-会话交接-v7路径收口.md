# 会话交接（W4）— 纯 v7 路径收口

> 会话：2026-09-12 ~ 2026-09-13（Claude Code 宿主）
> **下会话开局第一件事读本文件。**
> 红线：左栏每个事实必须附出处；写不出出处的放右栏当假设。
> 现状截面文档：`docs/reports/2026-09-12-需求与设计对账.md`（本次会话全程在更新它）。

---

## 一、已验证事实（附出处）

### 起点与终点

| 事实 | 出处 |
|---|---|
| 会话起点：v8.1.0 发版链全绿（pytest 1452 / 行为评测 22 / 四校验），但**无一层跑在「纯 v7 书仓」上** | `AGENTS.md:51,59`；行为评测 fixture 写 `.webnovel/state.json`（`run_behavior_evals.py:210`）；根解析测试 8/8 用例均造 state.json（`test_project_locator.py:34,53,72,91,110,131,148,162`） |
| 会话终点：纯 v7 路径端到端冒烟 **BREAK 11 → 0**（PASS 18 ｜ BREAK 0 ｜ BIZ 2） | `webnovel-writer/scripts/smoke_v7_newbook.py` 实跑；CI run `34732264481` 两 job 绿 |
| v7 正文路径与文件名格式都与 v6 不同 | v7 = `定稿/正文/NNNN-标题.md`（`v7_write.py:6,758`、`v7_cache.py:73`）；v6 = `正文/第NNNN章*.md`（`chapter_paths.py:127`） |
| v7 写链**不产**实体逐章状态/关系 | `record_state_change｜upsert_entity｜state_changes` 的 31 个命中文件里无 `v7_write.py`（grep 核验）；写入方全是 v6 链（`sql_state_manager.py` / `state_manager.py` / `migrate_state_to_sqlite.py`） |
| v7 写链**无**任何向量/检索代码 | `v7_*.py` 里 `vector｜embed｜rag` 唯一命中是 `v7_write.py:904` 的 "cove·rag·e" 子串巧合 |
| `.cache/index.db` 只有 4 张表（chapters/entities/summaries/meta） | `v7_cache.py:170-178` |
| 14 个 MCP 工具里，只有 `webnovel_context｜meter｜knowledge｜rag｜reader_signals` 五个名字，**skill/agent 零引用**（只在 `mcp/server.py` 与其测试里出现） | grep 全仓 |
| 三个 v6 域工具（rag/knowledge/context）在 v7 仓现返回 `{"status":"unsupported",…}` 且退出码 0 | `webnovel.py` 的 `_V7_UNSUPPORTED` 闸门 + `test_v7_unsupported_tools.py` 6 用例 |

### 本次会话的提交（v8-author，均已 push）

| 提交 | 内容 |
|---|---|
| `d2aa12f` | 对账文档落盘 D-2 讨论修正（拆甲/乙、作废「接 v7 .cache」措辞） |
| `e45f576` / `9890197` | 冒烟脚本 `smoke_v7_newbook.py` + 判定修正（码 0 但自报 error 也判 BIZ） |
| `079903e` | 冒烟接入 CI 两个 job（ubuntu + windows），带 `--allow-break` |
| `df65874` | **甲-1**：`_is_project_root` 放宽判据（`domain_contract.is_story_repo_strict()`：book.yaml + 六域顶层目录之一） |
| `8927da0` | **U-8**：设定目录改多路径回退（`config.settings_dirs` + `settings_digest.find_setting_path`） |
| `5753912` | 文档结案 + `agents/context-agent.md:40` 指引同步 |
| `f242105` | **U-12**：`find_chapter_file` 支持 v7 布局（先查 `定稿/正文/NNNN-*.md`） |
| `5368fda` | **U-11**：`placeholder_scanner` 扫描补 `设定/` |
| `b730d90` | **D-2 乙 第一批**：三工具明示不支持 + 移除 CI 的 `--allow-break` |

### 关键实现位置（下会话可能要动）

| 关注点 | 位置 |
|---|---|
| 根解析判据 | `project_locator.py:272-286`（`_is_project_root`）；`domain_contract.py:76-89`（`is_story_repo_strict` / `DOMAIN_TOP_DIRS`） |
| 设定目录候选 | `config.py:128-146`（`settings_dirs` / `settings_dir`）；`settings_digest.py:61-84`（`find_setting_path`） |
| 章节文件查找 | `chapter_paths.py:115-165`（v7 优先分支在最前） |
| v7 工具闸门 | `webnovel.py`：`_V7_UNSUPPORTED` 常量 + `project_root` 解析后的拦截块 |
| 冒烟脚本 | `webnovel-writer/scripts/smoke_v7_newbook.py`（三态 PASS/BREAK/BIZ，`--allow-break`） |
| CI | `.github/workflows/plugin-tests.yml`（两个 job 各含冒烟步骤，当前**不带** allow-break） |

---

## 二、假设 / 待确认

- **假设**：三次「本机与 CI 一致」不等于以后不会有平台差异。冒烟的价值在**未来**的退化告警，不是已验的平台等价性。（待验证：无——这是预期管理，不是待办）
- **假设**：`backup_manager._local_backup` 的 v7 路径缺口**影响面窄**（`backup()` 是 `if not self.git_available` 才走它，`backup_manager.py:277-279`），因为 `book-init` 默认建 git 仓。（待验证：找一个 `--no-git` 的 v7 仓实跑 `backup`）
- **假设**：`chapter_paths` 的另三个调用方（`run_ledger` / `extract_chapter_context` / `project_phase`）本就优雅，**只有 `user_report` 会误报**。（已读代码判定，未逐个跑端到端）
- **未核验**：`length_contract` 幽灵 section 的判定来自子代理提炼（`v7_write.py:329` 塞入 sections、`:360` 计入统计），**我未独立逐行复核**。
- **未做**：`docs/reports/2026-09-12-需求与设计对账.md` §6 的 **Part B（测试锚定分类）只做了统计**——15 个测试文件含 `设定集`、14 个同时造 `state.json`，**哪些合理、哪些缺 v7 变体未逐个分类**。

---

## 三、待办全貌（权威在 todohub，此处仅索引）

```bash
# 查本项目待办：
#   mcp__todohub__todo_list(workspace="ai-workspace", project="projects/zcode-plugins/webnovel-writer")
```

| 优先级 | 待办 |
|---|---|
| **P2** | `t-20260913-6202` `reader_signals` 换落点（`.cache/index.db` 加表 + `rebuild_cache` 重算；**方案已定，可直接做**） |
| **P2** | `t-20260913-1072` 纯 v7 仓仍写 v6 域（`pack` 副作用建 `style_samples.db` + `index.db` 被 settle 加入 git） |
| P3 | `t-20260913-ff22` U-14 幽灵 section ｜ `t-20260913-f847` `backup_manager._local_backup` |
| P3 | `t-20260912-ac94` W6/R9 ｜ `t-20260912-8f3e` D-8 ｜ `t-20260912-0cbc` N12 |

**未登记为待办但悬空的**：

- **D-2 乙 剩余决策**：`rag_search` / `knowledge` 的「真砍出工具面 vs 在 v7 写链补生产者」+ `meter` 去留。
  相关事实：MCP 只读面**内部零消费者**（skill/agent 不用），收缩只影响 `README.md:77` 的对外承诺数。
- **D-4**：zcode-native-adaptation 的 7 项重启验证——**只有 Human 能做**（需重启 ZCode 后按 `docs/zcode/zcode-native-adaptation/05-install-reinstall-runbook.md` §4 清单确认）。

---

## 四、交接给下一会话

- **进行到**：D-2（v6 线退役 Phase 3）由"待裁决"推进为"甲已修完 + 乙第一批已落地"；纯 v7 路径零入口级断点且有 CI 守护。
- **下一步（建议顺序）**：
  1. **`reader_signals` 换落点**（P2，方案已定、独立、可直接实施）
  2. **D-2 乙 剩余决策**（需 Human 拍板：rag/knowledge 真砍 vs 补生产）
  3. D-4（只有 Human 能做）
- **坑（本轮踩过，怎么绕开的）**：
  1. **Bash 被插件机器闸拦截**：`guard_runtime_write.py`（来自**已安装的**插件缓存 `~/.claude/plugins/cache/webnovel-writer-marketplace/webnovel-writer/6.2.1/hooks/`，不是本仓开发版本）会拒绝含 `.webnovel` 字面量的 shell 命令。**绕法**：改用 `Grep`/`Read` 工具（不走 Bash hook）；**不要**想办法绕过闸门。
  2. **子代理结论必须核验**：本轮两次靠核验纠正——① 子代理说"4 个宽松路由工具照常工作"，实测**需显式传 `--project-root`** 才成立；② 子代理把 `v7_write.py:904` 的 "coverage" 误报为 rag 命中（我核出是子串巧合，结论反而更硬）。
  3. **U-8 的原 plan 在实施前被推翻**：直接改 `config.settings_dir` 单值会破坏存量 v6 仓的设定读取（`init_project.py` 至今建 `设定集/`）→ 改采 B1 多路径回退。**教训：改路径前先查"谁还在用旧路径"**。
  4. **测试数据自身会撒谎**：U-11 首版测试用 `{新占位}`，而 `PLACEHOLDER_PATTERNS` 只认字面 `{占位}` —— 那次失败是我的数据问题，不是实现问题。**先确认测试数据合法，再判实现**。
  5. **`cd` 会改变后续命令的路径基准**：本会话多次因 cwd 在「仓库根 ↔ 插件本体」之间切换而写错 pathspec。**建议统一用 `git -C <仓库根>` + 全路径**。
- **本轮的元教训**：**已知的每个缺陷都是「撞见」的，不是「查出来」的**。把"撞见"变成"查出来"的手段有两个，都已就位：① 端到端冒烟（含 CI 守护）；② 只读排查专项（废弃路径 + 测试锚定，Part B 仍欠分类）。
