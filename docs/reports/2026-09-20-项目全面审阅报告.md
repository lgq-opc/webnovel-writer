# webnovel-writer 项目全面审阅报告（2026-09-20，Claude Code 宿主）

> **审阅对象**：`D:/lgq/ai-workspace/projects/zcode-plugins/webnovel-writer`（外层仓库根；插件本体在内层 `webnovel-writer/`）。
> **审阅基线**：`v8-author` @ **`3b7b5e0`**（工作树 clean）。
> **审阅方式**：只读。本机实跑机器闸 + 4 个只读探索代理分维度深挖；代理结论中影响定级的关键项由本审阅**逐条复验**（下文标「复验」/「单源」）。
> **上次复审**：`docs/opencode/项目复审/2026-09-13-项目复审报告.md`（v8.1.0）、`docs/zcode/项目复审/2026-09-20-项目复审报告.md`（同日 ZCode 侧，v8.1.1）。

## ⚠️ 基线声明（必读）

审阅**进行中**（03:43）仓库被并发写入：ZCode 会话落了提交 `3b7b5e0`（复审 P2 处置——补删 `prewrite_validator.py`、移除 `author_glossary.py` 及其数据源，并改动 `AGENTS.md` / `backup_manager.py` / `settings_digest.py`）。

- 本报告除特别标注外，**所有结论以 `3b7b5e0` 为准**（该提交后工作树 clean，四个校验脚本重跑 rc=0）。
- 因此：本会话早段（`0461637` 基线）发现的「`prewrite_validator.py` / `author_glossary.py` 为孤儿」两项，**已由 `3b7b5e0` 处置完毕**，不作为待办列出；但按 **W13 并发隔离**，同一仓库并发写入本身是流程问题（见 P2-13）。
- `3b7b5e0` **尚未推送**（`origin/v8-author` = `0461637`）。

---

## 结论

**P0 = 0；P1 = 3；P2 = 13（归并为 6 组）；P3 = 9。**

**最需要立刻处理的是「门禁失效」类问题，而不是代码缺陷**：

1. **CI 在 master 与 v8-author 上双双变红，已连续 6 次失败（2026-09-15 起）**，且**本地无法复现**——这正是「绿灯只在本地成立」的那类问题，而它正是 2026-09-10 新增 Windows job 想要防住的。
2. **自动发版链已断**：`Plugin Release` 工作流读取一个**不存在的清单路径**，`v8.1.0` / `v8.1.1` 两个 tag 都没有对应的 GitHub Release（API 实证远端 Release 仅 `v8.0.0` / `v7.0.0`）。
3. **一处可动态复现的路径穿越**：`materials discard --batch` 可删除书仓之外的任意文件（本机已实证删除成功）。

代码本体质量仍然在线：全量测试 **1504 passed / 覆盖率 82.47%**、四校验脚本全绿、冒烟 `PASS 18 / BREAK 0`、密钥与注入面零命中、MCP 只读工具面与文档计数一致。**本次 P0 为空，可以维持 v8.1.1 的代码状态；但发布与 CI 两条链需要先修。**

---

## 一、必跑验证命令（本机实跑）

| 命令 | 结果 |
|---|---|
| `PYTHONUTF8=1 python -X utf8 -m pytest` | **1504 passed**，覆盖率 **82.47%**（门槛 80），用时 103.90s，rc=0 |
| `python -X utf8 webnovel-writer/scripts/validate_plugin_package.py` | `OK plugin package`，errors 0 / warnings 0，rc=0 |
| `python -X utf8 webnovel-writer/scripts/validate_release_notes.py` | `OK release notes`，version 8.1.1 / previous_tag v8.1.0，rc=0 |
| `python -X utf8 webnovel-writer/scripts/sync_plugin_version.py --check` | `Versions are in sync: 8.1.1`，rc=0 |
| `python -X utf8 webnovel-writer/scripts/validate_reference_wiring.py` | `OK reference wiring: assets=63 consumers=53 drift=0`，rc=0 |
| `python -X utf8 webnovel-writer/scripts/smoke_v7_newbook.py` | **PASS 18 / BREAK 0 / BIZ 2**；2 项 BIZ 为既有基线（文笔门禁按设计拒绝 fixture 正文、timeline-check 需人工判读），非缺陷 |

测试数对比：`0461637` 时 1510 → `3b7b5e0` 时 1504，差 6 恰为 `3b7b5e0` 删除的 `test_prewrite_validator.py` + `test_author_glossary.py` 用例数，**无新增失败**。

---

## 二、P1-1｜CI 已红，且本地不可复现

**事实（GitHub Actions API 实证）**：

| 时间（UTC） | 工作流 | 分支 | 结论 | HEAD |
|---|---|---|---|---|
| 2026-09-19 17:29:32 | Plugin Tests | **master** | **failure** | 04616371 |
| 2026-09-19 17:29:04 | Plugin Tests | **v8-author** | **failure** | 04616371 |
| 2026-09-17 17:49:08 | Plugin Tests | master | failure | bce58ea5 |
| 2026-09-15 04:47:55 | Plugin Tests | v8-author | failure | 578b4bfa |
| 2026-09-15 02:09:59 | Plugin Tests | v8-author | failure | 676fc5c3 |
| 2026-09-13 13:02:54 | Plugin Tests | v8-author | **success** | 6082366c |

即：**最后一次绿是 2026-09-13，此后 6 连红**。

**失败定位**（run 35458247431 的 jobs API）：`tests`（ubuntu）job **全步骤 success**；失败的是新增的 `tests-windows` job，卡在 **step 5「Run test suite (documented command, no PYTHONUTF8)」**（17:30:56 → 17:34:31，耗时 3m35s，非超时）。

**已排除的可能原因**（逐条实证）：

| 假设 | 实证 | 结论 |
|---|---|---|
| 依赖锁变化 | `git log -- requirements.lock` → 最后一次改动是 2026-09-10 `aa78fa7`，在绿/红窗口内**未变** | 排除 |
| 工作流 YAML 变化 | `git diff 6082366c 676fc5c3 -- .github/workflows/plugin-tests.yml` → **空** | 排除 |
| pytest.ini / conftest.py 变化 | 同上 diff → **空**（根 conftest） | 排除 |
| 换行符 / `core.autocrlf` | `.gitattributes` 已把 `*.md/*.py/*.json/*.yml` 钉为 `eol=lf` | 排除 |
| 日期敏感测试 | `grep -rl "datetime.now()\|date.today()"` 在测试目录 → **0 命中** | 排除 |
| 本机可复现 | 见下 | **不可复现** |

**本机复现尝试（三次，全部通过）**：

1. 本机系统 Python 3.13.5 + 已装包，跑 `python -X utf8 -m pytest`（**不带** PYTHONUTF8，与 CI 同命令）→ **1504 passed，rc=0**。
2. 建干净 venv 装 `requirements.lock`（pytest **9.1.1** / pytest-asyncio **1.4.0** / coverage **7.16.0**，与本机原有的 8.3.3 / 0.24.0 / 7.10.4 不同），同命令 → **1504 passed，rc=0**。
3. `git clone` 到全新目录（排除本机 `.tmp/` `.webnovel/` `__pycache__` 等残留态）+ 同一 venv，同命令 → **1504 passed，rc=0**。

**结论**：该失败**不是依赖、不是工作流配置、不是本机脏状态**导致，只在 GitHub `windows-latest` runner 上出现。**根因需要 CI job 日志**——本次审阅无法取得（`/actions/jobs/{id}/logs` 返回 `403 Must have admin rights to Repository`）。

**处置建议**：本仓库管理员在 Actions 页面打开失败 run 的 `tests-windows` job 日志，即可定位。**在此之前不应再宣称「发版链全绿」**——`AGENTS.md` 与 `docs/zcode/项目复审/2026-09-20-项目复审报告.md` 中「发版链全绿」的表述目前**只在本地成立**。

---

## 三、P1-2｜自动发版链已断，v8.1.0 / v8.1.1 没有 GitHub Release

**事实**：

- GitHub API `releases` 列表**只有 `v8.0.0` 与 `v7.0.0`** 两个 Release；`v8.1.0` / `v8.1.1` **有 tag、无 Release**。
- `Plugin Release` 工作流在 master 上**连续失败**（run 35458247434 @ 04616371、35255032291 @ bce58ea5），失败步骤是 **step 4「Resolve release version」**，其后 7 个步骤全部 skipped。

**高置信根因**：v7.1 迁移把插件清单从 `.claude-plugin/` 改名到 `.zcode-plugin/`（提交 `de623de`），**Python 脚本跟进了，CI YAML 没有**：

```
$ ls webnovel-writer/.claude-plugin      → No such file or directory
$ grep -n "\.claude-plugin/plugin\.json" .github/workflows/*.yml
.github/workflows/plugin-release.yml:9:      - 'webnovel-writer/.claude-plugin/plugin.json'   ← 触发路径，永不命中
.github/workflows/plugin-release.yml:49:  version="$(python -c '… Path("webnovel-writer/.claude-plugin/plugin.json") …')"  ← 直接抛 FileNotFoundError
.github/workflows/plugin-version.yml:7:      - 'webnovel-writer/.claude-plugin/plugin.json'
.github/workflows/plugin-version.yml:17:     - 'webnovel-writer/.claude-plugin/plugin.json'
```

对照：`scripts/sync_plugin_version.py:13-14` 是**对的**（`.zcode-plugin` 优先、`.claude-plugin` 回退），所以 `--check` 一直绿——**脚本层修了，工作流层漏了**。

**连带影响**：
- `plugin-release.yml` 的触发路径不含真实清单 → 改 `plugin.json` 不会触发发版；而改 `README.md`/`CHANGELOG.md`/`releases/**` 会触发，然后**必然崩在第 4 步**。
- `plugin-version.yml` 的路径过滤同样漏掉真实清单 → **改清单时版本一致性检查不触发**（该工作流当前靠 README/CHANGELOG 变更才被带起来）。
- `docs/operations/plugin-release.md` 第 49、94 行同样写 `.claude-plugin/plugin.json`，需一并订正。

---

## 四、P1-3｜路径穿越：`materials discard` 可删除书仓外任意文件（**已动态证实**）

**位置**：`webnovel-writer/scripts/data_modules/material_intake.py:31-32`（`gallery_dir`）、`:125`（`adopt_entries`）、`:171-174`（`discard_batch`）。

```python
def gallery_dir(project_root) -> Path:
    return Path(project_root) / GALLERY_DIR        # GALLERY_DIR = 素材/regen

def discard_batch(project_root, batch):
    path = gallery_dir(project_root) / batch       # batch 未净化
    if not path.is_file(): return {"ok": False, ...}
    path.unlink()                                  # ← 任意文件删除
```

`batch` 来源链路完整：`webnovel.py:1108-1115`（`materials` 子命令，`material_args` 为 `REMAINDER` 原样透传）→ `cmd_materials`（`:354-371`）→ `material_store.main` → `material_intake.main:197` `--batch`（默认 `""`，无校验）。

**本机实证（复验）**：

```
$ python -X utf8 webnovel.py book-init <tmp>/mybook 测试书 --genre 玄幻      # 建纯 v7 书仓
$ printf 'VICTIM\n' > <tmp>/victim.txt
$ python -X utf8 webnovel.py --project-root <tmp>/mybook materials discard --batch "../../../victim.txt"
OK 已丢弃 ../../../victim.txt
$ ls <tmp>/victim.txt
ls: cannot access '.../victim.txt': No such file or directory      ← 书仓外的文件被删除
```

同链路的 `adopt_entries`（`:125`）同形，可**读取**书仓外任意文件内容并入活层素材（信息泄露 + 数据污染）。

**同族（P2-2，静态分析，未动态证实）**：`regen_gallery.py:25-28/38-41/113-119` 的 `--key` 未净化 → `regen adopt --domain 章纲 --key "../../../../tmp/pwn"` 可在书仓外写入 `.md` 文件（`_target_path` 强制后缀 `.md`，且 `mkdir(parents=True)` 会建目录）。`webnovel.py:1076-1084` 的 `--key` 默认 `""` 且无校验。

**缓解事实**：仓库**已有** `security_utils.sanitize_filename`（`security_utils.py:40-91`），`chapter_paths.py:50-54` 等处在用——**这两处是漏用，不是缺工具**。

---

## 五、组件面接线核查

### 一致项（实证）

- `plugin.json` 四类组件声明 ↔ 磁盘**一一对应**：skills **8** / agents **4** / commands **13** / hooks（`hooks.json` + 4 脚本）；MCP 工具 **12** 个（`mcp/server.py:194-301`：where / project_status / doctor / setting_read / timeline_check / meter / knowledge / materials_status / materials_assemble / power_check / foreshadow_scan / reader_signals）。
- 13 条命令的 CLI 转发目标**全部命中** `data_modules/webnovel.py:1026-1380` 的 parser 表；8 个 skill 引用的脚本路径与 32 个 CLI 动词全部存在；4 个 agent 的 frontmatter `tools` 与正文用法自洽。
- `references/` 接线：`validate_reference_wiring.py` drift=0；抽查 3 对 asset→consumer（`references/shared/pov-management.md`、`skills/webnovel-write/references/draft-rubric.md`、`templates/output/大纲-卷节拍表.md`）全部真实。
- CLI 注册表**双向无缺口**：63 个 parser / 52 个顶层子命令 / 33 个 `cmd_*` handler，无空壳、无死入口。

### 落差项

**P2-3｜`/webnovel:doctor` 与 `webnovel-doctor` skill 是两条不同行为的路（复验）**
`README.md:59` 与 `docs/guides/commands.md:3` 都写「**前 8 条**转发同名 skill」，但 `commands/webnovel/doctor.md:10-14` 是**独立复刻**（首推 MCP 工具 `webnovel_doctor`，否则跑 `doctor --format json`），**不调用** `webnovel-writer:webnovel-doctor`；而 skill 走的是 `project-status --format summary` + `doctor --format text` 的阶段感知流程。**实际是 7 条转发 skill、6 条 CLI 壳。**

**P2-4｜在役 agent 依赖已被撤掉的 CLI，且评测把死词锁成契约（复验）**
`agents/data-agent.md:15/28/69/73/74/122` 反复依赖 `chapter-commit` 与 `projections retry`，而 CLI 已无这两个子命令（`webnovel.py --help` 零命中；`project_status.py:57` 自述「v6 写链已退役，`chapter-commit` CLI 已撤」）。**加重情节**：`scripts/run_behavior_evals.py:116-133` 把字面量 `"chapter-commit"` 列为 `data-agent.md` 的**必需字符串**，缺了判 `data-agent boundary drifted`——**改文档会先让评测变红**，代码与文档互锁在错误状态。

> **⚠️ 定级修正（todohub 证据）**：这不是「没人发现」。todohub 工作记录 `w-20260915-6ec3` 显示曾有分支 `pilot/docs-deadrefs` 把这三处死引用改成 v7 等价物（`4f21ef9`，改前 grep 14/6/6 → 改后 0/0/0）；随后 `w-20260915-cda1` 又把 `data-agent.md` 的 6 处措辞**全部还原**为原文（`7d0dcd5`，分支 `pilot/dataagent-fix`，未推送未合入），理由即「评测锁定」。
> 因此本项**不是遗漏，而是一处已知且被搁置的设计张力**——把它留在原位是当时的自觉选择。本报告仍列为 P2，但建议按「**主动裁决**」处理（要么删评测的必需项再改文档，要么在 `data-agent.md` 顶部把「本文件描述的是已撤的 v6 写链，仅存 schema 参考价值」写清楚），而不是当作待修的 bug。

**P2-5｜两条在役指令互相矛盾：`learn` 是否写 `project_memory.json`（复验）**
`references/index/reference-loading-map.md:92`：「webnovel-learn｜只读 state 后追加 `.webnovel/project_memory.json`」；`skills/webnovel-learn/SKILL.md:24`：「**v7 不再有 `project_memory.json`**（该文件随 v6 退役 Phase 2 一并删除）」。该矛盾**已于 2026-09-14 被 `docs/zcode/2026-09-14-引用盲区复查/report.md:255` 登记**，6 天未修。

**P2-6｜`webnovel-write` 评测集仍断言 v6 六步链**
`skills/webnovel-write/evals/evals.json:8-26` 期望「Step 1 由 context-agent 生成写作任务书」「Step 5 调用了 data-agent 回写数据」，而 `skills/webnovel-write/SKILL.md` 已整份改写为 v7-only，**零处**提到 `data-agent`/`context-agent`/`chapter-commit`。属已登记的有意取舍（AGENTS.md「记录边界」），但后果是这 3 条用例若真跑，会要求 v7 skill 产出 v6 行为。

**P2-7｜`skills/webnovel-write/SKILL.md` 内对 reviewer 是否写盘互斥（单源）**
`:124`「只返回严格的 reviewer schema JSON，**不写任何文件**」vs `:127`「reviewer 持受限 `Write`：它自己把审查 JSON 写入 `${PROJECT_ROOT}/.webnovel/tmp/review_results.json`」。同一次 Agent 调用里的两条互斥指令，而 `settle` 门禁依赖该文件存在。

**P2-8｜包外悬空引用（装机后必然失效，单源）**
`skills/webnovel-write/SKILL.md:82` 引 `docs/guides/v7-write-path.md`；`agents/data-agent.md:11` 引 `webnovel-writer/README.md` 与同一 `docs/` 路径。`docs/` 只在仓库根、**不在插件包内**（`webnovel-writer/` 下无 `docs/`）。校验器看不见：`validate_reference_wiring.py:29` 的正则只匹配 `references/|templates/`。

**P2-9｜`deconstruction-agent` 自相矛盾且入口未接线（单源）**
`:37`/`:144`「不写任何文件」，`:132` 却要求写到 `工作区/素材候选-拆书.csv`，而 frontmatter `tools: Read, Grep, Bash` **无 Write**；`:127` 声称入口是 `/webnovel:materials 拆书`，但 `commands/webnovel/materials.md` 无对应接线。

**P2-10｜`context-agent` 依赖 `${SCRIPTS_DIR}`，调用方不传（单源）**
`agents/context-agent.md:24,32-35,57-62` 全靠 `${SCRIPTS_DIR}`/`{project_root}`；调用点 `skills/webnovel-write/SKILL.md:82` 只说「按注册名调 agent」，**未传参**。对比 `reviewer` 有传（`skills/webnovel-write/SKILL.md:123`、`skills/webnovel-review/SKILL.md:71`）。

**P2-11｜hook 内部子进程预算（10s）超出宿主预算（5s）（双源一致）**
`hooks/hooks.json:22` `"timeout": 5`；`hooks/session_start.py:59` 调 `author-sync` 用 `timeout=6`，`:60-66` 再串行调 `project-status` 用默认 `timeout=4`（`:28`）——最坏 10s。宿主会在第 5 秒杀掉 hook，第二段输出必被截断。对照 `mcp/server.py:28` `SUBPROCESS_TIMEOUT_SECONDS = 25` ↔ `plugin.json:37` `timeoutMs: 60000`（留了余量），说明作者知道要对齐，**hook 这处没对齐**。

**P2-12｜`guard_runtime_write.py` 的 deny 通道写在 stderr（单源）**
`hooks/guard_runtime_write.py:57-62` 把 `{"hookSpecificOutput": {"permissionDecision": "deny"}, ...}` 打到 **stderr** 并 `return 2`。按本仓自己的宿主适配约定（工作区 `CLAUDE.md`：钩子输出契约须为 `hookSpecificOutput.additionalContext`），结构化字段应走 stdout。测试已把该行为固化（`scripts/tests/test_hooks.py:39` `assert "permissionDecision" in proc.stderr`）。

**P2-13｜并发写入（W13）**
审阅期间 `3b7b5e0` 落入同一分支同一工作树。工作区准则 W13 要求「同一仓库的并发任务必须各自**分支 + worktree** 隔离，禁止共享工作树」。本次未造成冲突（审阅只读），但**该提交尚未推送**，且与本次审阅共享了工作树。

---

## 六、安全与健壮性（零命中项与加固项）

### 零命中（实证）

| 维度 | 结果 |
|---|---|
| 硬编码真凭据 | **0**。命中项全部是环境变量读取（`config.py:184/194`，默认空串）、docstring 占位符（`api_client.py:16/21`）、`.env` 空模板（`init_project.py:618/623`）、题材卡字段名 `<entity secret="…"/>`。高熵串正则（`sk-`/`AKIA`/`ghp_`/`xox`/`Bearer`/JWT）**零命中** |
| `shell=True` / `os.system` / `os.popen` | **0**。17 个非测试 `subprocess.run` 全部走 argv 列表 |
| `pickle` / `yaml.load` / `eval` / `literal_eval` | **0**（全项目不依赖 PyYAML，`book.yaml` 走自研行解析） |
| SQL 注入 | **0**。6 处 f-string `execute` 的插值变量全部来自硬编码常量或参数化白名单（`doctor.py:358` 的表名来自硬编码元组 + 前置 `sqlite_master` 白名单） |
| 非回环默认绑定 | **0**。`dashboard/server.py:69` 默认 `127.0.0.1`，`:80-84` 非回环需显式 `--allow-nonlocal`，且在 `uvicorn.run` 之前 `parser.error` 拒绝；CORS 仅本机来源；全部路由为 `@app.get` |
| hook 内网络调用 | **0** |

**加分项**：日志侧有脱敏实现（`run_logger.py:11-45` 的 `SENSITIVE_KEY_RE`，`run_ledger.py:221` 落盘前调用）；dashboard 只暴露 `api_key_present` 布尔值（`app.py:212-233`）；dashboard 读路径有 `path_guard.safe_resolve` + 目录白名单双层防护。

### 加固项

**P2-14｜MCP server 遇非字符串标量参数即崩溃退出（**已动态证实**）**

`mcp/server.py` 的 `call_tool`（`:373-389`）只把 `_reject_leading_dash` 与 `build` 包在 `try` 里，`run_webnovel_cli` 在 `try` 之外，而后者（`:315-338`）只捕 `TimeoutExpired` 与 `OSError`。

**本机实证（复验）**：

```
$ printf '…initialize…\n{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"webnovel_where","arguments":{"project_root":123}}}\n' | python -X utf8 mcp/server.py
  File ".../subprocess.py", line 614, in list2cmdline
    for arg in map(os.fsdecode, seq):
  File "<frozen os>", line 862, in fsdecode
TypeError: expected str, bytes or os.PathLike object, not int
```

`_reject_leading_dash`（`:364-370`）对 int 走 `items = ()` 直接跳过 → `_base_args` 把 `123` 塞进 argv → `subprocess.run` 抛 `TypeError` → 未被捕获 → 冒泡出 `handle_request`（`:404-446`，无 try）与 `serve`（`:449-466`，try 只包 `json.loads`）→ **整个 stdio 循环崩溃、MCP server 进程退出**。触发条件是宿主未按 inputSchema 严格校验类型——**server 自身不做类型校验**。

> 注：`_reject_leading_dash` 对 **flag 注入**本身的覆盖是完整的（`{"table":["a","--foo"]}`、`{"name":"--help"}`、`{"project_root":"-rf/tmp"}` 均被拒，`mcp/tests/test_server.py:139-156` 有用例）。缺口在**类型**，不在前缀。

**P3 级加固缺口**（知悉即可）：

- `v7_write.py:775/817` settle 把章节标题直接拼进文件名（`定稿/正文/{chapter:04d}-{title}.md`），未过 `sanitize_filename`。当前被两点缓解（`{chapter:04d}-` 前缀使首段恒为字面目录名；`chapter_file.parent` 未 `mkdir`），实际抛 `FileNotFoundError` 而非越界写入——**是加固缺口，不是可利用越界**。
- `hooks/guard_runtime_write.py:31` 裸 `sys.stdin.read()`（无 `isatty` 保护），与 `chapter_body_trace.py:59` 的写法不同源；其拦截是正则启发式（`hooks.json` 自述 "best-effort blocking"），**不应作为唯一防线**。
- `mcp/server.py:35-50` 的 `_cli_path()` 优先取 `WEBNOVEL_PLUGIN_ROOT` 环境变量下的 `scripts/webnovel.py`——环境信任边界，非远程输入。
- `dashboard` 无鉴权；`--allow-nonlocal` 一旦使用，同网段可读全书内容（风险已在 flag help 与 error 文案显式披露，属已披露取舍）。

---

## 七、死代码与重复实现

> `3b7b5e0` 已处置本会话早段发现的两个真孤儿，本节为**处置后的存量**。

- **半孤儿**：`data_modules/settings_digest.py`（116 行）6 个公开符号中仅 `find_setting_path` 存活（被 `webnovel.py:764` 的 `setting-read` 消费）；L0 摘要链 `digest_dir`/`build_digest`/`get_setting_digest` 生产消费者为零（原主 `context_manager.py` 已随 `c1f4432` 删除），并连带 `config.py:250` 的 `context_settings_digest_max_chars` 成为死旋钮。**建议定性注释或裁剪，不宜整文件删。**
- **半孤儿（豁免）**：`data_modules/scale_drill.py`（284 行，有 `__main__`，无任何消费者含 CI）；`run_behavior_evals.py`（无 CI 消费者，但被 README 与发版清单引用）。
- **同名/同职责重复实现 8 组**，最高优先级两条：
  1. `_find_setting_path` 在 `v7_write.py:150` 与 `settings_digest.py:65` **逐字节相同**，且 `v7_write.py` 那份**没跟进 B1 的 `设定/`→`设定集/` 双布局收口**——**已行为分叉**。
  2. `_latest_story_system_chapter` 在 `project_phase.py:213` 与 `story_runtime_health.py:28` 两份实现；`_extract_chapter_from_name` 在 `story_runtime_health.py:18`（`chapter_(\d{3,4})`）与 `memory/bootstrap.py:21`（`ch(\d{1,6})` + `第\s*(\d+)\s*章`）同名同职责但正则不同。
- **v6/v7 双实现同时可达**：`init`/`book-init`、`status`/`project-status`、`update-state`/`state` 三对。`rag`/`status` 已在 `webnovel.py:186` 的 `_V7_UNSUPPORTED` 中拒答，但 **`init` 与 `update-state` 不在拒答名单内**，而 `webnovel-plan/SKILL.md:223`、`webnovel-review/SKILL.md:119` 两个**活跃 v7 skill 仍无条件调用 `update-state`**——纯 v7 书仓上必然空转（`update_state.py:141-143` 只打印「❌ 状态文件不存在」并返回 False）。**这是本次发现的第二条「活跃 skill 调用已不适用命令」的实例**（第一条见 P2-4）。
- **体量 × 死度最高**：`data_modules/rag_adapter.py` **1625 行 = 全仓最大文件**，而 RAG 已于 2026-09-18 正式下线。
- **结构可疑**：`index_*` 家族合计 3959 行（`index_manager.py` 自身拆出 5 个 mixin 后仍 1442 行）；`doctor.py` 1243 行单文件线性堆叠；`state_manager.py`(1510) + `sql_state_manager.py`(621) 封装已破（大量穿透私有属性），实质耦合为 2131 行复合体。
- **mcp 侧零重复逻辑**：`mcp/server.py` 零 `data_modules` 导入，全部经 `run_webnovel_cli` 子进程转发——**本次扫描中最干净的边界**。

---

## 八、文档与状态漂移

### 属实项（对照上次复审，无漂移）

- 版本号**四处一致**：`plugin.json:3` / `CHANGELOG.md:5` / `mcp/server.py:26` `SERVER_VERSION` / `marketplace.json:14` 均为 `8.1.1`，且 `sync_plugin_version.py --check` 通过。
- tag `v8.1.1` **已推送远端**（`ls-remote`：`72770c3` → 剥离指向 `0461637`）；`releases/v8.1.1.md` 在位；CHANGELOG 自称「137 笔」经 `git rev-list --count v8.1.0..v8.1.1^` 实证**准确**。
- `marketplace.json` 双位置（根 + `.claude-plugin/`）`diff` 为空。
- 三份待办文档（`TODO-from-v8-author-review.md` 24 项全 `[x]`、`v8-gap-review-3rounds/README.md` 13 项全 ✅、`2026-08-25-status-and-pending-work.md` 显式 superseded）**定性均属实**。
- `3b7b5e0` 处置后四个校验脚本**仍全绿**，无悬空引用回归。

### 漂移项

**P2-15｜待办入口真空（单源）**
`AGENTS.md:60` 把三份文档全部划为「已清空或降为历史」后，**该节不再指向任何在役待办入口**；而项目规则 `AGENTS.md:68` 要求「发现文档与代码不一致时，**先在当前待办入口**登记」——该规则现已不可执行。同时 `2026-09-10-v6线退役方案.md:270` 明写的挂账项 W6/D-8、以及 `docs/opencode/项目复审/2026-09-13-项目复审报告.md:95` 列的 7 个 todohub 任务号，**在仓内无台账承载**（`grep -rn "t-2026091" docs/` 只命中报告正文自身）。

**P2-16｜`docs/README.md` 索引漏收 47 项（单源）**
索引里的路径**全部存在**（无悬空链接），但漏收 47 项，其中属**活跃文档**的有：`docs/guides/v7-write-path.md`（还被 `architecture/overview.md:7` 与 `webnovel-write/SKILL.md` 引用）、`docs/guides/model-routing.md`、`docs/guides/setting-card-contract.md`、`docs/reviews/` 整目录、`docs/reports/experience-log.md`、`docs/claude/` 整目录、`docs/zcode/项目复审/`。

**P2-17｜`04-roadmap.md` 表格与正文自相矛盾（单源）**
`docs/zcode/writing-quality-review/04-roadmap.md:18` 表格仍写 `| W6 | R9 | 无对应任务 | — | ❌ 未实现 |`、`:24` 仍写「合计 9/10 完成」，而同文件 `:61-85` 已记「✅ 2026-09-13 结案」。该文档 `:7` 自称「本节是**本文档唯一的进度事实源**」。W6 已于 `6082366` 结案（`git merge-base --is-ancestor 6082366 HEAD` 为真）。

**P2-18｜`v7.1.0` 有发布说明、无 tag（单源）**
`CHANGELOG.md:29` 有 v7.1.0 章节、`releases/v7.1.0.md` 在位，但本地与远端**都没有 `v7.1.0` tag**。校验器只覆盖当前版本（`validate_release_notes.py:104/167`），对历史版本缺口不设防。

**P2-19｜`.opencode/commands/review.md:44` 仍写「14 个 MCP 只读工具」（复验）**
代码为 12。2026-09-14 夜审 I-1 登记的三处（`AGENTS.md:41`、`docs/architecture/overview.md`、`docs/guides/commands.md:3`）均已修，**此处漏修**。

**P3 项**：

- `docs/architecture/overview.md:61` 写「Hooks ×4：SessionStart / UserPromptSubmit / PreToolUse / **Stop**」——实际第 4 个是 **PostToolUse**，全仓 `grep '"Stop"'` **零命中**（复验）。
- `docs/guides/commands.md:3` 口径版本仍写 v8.1.0（现 8.1.1）。
- `README.md:256-266` 常用子命令表混装 v6 冻结项（`story-system`/`memory`/`rag`）且无 legacy 标注。
- `sync_plugin_version.py:14` 候选路径含不存在的 `webnovel-writer/.claude-plugin/plugin.json`（属 fallback 列表，不报错，但属陈旧假设）。
- `skills/webnovel-doctor/SKILL.md:5` 独有 `version: 0.1.0`（其余 7 个 skill 无此字段）；`:2` description 用 `/webnovel-doctor`（实际是 `/webnovel:doctor`）。
- `agents/evals/files/test-project/` 下 6 个 fixture `.md` 随包落在 `agents/` 子树；`validate_plugin_package.py:265` 用扁平 glob（`agents/*.md`）覆盖不到——**取决于宿主 agent 发现面是否递归**。
- `commands/**/*.md` 一律用 `<ZCODE_PLUGIN_ROOT>`（19 处）而非 `${ZCODE_PLUGIN_ROOT}`；`validate_plugin_package.py:290-291` 的可移植性检查**不覆盖 commands**。需确认宿主是否展开尖括号形式。
- `docs/plans/2026-08-25-status-and-pending-work.md` 留 8 条 `[ ]` + 1 条 `[~]` 原样（其中 6 条实际已交付、1 条已否决），虽有 superseded 横幅仍易误读。
- `docs/README.md:40` 索引条目仍写「W6 学习闭环未实现」，未反映 09-13 结案。

---

## 九、问题清单（按严重度）

| # | 级别 | 问题 | 位置 | 证据等级 |
|---|---|---|---|---|
| 1 | **P1** | CI 自 2026-09-15 起 6 连红（master + v8-author），本地不可复现 | `Plugin Tests` / `tests-windows` job | 实证（API）+ 三次本地复现 |
| 2 | **P1** | 自动发版链断，v8.1.0/v8.1.1 无 GitHub Release | `.github/workflows/plugin-release.yml:9,49`、`plugin-version.yml:7,17` | 实证（API + 文件不存在） |
| 3 | **P1** | `materials discard --batch` 路径穿越 → 任意文件删除 | `material_intake.py:171-174`（同 `:125`） | **动态证实** |
| 4 | **P2** | MCP server 非字符串参数 → `TypeError` 未捕获 → 进程退出 | `mcp/server.py:315-338,373-389` | **动态证实** |
| 5 | **P2** | `regen_gallery --key` 路径穿越 → 书仓外写 `.md` | `regen_gallery.py:25-41,113-119` | 静态分析 |
| 6 | **P2** | `/webnovel:doctor` 不转发 skill；文档「前 8 条」实为 7 条 | `commands/webnovel/doctor.md:10-14` | 复验 |
| 7 | **P2** | 在役 agent 依赖已撤 CLI；评测把死词锁成契约（**已知张力**：09-15 曾修又被有意还原，见 todohub `w-20260915-6ec3` / `cda1`） | `agents/data-agent.md:15/28/122`、`run_behavior_evals.py:121` | 复验 + todohub |
| 8 | **P2** | `project_memory.json` 两处指令直接矛盾（09-14 已登记未修） | `reference-loading-map.md:92` vs `webnovel-learn/SKILL.md:24` | 复验 |
| 9 | **P2** | `webnovel-write` 评测集仍断言 v6 六步链 | `skills/webnovel-write/evals/evals.json:8-26` | 单源 |
| 10 | **P2** | reviewer 是否写盘，同一 prompt 内互斥 | `skills/webnovel-write/SKILL.md:124` vs `:127` | 单源 |
| 11 | **P2** | 包外悬空引用（装机后失效），校验器正则覆盖不到 | `webnovel-write/SKILL.md:82`、`data-agent.md:11` | 单源 |
| 12 | **P2** | `deconstruction-agent` 写盘要求与「不写文件」+ 无 Write 冲突 | `deconstruction-agent.md:37/132/144` | 单源 |
| 13 | **P2** | `context-agent` 依赖 `${SCRIPTS_DIR}` 但调用方不传 | `context-agent.md:24,32-35,57-62` | 单源 |
| 14 | **P2** | hook 内部预算 10s > 宿主 5s，SessionStart 第二段必截断 | `hooks.json:22` vs `session_start.py:28,59` | 双源 |
| 15 | **P2** | `guard_runtime_write` deny 通道写 stderr（契约应为 stdout） | `guard_runtime_write.py:57-62` | 单源 |
| 16 | **P2** | 活跃 v7 skill 无条件调用 `update-state`（纯 v7 仓必然空转） | `webnovel-plan/SKILL.md:223`、`webnovel-review/SKILL.md:119` | 单源 |
| 17 | **P2** | `settings_digest.py` 半孤儿 + `context_settings_digest_max_chars` 死旋钮 | `settings_digest.py`、`config.py:250` | 单源 |
| 18 | **P2** | `_find_setting_path` 逐字节重复且已行为分叉 | `v7_write.py:150` vs `settings_digest.py:65` | 单源 |
| 19 | **P2** | 待办入口真空（`AGENTS.md:68` 的规则已不可执行） | `AGENTS.md:60` | 单源 |
| 20 | **P2** | `docs/README.md` 索引漏收 47 项（含 3 份现行 guide） | `docs/README.md` | 单源 |
| 21 | **P2** | `04-roadmap.md` 表格与正文矛盾（W6） | `04-roadmap.md:18/24` vs `:61-85` | 单源 |
| 22 | **P2** | `v7.1.0` 有发布说明、无 tag；校验器不设防 | `releases/v7.1.0.md`、`validate_release_notes.py:104` | 单源 |
| 23 | **P2** | `.opencode/commands/review.md:44` 仍写 14 工具（实为 12） | 同左 | 复验 |
| 24 | **P2** | 审阅期间同分支同工作树并发写入（W13） | `3b7b5e0` | 实证 |
| 25–33 | **P3** | 见第八节「P3 项」9 条 | — | — |

---

## 十、处置建议（按顺序）

**第一批——恢复门禁（最高优先）**

1. 取 `tests-windows` job 日志定位 CI 根因；修复前，`AGENTS.md` 与复审报告中「发版链全绿」的表述应改为「本机全绿，CI `tests-windows` 自 09-15 起红」。
2. 把 `.github/workflows/plugin-release.yml:9,49` 与 `plugin-version.yml:7,17` 的 `.claude-plugin/plugin.json` 改为 `.zcode-plugin/plugin.json`（或直接删掉这两条路径过滤，因为清单已在 `.zcode-plugin/`）；同步订正 `docs/operations/plugin-release.md:49,94`。修完重跑 `Plugin Release` 可补建 v8.1.0 / v8.1.1 的 GitHub Release（工作流本身支持补建）。

**第二批——安全加固（小改动、高收益）**

3. `material_intake.py` 的 `batch`、`regen_gallery.py` 的 `key` 一律过 `security_utils.sanitize_filename` + `resolve()` 后 `relative_to(project_root)` 校验（工具已存在，属漏用）。补两条回归测试（含 `../../../` 负例）。
4. `mcp/server.py`：把 `run_webnovel_cli` 纳入 `call_tool` 的 `try`，并对 `arguments` 做类型校验（非 str/list 标量直接回 `-32602`）。补一条「`project_root` 传 int 不得崩进程」的回归测试。

**第三批——插件内一致性（改文档即可，但注意 P2-7 的互锁）**

5. `agents/data-agent.md` 的 `chapter-commit` 互锁**请主动裁决**（09-15 曾修又被有意还原，属搁置张力而非漏改）。二选一：①删掉 `run_behavior_evals.py:121` 的 `"chapter-commit"` 必需项，再把文档改成 v7 等价物；②保留原文，但在 `data-agent.md` 顶部把「本文件描述的是已撤的 v6 写链，仅存 schema 参考价值」写明。**无论选哪条，都要同批改文档与评测**，否则评测先红。
6. `reference-loading-map.md:92` 与 `webnovel-learn/SKILL.md:24` 二选一，按 §3.1「v7 不再有 project_memory.json」订正索引。
7. `webnovel-plan` / `webnovel-review` 两条 skill 里的 `update-state` 改为 v7 等价路径（或按书仓形态分流）。
8. `docs/architecture/overview.md:61` 的 `Stop` 改 `PostToolUse`；`docs/guides/commands.md:3` 的「前 8 条」改「前 7 条」或补上 `doctor` 的 skill 转发。

**第四批——清理（可排期）**

9. `settings_digest.py` 定性：删 L0 摘要链 + 死旋钮，或加注释说明保留理由。
10. `_find_setting_path` 两处合一（优先保留已跟进双布局收口的那份）。
11. `hooks/hooks.json` 的 SessionStart 超时与 `session_start.py` 内部预算对齐（把内层降到 ≤4s 之和，或把宿主超时提到 10s）。
12. 重建待办入口：在 `AGENTS.md`「当前状态」指定一份在役入口（建议新建 `docs/reports/2026-09-20-待办入口.md` 或复用本报告的问题清单）。

---

## 附：本报告的核查边界（如实声明）

- **未取得 CI 日志**：`/actions/jobs/{id}/logs` 返回 403（需仓库 admin 权限），故 P1-1 的根因**未定论**，只给出排除性证据与三次本地复现结果。
- **未比对上游** `lingfengQAQ/webnovel-writer`（本仓自 v6.2.1 分叉后独立演进）。
- **未审**：`.tmp/`、`.tmp_story_system_engine/`、`__pycache__/`、`node_modules/`、`dashboard/frontend/dist/`（预打包产物）、`docs/archive/`。
- **v6 frozen-legacy 缺 v7 特性不计为缺陷**（沿用 2026-09-13 复审口径）。
- 第五节、第七节、第八节的多数条目来自只读探索代理，**证据等级**已在问题清单末列标注：标「复验」者由本审阅逐条重跑命令确认；标「单源」者仅代理单方证据，**处置前建议再核一次**。
