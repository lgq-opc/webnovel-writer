# 待办全景梳理（2026-09-11）

> **类型**：现状诊断 / 只读梳理（无代码改动、无生产文件变更）
> **方法**：遍历项目内全部待办入口文档 + todohub 待办库 + git 分叉与工作树实测
> **证据口径**：本文所有数字均为 2026-09-11 本机实测命令输出，逐条见 §5 证据附录；未实测者显式标注「假设」
> **快照声明**：本文是某一时点的截面，不取代 `AGENTS.md`「当前状态」中的待办入口指针（`AGENTS.md:59`），也不取代 `docs/TODO-from-v8-author-review.md` 本身。引用本文具体数字前请重新实测。

---

## 0. 结论速览

| # | 待办 | 性质 | 状态 |
|---|---|---|---|
| A | **CI 首次真跑** | 悬空的验证 | 改动已落地，**待一次真实 CI 运行确认**（1 条在册 + 1 条同类悬空） |
| B | **v6 线退役 Phase 2 剩余 + Phase 3** | 进行中的方案 | Phase 1 与 Phase 2 增量 1–3 已完成；剩余项已定位，**Phase 3 需 Human 决策** |
| C | **21 条提交未推送** | 仓库动作 | 本地干净、可 fast-forward，**待 Human 批准 `git push`** |
| D | **README / marketplace.json 账号名漂移** | 本轮新发现 | 已登记 `t-20260911-7c7b` |

**一句话**：v8-author 分支的**缺陷修复队列已经清空**，剩下的不是「还有多少 bug」，而是**三类收尾**——把已验证的成果推出去并让 CI 认账、给 v6 退役的剩余部分定性、以及一处文档漂移。

---

## 1. 待办入口地图

项目内共有 5 份文档声称或曾声称自己是「待办清单」。判定归属如下：

| 文档 | 判定 | 依据 |
|---|---|---|
| `docs/TODO-from-v8-author-review.md` | ✅ **当前权威入口** | `AGENTS.md:59` 明确指向 |
| `docs/plans/2026-09-10-v6线退役方案.md` | ✅ **并行进行中** | 文件头状态 `[~] Phase 1 已完成`；`AGENTS.md:54` 指向其 Phase 2 |
| `docs/zcode/v8-gap-review-3rounds/README.md` | ⬜ 历史（已完成） | P1-1 ~ P4-3 全部 ✅（2026-09-04 收官） |
| `docs/plans/2026-08-25-status-and-pending-work.md` | ⬜ **已 superseded** | 文件头自述「不再作为待办入口」 |
| `docs/architecture/author-friendly-reporting-plan-2026-06-07.md` | ⬜ 历史施工细目 | 文件头「状态说明」自述未勾选项为历史细目 |
| `docs/cursor/项目复审/2026-09-04-项目复审报告.md` | ⬜ 大部分已闭 | P0-1 ~ P0-4 见 §3 |
| todohub 待办库 | ⬜ 空 | 该项目**无未完成条目**（实测） |

> **含义**：盘点时只需看前两份文档；其余四份的 `[ ]` 若被 grep 扫出来，属**误报**，不要重复排期。本次梳理即因此先做归属判定。

---

## 2. 真正剩下的待办

### 2.1 A 类：CI 首次真跑（一件事关掉两条悬挂）

两条在册悬空项本质是同一件事——**CI 从未真实运行过**：

| 在册项 | 出处 | 原文自述的未证实点 |
|---|---|---|
| P1「补一条跑测试套件的 CI workflow」 | `TODO-from-v8-author-review.md:17` | 「真实 Linux 结果待 workflow 首次触发确认」 |
| **N-2 `[~]`** | `TODO-from-v8-author-review.md:48` | 「GitHub windows runner 的真实结果无法本地验证」 |

N-2 的改动**已完成**（非「未做」）：`actions/checkout@v4→@v7`、`actions/setup-python@v5→@v7`（版本号经 GitHub API `releases/latest` 查证，非凭记忆）；新增 `tests-windows` job——**刻意不设 `PYTHONUTF8`**，专门复现「用户照 AGENTS.md 原样敲命令」的场景；`on.push/pull_request.paths` 补 `conftest.py`。

**为什么本地验不了**：本机是中文 Windows（ANSI=GBK），GitHub runner 是 en-US（ANSI=cp1252）。N-1 的整条修复思路就是「让测试不再依赖 locale」，而这一点**只有换一个 locale 真实跑一次才算验过**。

- **触发条件**：`.github/workflows/plugin-tests.yml` 在 push/PR 触及 `webnovel-writer/scripts/**`、`mcp/**`、`dashboard/**`、`conftest.py` 且分支为 `master`/`v8-author` 时触发。
- **关联**：本项与 §2.3 的推送是**同一个动作**——推送 21 条提交即触发 CI。故建议合并执行。

### 2.2 B 类：v6 线退役（Phase 2 剩余 + Phase 3）

出处：`docs/plans/2026-09-10-v6线退役方案.md`。文件头状态：`[~] Phase 1 已完成；Phase 2 第 1 条已完成`。

**已完成的部分**（不再重复排期）：

| 里程碑 | 证据 |
|---|---|
| Phase 1 写路径软退役 | `ae512b2`；SKILL 499→275 行；`dual_format_guard` 与 `write_gates` 解耦 |
| Phase 2 第 1 条 v7-native 初始化 | `629a899`（`book-init`，播种 book.yaml + 六域骨架） |
| Phase 2 增量 1：v6 提交链簇 + memory-contract 簇 | `511531e`；−6747 行 / 24 文件；测试 1693→1528 |
| Phase 2 增量 2：`write_gates` 整包 | `438e45e`；−1291 行 / 11 文件；测试 1528→1501 |
| Phase 2 增量 3：`story-events` 簇 | `1b5bf95`；测试 1501→1489 |

**剩余项（3 类）**：

**(a) §1.5 B 类「需拆」4 个模块**——拆分点已定位，未执行：

| 模块 | KEEP 侧引用者 | 拆分点 |
|---|---|---|
| `continuity_check.py` | `timeline_view.py`、`chapter_outline_validate.py` | 拆出 `load_known_names` / `parse_anchor_day` / `build_age_columns` / `_book_age_base` |
| `artifact_validator.py` | `user_report.py` | 先让 `user_report.py` 去掉 v6 五投影相关的 6 个 import |
| `memory/writer.py` | `state_manager.py`、`memory/store.py` | **归属待定，需单独判定** |
| `context_weights.py` | `config.py`（`TEMPLATE_WEIGHTS_DYNAMIC_DEFAULT`） | 方案建议**整模块保留**（体量小），只删 v6 专用权重 |

> **纪律提醒**：方案 §1.5 D-2 明确要求「B 类拆出的共享函数**必须有测试留存**」，否则 v7 侧失去守护。

**(b) `contract_migrations` —— 方案明确「暂缓，不随本轮删」**：删它的前提是砍掉 doctor 的 v6 合同检查组，而那会拿「存量 v6 用户的可诊断性」换模块行数。方案建议**合并到 Phase 3** 一并决定 v6 诊断面是否下线。

**(c) Phase 2 第 2 条「删 v6 CLI 组」——已实测大面积不可执行**：

增量 3 前做过一次面向技能的命令引用反查，结论是 §1.2 列出的剩余 v6 CLI 命令**几乎每条都被仍在服役的技能/代理引用**：

| 命令 | 引用者 | 判定 |
|---|---|---|
| `preflight` | **webnovel-write（v7 技能）**、dashboard | v7 在用，不可删 |
| `extract-context` | **context-agent**（v7 写链降级路径） | 被在役代理依赖 |
| `review-pipeline` | **webnovel-review 技能 + reviewer 代理** | 在用 |
| `update-state` | **webnovel-plan + webnovel-review 技能** | 在用 |
| `state`/`index`/`memory`/`entity`/`context` | init/plan/query/learn/dashboard 技能 | 在用 |
| `backup`/`archive` | query 技能 | 在用 |
| `story-events` | 无 | ✅ 已删（增量 3） |

方案自己的建议：把 Phase 2 剩余**重定性为「冻结而非删除」**——v6 代码保留、标注 frozen-legacy（Phase 1 已做）、不再新增。

**(d) Phase 3 读侧重建 —— 方案标注「需要 Human 决策」**：

| 待决策项 | 选项 |
|---|---|
| dashboard | 改读 v7 `.cache` 还是冻结下线 |
| RAG / 记忆 | 接 v7 cache 还是下线 |
| `knowledge_query` / `reader_signals` | 接 v7 cache 还是砍 MCP 工具 |
| MCP 工具面 | 可能 14 → 约 9，涉及 README / AGENTS / overview 承诺数同步 |

> Phase 3 的本质（方案原文）：不是「删代码」，而是**决定这些功能归谁**。v2 实测佐证——pack 的读者信号/素材/作者模型节在纯 v7 仓本就降级为空，Phase 3 即决定这些节是「补数据源」还是「正式承认无此功能」。

### 2.3 C 类：21 条提交未推送

| 检查 | 实测结果 |
|---|---|
| 当前分支 | `v8-author`（工作树干净） |
| 本地领先 `origin/v8-author` | **21 条** |
| 未推送范围 | `7585257`（v6 退役方案）→ `1b5bf95`（Phase 2 增量 3） |
| 分叉情况 | `origin/v8-author` **是 HEAD 的祖先** → 可 fast-forward，**无分叉** |
| 本地领先 `origin/master` | 152 条 |
| `master` 独有提交 | 0 条 |
| 本地 tag | `v7.0.0` / `v8.0.0` / `v8.1.0` |
| HEAD 相对 tag | `v8.1.0-31-g1b5bf95` |

**未推送的 21 条包含**：v6 退役 Phase 1 + Phase 2 增量 1–3，以及一批已独立核验的 F 系列 / N 系列修复（F4/F5/F6/F7/F8、N-1、N-4 等）。

**工作区集成**：外层工作区 `git status` 显示 `projects/zcode-plugins/webnovel-writer` 为 modified——**子模块/gitlink 指针未随本仓新提交同步**，需在外层仓一并提交指针。

> **须 Human 批准**：`git push` 属 AGENTS.md 安全红线内的远程操作，不自行执行。

### 2.4 D 类：README / marketplace.json 账号名漂移（本轮新发现）

**事实**：`git remote -v` 实测为 `git@github.com:lgq-opc/webnovel-writer.git`（fetch/push 均是），但下列位置仍写旧账号名：

| 位置 | 内容 |
|---|---|
| `README.md:17` | 「本仓 `alittleseven/webnovel-writer`」 |
| `README.md:109` | 装机指引「添加 marketplace：GitHub 仓库 `alittleseven/webnovel-writer`」 |
| `README.md:118` | 依赖下载 URL `raw.githubusercontent.com/alittleseven/...` |
| `marketplace.json` | `owner.name`、`plugins[0].author.name`、`homepage` 仍写上游 `lingfengQAQ` |

**性质**：2026-09-10 的 P3 待办只修了 `AGENTS.md` 一处（已改为 `lgq-opc`），**未覆盖 README 与 marketplace.json**。交付物是「ZCode 插件市场装机源」，指向与实际 `origin` 分叉的仓库名。GitHub 对旧账号名会 302 重定向，故未必当场报错——属**静默漂移**。

**不在本项范围**：`README.md:9` 的 Trendshift 徽章与 `:396` 的 Star History 指向上游，是维护者**主动确认保留**的现状（见 `TODO-from-v8-author-review.md` §已验证无需处理），非缺陷。

**已登记**：`t-20260911-7c7b`。

---

## 3. 已关闭与历史（防重复排查）

| 文档 / 条目 | 关闭依据 |
|---|---|
| `TODO-from-v8-author-review.md` 的 P1 / P2 / P3 全部 11 项 | 均 `[x]`，且经 `evidence-verifier` 独立核验属实（2026-09-10） |
| 同文档 N-1 / N-3 / N-4 | 均 `[x]`，附修前修后对照实测 |
| 同文档 F1 – F8 全部 | 均 `[x]`，逐条附真仓或构造仓实测证据 |
| 同文档 §已验证无需处理 | 六项交接假设已验证通过；README 徽章指上游为主动保留 |
| `docs/zcode/v8-gap-review-3rounds/README.md` 41 项缺口 | P1-1 ~ P4-3 **全部 ✅**（2026-09-04），含范围/实现对照表 |
| `docs/cursor/阶段一 ~ 阶段四` spec/plan | 即上述 4 阶段的宿主产出，随阶段完成 |
| `docs/plans/2026-08-25-status-and-pending-work.md` | 文件头 `[superseded]`（v7.0.0/v7.1.0 已交付其 P2 四项） |
| `docs/architecture/author-friendly-reporting-plan-2026-06-07.md` 的 `[ ]` | 文件头自述为历史施工细目；核心 runtime 已落地 |
| `2026-09-04-项目复审报告` P0-2 | AGENTS.md「当前状态」已为 v8.1.0 / `v8-author` / 13 命令 / 14 MCP |
| `2026-09-04-项目复审报告` P0-3 | `2026-08-25` 清单已标 superseded；待办入口已改指 |
| `2026-09-04-项目复审报告` P0-4（F-13 追溯断裂） | 由 P4-1 八组 `gov.*` 治理检查落地（`c0c5981`，fantasy01 实测 8/8） |
| `2026-09-04-项目复审报告` P0-1 | **部分关闭**——装机源已不再指向上游 `lingfengQAQ`，但账号名仍旧 → 见 §2.4 |

---

## 4. 建议执行顺序

1. **推送 21 条提交**（需 Human 批准）→ 同一动作触发 CI → **一次关掉 §2.1 的两条悬挂**（Linux 结果 + N-2 的 windows runner 结果）
2. 外层工作区**同步子模块指针**（与本仓推送同批）
3. **Phase 3 功能归属决策**（需 Human 拍板；它同时解锁 `contract_migrations` 与 Phase 2 的「冻结 vs 删除」定性）
4. Phase 2 剩余：§1.5 B 类 4 个模块**先拆后删**（拆出的共享函数须留测试）
5. 顺手清 §2.4 的账号名漂移（低风险、纯文档）

> **为什么 1 排在 2 之前**：CI 只在推送时触发，而 CI 结果可能反过来要求改代码——先生效再决策，避免在未验证的基线上继续叠加。

---

## 5. 证据附录（2026-09-11 本机实测）

```bash
# —— 工作树与分支 ——
git status --short                      # → 空（干净）
git branch                              # → master / tmp/zcode / v8-author(当前)

# —— 分叉 ——
git log --oneline origin/v8-author..v8-author | wc -l   # → 21
git log --oneline origin/master..v8-author   | wc -l    # → 152
git log --oneline v8-author..master          | wc -l    # → 0
git merge-base --is-ancestor origin/v8-author HEAD      # → exit 0（可 fast-forward）
git log --oneline origin/v8-author..HEAD | tail -1      # → 7585257 docs(plans): v6 线退役方案…
git log --oneline origin/v8-author..HEAD | head -1      # → 1b5bf95 refactor!: 删除 story-events…

# —— tag ——
git tag -l                              # → v7.0.0 / v8.0.0 / v8.1.0
git describe --tags HEAD                # → v8.1.0-31-g1b5bf95
git ls-remote --tags origin             # → 远端含 v7.0.0 / v8.0.0 / v8.1.0

# —— 远程 ——
git remote -v
# → origin  git@github.com:lgq-opc/webnovel-writer.git (fetch)
# → origin  git@github.com:lgq-opc/webnovel-writer.git (push)

# —— 账号名漂移 ——
grep -n "lingfengQAQ" README.md         # → 9 / 19 / 109 / 396
grep -rn "alittleseven" README.md marketplace.json
# → README.md:17 / 109 / 118；marketplace.json: owner.name / plugins[0].author.name / homepage

# —— 待办库 ——
mcp__todohub__todo_list(project="projects/zcode-plugins/webnovel-writer")
# → （无未完成条目）
```

**未实测项（显式标注）**：

- `alittleseven` → `lgq-opc` 系账号改名还是两个独立仓库，**未验证**；本项只主张「文档与实际 `origin` 不一致」这一事实。
- CI 在 `ubuntu-latest` / `windows-latest` 上的真实结果——**本文撰写时尚未触发过**，即 §2.1 的待办本身。

---

## 6. 与既有文档的关系

- 本文**不是新的待办入口**。待办入口的权威指针仍为 `AGENTS.md:59`；本文是某一时点的**诊断截面**。
- 本文**不修改** `docs/TODO-from-v8-author-review.md` 与 `docs/plans/2026-09-10-v6线退役方案.md`——那两份仍各自维护自身条目状态。
- §2.4 的新发现已作为独立待办登记（`t-20260911-7c7b`），不留在本文里等待被记起。
