# 05 · 维护一本书的完整目录结构

> 基座：v7 story-repo 实跑形态（fantasy01）+ A 线六域契约 + 织机 Loom 文风域借鉴，经批判一/三轮修正后定稿。
> 作者日常只碰四处：`大纲/`、`素材/活/`、`定稿/正文/`（AI 写、作者可改）、`设定/`。其余为编译产物或系统域。

## 1. 六域总图（+ 演化事件域）

> **口径（2026-09-12 对账 D-1 裁决 = A）**：六域 = 大纲 / 正文(定稿) / 设定 / 素材 / 作者 / **文风**；**演化是附带事件域**，不计入六域。
> 依据：`domain_contract.py:1-3` 的实现口径 + `REQUIRED_DIRS` 含 `文风` 而不含任何演化目录。
> 低层原因：`演化/` 的三个使用点全部对目录缺失免疫（`freeze_manager.py:108` / `setting_forge.py:219` 按需 mkdir，`invariant_check.py:309` 缺失降级为空集），而 `文风/` 缺失是真缺口。
> 故下图 7 个框 = 六域 + 演化事件域。

```text
my-novel/                                # 书项目 = 独立 git 仓库（唯一真源）
├── book.yaml                            # 书级元数据（spec_version: "7.2"：新增 素材/作者/文风 三域声明）
│
├── 大纲域 ──────────────────────────────────────────────────────── 作者主战场①
│   ├── 总纲.md                          # 三区阶段结构（见 §2.1）：已写卷详案（冻结）/当前卷（活跃）/未来卷锚点
│   ├── regen/总纲/                      # 总纲 regen 版本画廊（v1.md v2.md … + diff 报告）
│   ├── 卷纲/
│   │   ├── 第01卷.md                    # 卷详案（Strand 分布/爽点密度/伏笔规划，现有形态保留）
│   │   ├── 第01卷-节拍表.md             # 现有形态保留
│   │   └── 第01卷-时间线.md             # 新增：timeline-check 导出的时间线视图（章×故事内时间×事件×伏笔点×战力锚点）
│   ├── 章纲/
│   │   ├── 0039.md …                    # 章纲卡（front matter：章号/标题/节点 CBN-CPNs-CEN/禁区/时间锚/承诺推进/素材引用）
│   │   └── regen/                       # 单章 regen 画廊（≤3 版 + diff）
│   └── 条目/                            # 承诺账本（三类条目，每条挂最晚回收章）
│       ├── 伏笔/F-001-xxx.md            # 状态：open/推进中/已回收/逾期/作废(retcon)
│       ├── 悬念/S-001-xxx.md
│       └── 感情线/R-001-xxx.md
│
├── 素材域 ──────────────────────────────────────────────────────── 作者主战场②（M2 新建）
│   ├── 活/                              # 活层：作者可自由编辑
│   │   ├── 桥段.csv  爽点节奏.csv  人设关系.csv  场景写法.csv  写作技法.csv
│   │   ├── 命名风格.csv  金手指零件.csv  世界观零件.csv  台词金句.csv  梗与反差.csv
│   │   └── README.md                    # 每张表 schema 说明（列约定见 06 §7）
│   ├── 定版/
│   │   └── v01/  v02/ …                 # 卷收尾冻结快照（每卷一目录，内容=当卷活层快照+冻结清单 manifest.json）
│   └── 使用轨迹.jsonl                    # 轨迹层：{章, 条目id, 定版版本, 用法} append-only
│
├── 设定域 ──────────────────────────────────────────────────────── 作者主战场③
│   ├── 世界观.md  力量体系.md  时间线.md  反派设计.md           # v7 定稿/设定 形态保留（半结构化 md）
│   ├── 名册.md  名册/角色/*.md                                 # 实体档案（现有形态）
│   ├── 力量锚点.yaml                    # 新增：战力锚点层（境界序/战例账本/通胀记录，见 06 §8）
│   ├── 信息差.md                        # 新增：谁知道什么（条目：信息点/知晓者/知晓章/泄露禁忌）
│   └── regen/工坊/                      # 设定工坊提案画廊（境界/功法/法宝/命名 四类，采纳后登记）
│
├── 正文域（定稿）────────────────────────────────────────────────  AI 写 + 作者可改（修改走 journal+retcon）
│   ├── 正文/0001-天裂.md …              # front matter（章号/标题/卷/字数）+ 正文（现有 v7 形态）
│   └── 记忆/章摘要/0001.md …            # L2 摘要（现有形态）
│
├── 文风域（M3 新建）─────────────────────────────────────────────
│   ├── 宪法.md                          # 文风契约（现有「设定集/风格契约.md」迁移并入）
│   ├── 金句库.md                        # 作者标记的高分片段（素材自喂入口）
│   └── 指纹.yaml                        # style_profile：量化文风指纹（句长分布/口头禅/标点节奏，脚本可算）
│
├── 作者域（M0/M3 新建）────────────────────────────────────────── 全部纯文本，无数据库
│   ├── journal.jsonl                    # append-only 修改事件流（06 §3）
│   ├── author_model.md                  # 项目层作者模型（从 journal+定稿归纳，LLM 生成、作者可改）
│   └── 跨书偏好.yaml                    # 用户层偏好（节奏/雷点/审稿习惯——可由宿主级配置同步）
│
├── 演化域（现有 v7 形态保留）─────────────────────────────────────
│   ├── run-ledger.jsonl                 # settle/vol/ch/retcon/fix 机器事件链
│   └── signals.jsonl                    # 旁路信号（品味进化输入）
│
├── 工作区/（gitignored）────────────────────────────────────────── v7 现有形态
│   ├── 决策卡-NNNN.md  决策-NNNN.json  草稿-NNNN.md  上下文包-NNNN.md
│
├── 审查报告/                            # 现有形态（第N章审查报告.md）
│
└── 系统域（编译产物，gitignored 或可重建）
    ├── .story-system/                   # 合同树（编译产物：由正典经 story-system 生成）
    ├── .webnovel/                       # 投影 read-model（state/index.db/tmp/logs）
    ├── .cache/                          # v7 缓存（可删）
    └── .loom/                           # 预留（loom 工具只读兼容视图，可选）
```

## 2. 关键目录语义

### 2.1 总纲三区（一个文件内的结构约定）

```markdown
# 总纲
## 甲区 · 已写卷详案（冻结区）        ← 第1~N-1 卷的详案快照，修改触发 impact+retcon 流程
## 乙区 · 当前卷活跃区                ← 第 N 卷详案，作者/AI 自由迭代，regen 只作用于此区
## 丙区 · 未来卷锚点（每卷 ≤10 行）    → 升级为详案时展开（从锚点扩写，走 regen 画廊）
```

### 2.2 素材三层流转

`活/`（作者编辑+AI 归纳追加+拆书投喂）→ 卷收尾 `freeze` → `定版/v{NN}/`（快照+manifest）→ 写作装配只取「定版+活层精选」；`使用轨迹.jsonl` 记录每章消费；`material-review` 每卷出淘汰/合并建议。

### 2.3 系统域的 git 策略

```gitignore
# 书项目 .gitignore
.cache/
工作区/
.webnovel/tmp/
.webnovel/logs/
# .story-system/ 与 .webnovel/index.db 是否入库：默认入库（体积可控、便于换机），
# 但 doctor 提供「重建对账」校验两者与正典一致 —— 漂移即报错。
```

## 3. 与既有形态的迁移映射

| 旧（v6/v7 现状） | 新（本方案） | 迁移 |
|------------------|-------------|------|
| `大纲/总纲.md`（平铺） | 三区结构 | `author-sync` 首跑自动分区（按已写卷号切分） |
| `.story-system/`（写前真源） | 编译产物 | 无数据迁移；改定位与生成方向 |
| `设定集/风格契约.md` | `文风/宪法.md` | `migrate` 子命令平移 |
| v6 override_ledger / v7 决策卡豁免 | `大纲/条目/` 承诺账本 | `migrate` 从 commit/合同抽取播种 |
| `设定集/力量体系.md` | 保留 + 抽取 `力量锚点.yaml` | 锚点抽取器（半自动，作者确认） |
| 插件 `references/csv/` | `素材/活/` 播种 | init 时按题材复制子集 |
| `project_memory.json`（.webnovel 内） | `作者/author_model.md` + `文风/指纹.yaml` | `migrate` 平移 patterns 为 author_model 播种 |

## 4. 300 章增长预估（容量设计依据）

| 资产 | 每章增量 | 300 章规模 | 说明 |
|------|---------|-----------|------|
| 正文 | 1 文件 ~2.5KB×字数（约 8-12KB） | ~300 文件 / ~3MB | git 可轻松承载 |
| 章摘要 | ~0.6KB | ~180KB | L2 层 |
| journal 事件 | 0-3 行/章（作者修改频率） | ~2-5MB jsonl | append-only，按卷归档选项 |
| 使用轨迹 | 2-4 行/章 | ~150KB | — |
| 素材活层 | 净增 0-2 条/章（含淘汰） | 稳态 200-400 条 | material-review 控制上限 |
| 定版快照 | 每卷一份活层快照 | ~30 卷 × 活层体积 | git 存量存储，可接受 |
| 条目（伏笔等） | 净增 0-1 条 | 稳态 open ≤20 条 | 逾期扫描强制收敛 |
| index.db / .cache | 线性 | ~50-100MB | 编译产物，可删重建 |

结论：纯文本+git 的容量曲线在 300 章规模完全可控；唯一需主动治理的是活层素材与 open 条目数量（material-review/逾期扫描即为此设计）。
