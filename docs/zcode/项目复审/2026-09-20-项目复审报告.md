# 项目复审报告（2026-09-20，ZCode）

> 审阅对象：webnovel-writer 仓库根（含 marketplace.json / opencode.json / .claude-plugin/），插件本体在内层 `webnovel-writer/`。
> 审阅基线：`v8-author` @ `0461637`（tag v8.1.1），工作树 clean。
> 审阅纪律：沿用 2026-09-13 复审口径（OpenViking 记忆卡「审阅范围与不计入项」）——v6 frozen-legacy 缺 v7 特性不算缺陷；不比对上游；不审 .tmp/、__pycache__/、node_modules/、dashboard dist/。
> 上一次复审：`docs/opencode/项目复审/2026-09-13-项目复审报告.md`（P0 空，v8.1.0 发版链全绿）。

## 结论

**P0 = 0，P1 = 0；P2 = 2，P3 = 5。** 全部验证命令通过，对外承诺逐项兑现，AGENTS.md「当前状态」未发现状态漂移。可以维持 v8.1.1 发布状态，不阻塞任何在途工作。P2 两项均为「孤儿模块处置」待拍板（已挂板 T-20260920-11 / T-20260920-12），不涉及功能缺陷。

## 一、必跑验证命令（全绿）

| 命令 | 结果 |
|---|---|
| `python -X utf8 -m pytest`（PYTHONUTF8=1） | **1510 passed**，覆盖率 **82.51%**（门槛 80），用时 2:03 |
| `validate_plugin_package.py` | OK，errors 0 / warnings 0 |
| `validate_release_notes.py` | OK（version 8.1.1 / previous_tag v8.1.0） |
| `sync_plugin_version.py --check` | Versions are in sync: 8.1.1 |
| `validate_reference_wiring.py` | OK，assets=63 consumers=53，**drift=0** |
| `smoke_v7_newbook.py` | **PASS 18 / BREAK 0**；2 BIZ 为既有基线（fixture 正文按设计被文笔门禁拒绝、timeline-check 输出需人工判读），非缺陷 |

对比上次复审（1513 passed / 82.64%）：测试数 -3、覆盖率 -0.13pp，系 context 链删除连带的测试精简，仍高于门槛，无预警信号。

## 二、对外承诺兑现对账

| 承诺 | 实测 | 证据 |
|---|---|---|
| MCP 只读工具 ×12 | ✅ 12 | `mcp/server.py:194-305`（where/project_status/doctor/setting_read/timeline_check/meter/knowledge/materials_status/materials_assemble/power_check/foreshadow_scan/reader_signals），全部转发 CLI 只读子命令，`foreshadow-scan` 硬编码 `--no-apply`（:162）保持只读 |
| 斜杠命令 ×13 | ✅ 13 | `commands/webnovel/` 13 个 .md（dashboard/doctor/forge/init/learn/materials/plan/power/query/review/status/style/write），引用接线 drift=0 |
| skills ×8 / agents ×4 / hooks ×4 | ✅ | 目录实测；hooks.json 四事件（UserPromptSubmit/SessionStart/PreToolUse×2/PostToolUse）全部 `${ZCODE_PLUGIN_ROOT}` + 5s 超时 |
| 六域书仓 | ✅（5/6 顶层目录，刻意） | `data_modules/domain_contract.py:75` 六域契约；`init_book.py:21` 明文自述「实测顶层域 5/6：`设定/` 不预建（内容属 advisory，按需创建）」；严格判定 `is_story_repo_strict` 有宽松路兜底 |
| `rag search` 对 v7 的口径 | ✅ structured unsupported | v7 实仓实测返回 `{"status":"unsupported","reason":"v7 书仓不提供语义检索…","hint":…}`，码 0——与 §3.1「RAG 正式下线」一致，非静默空结果 |
| `knowledge` v7 只答名册级 | ✅ 正式行为 | `server.py:247-249` 工具描述明示「2026-09-18 起正式行为，不是临时降级」 |

## 三、状态漂移对账（AGENTS.md「当前状态」vs 实况）

- 版本 **8.1.1 三处一致**：plugin.json / CHANGELOG 头部 / `SERVER_VERSION`（server.py:26）+ sync --check 通过。
- **tag v8.1.1 已推送远端**：`git ls-remote --tags` 实证（附注 tag 72770c3 → 剥离指向 0461637）。
- **marketplace.json 双位置一致**：根与 `.claude-plugin/marketplace.json` `diff` 为空。
- plugin.json `userConfig.bookProjectRoot.default: ""` 在位（D-4 修复 b6de678 无回归）。
- CI：`.github/workflows/` 三工作流在位，plugin-tests/plugin-release/plugin-version 对四校验 + 冒烟的接线与 AGENTS.md 描述一致（探索代理核查零残留）。
- `releases/v8.1.1.md`、`docs/reports/2026-09-12-需求与设计对账.md`、`docs/plans/2026-09-10-v6线退役方案.md` 均存在。
- 密钥门：`scripts/`、`mcp/`、`hooks/`、`skills/`、`commands/`、`agents/` 及两份 json 的硬编码密钥扫描**零命中**（api_key/secret/token/password + 16 位以上字面量）。

未发现任何「文档说做了、代码没有」或反向的漂移。

## 四、v6/v7 双布局路径收口复查（五风险点）

| 模块 | 状态 | 关键证据 |
|---|---|---|
| `chapter_paths.py`（181 行） | 已收口 | v7 优先分支 `定稿/正文/NNNN-*.md`（:126-134）+ v6 回退；测试覆盖 v7 命中与 v7 优先 |
| `backup_manager.py`（580 行） | 已收口 | 本地备份清单（:236）为八目录超集，v7 六域全覆盖；--no-git 书仓不自动 git init（:92-100）+ 统一降级闸；测试 `test_local_backup_covers_v7_six_domains` 等三例 |
| `placeholder_scanner.py`（76 行） | 已收口 | 双目录扫描 `("大纲","设定","设定集")`（:45-47，U-11）；测试覆盖纯 v7 与迁移期 |
| `settings_digest.py`（116 行） | 路径已收口（半孤儿见 P3-3） | `设定/` 优先、`设定集/` 兜底（:72）；测试覆盖目录顺序与纯 v7 仓 |
| `project_locator.py`（479 行，消费面最广：17 处 import/调用） | 已收口 | v7 判定走 `is_story_repo_strict`（:275-279）；报错文案 v7 感知；测试覆盖 v7 识别/子目录探测/裸 book.yaml 拒绑/双仓歧义 |

## 五、context 链级联删除完整性（§3.1 第③条复核）

**零悬空引用。** 已删的 7 个文件（extract_chapter_context / context_manager / context_ranker / context_weights / genre_aliases / writing_guidance_builder / genre_profile_builder）磁盘无残留；skills/commands/agents/hooks/mcp/scripts/pytest.ini/conftest/CI 全范围 grep 无指向已删文件的引用。保留的 `agents/context-agent.md` 引用的 5 个命令全部现存。残留仅四类良性形态：防回归守卫测试（`KNOWN_DELETED_FILES`、CLI 注册表断言、context-agent 主入口必须 v7-write pack）、历史注释、存量数据来源标签 `source: "context_manager"`（index_manager.py:228，兼容旧数据）、配置注释。

## 六、MCP server.py 质量审（通读）

adapter 模式零 runtime 复制；纯标准库（json/os/subprocess/sys/pathlib/typing）。安全与健壮性到位：`_reject_leading_dash`（:364-370，P3-1 加固防 argparse flag 注入）；子进程 `sys.executable -X utf8` 无 shell=True、25s 超时、UTF-8 errors=replace；exit_code≠0 如实标 `isError`（stdout 为 JSON 时也追加 `[exit_code]`，:355 不被 is_json 短路）。通知（无 id）不应答、parse error 回 -32700，协议面完整。未发现正确性问题。

## 七、问题清单

### P2（待拍板，已挂板）

1. **`data_modules/prewrite_validator.py` 是 context 链删除的漏网孤儿**。c1f4432 显示其唯一生产消费者是被删的 `from .prewrite_validator import PrewriteValidator`（原 context_manager 内），现全仓仅 `tests/test_prewrite_validator.py` 引用；a766365 孤儿清理未扫到。按 §3.1 第③条级联删除口径应补删（连同测试），或显式标注保留理由。→ **T-20260920-11**
2. **`data_modules/author_glossary.py` 零生产消费者**（作者术语表 glossary/v1，仅测试引用）。接线进 v7 链路或标注/移除。→ **T-20260920-12**

### P3（知悉即可，建议下次清理时顺手处置）

3. `settings_digest.py` 半孤儿：L0 摘要构建链（`digest_dir`/`get_setting_digest`）生产消费者为零（原主已删），仅 `find_setting_path` 仍被 setting-read 兜底消费——与 c1f4432 提交注记「memory/ 与 context_budget 仍保留」同属有意保留的无主功能，建议定性后注释说明。
4. `backup_manager.py` 两处轻微残留：本地备份清单不含根级 `book.yaml`（git 模式 `git add .` 会带上，--no-git 本地模式不拷，:236 只有目录）；文件头 docstring「每次 /webnovel-write 完成后自动 commit」是 v6 git 模式语义，v7 settle 不自动触发备份（入口仅 CLI `backup`），对 v7 用户有误导。
5. AGENTS.md「播种 book.yaml + 六域骨架」与实际 5/6 域有措辞精度差（代码侧 `init_book.py:21` 已自述，属文档表述问题）。
6. `index_manager.py:228` `source: str = "context_manager"` 数据标签默认值残留（良性，存量数据兼容；改名需连带迁移判断，不必须）。
7. `scale_drill.py` 零 import 引用但自带 `__main__`（:283）可手动跑——按既有「__main__ 入口豁免」规则不算孤儿，从严属半孤儿，知悉即可。

## 八、测试质量与常规维度

- **行为导向抽样**：`test_webnovel_cli_v7_write.py`（转发/门禁退出码/无根诊断不吐 traceback）、`test_backup_manager.py`（断言快照内真实文件）、`test_project_locator.py`（v7 场景矩阵）、守卫测试（断言已删文件保持删除、context-agent 主入口指向）——均为断言可观测行为而非内部实现。v7 写链有 gates/pack_sections/post_hooks/run_log/cache 五个专项测试文件 + data_modules/tests 89 个测试文件，覆盖结构健康。
- **正确性**：全量 pytest + 冒烟 + 四校验全绿；MCP 通读未发现逻辑缺陷。
- **安全性**：密钥零硬编码；MCP 双层参数加固；子进程无 shell；hooks 5s 超时防卡宿主。
- **性能**：MCP 每调用一子进程（25s 上限）是有意设计，只读查询频度下无问题。
- **风格一致性**：中文注释/UTF-8 纪律统一；模块头部均带出处注记（如 U-11/U-12、t-20260913-f847），可追溯性好。

## 九、处置建议

P2 两项待拍板后执行；P3-4 的 docstring 误导建议随下一次触碰 backup_manager 的改动顺手修（一行改文档）；P3-5 建议 AGENTS.md 措辞改「播种 book.yaml + 六域骨架（设定/ 按需）」。其余不动。

## 十、处置记录（2026-09-20 当日裁决并执行）

用户裁决：**1 补删、2 移除、其他按建议**。已落地：

- **P2-1 补删**：`data_modules/prewrite_validator.py` + `tests/test_prewrite_validator.py`（§3.1 第③条 context 链级联删除口径的执行遗漏补齐）。删除前复核：全仓仅测试引用，其依赖的 `placeholder_scanner` 另有活消费者不受影响。
- **P2-2 移除**：`data_modules/author_glossary.py` + `tests/test_author_glossary.py` + 其唯一数据源 `references/author_glossary.json`（随功能一并移除）。
- **P3-3 定性注释**：`settings_digest.py` 文件头加状态注记（find_setting_path 持续服役；L0 摘要构建链无生产消费者、有意保留待接线或后续裁决）。
- **P3-4 docstring 修正**：`backup_manager.py` 文件头「自动 commit」改为 v6/v7 分述（v7 不自动触发备份，入口 CLI `webnovel.py backup`）。book.yaml 不入本地快照清单维持现状（git 模式经 `git add .` 覆盖）。
- **P3-5 措辞修正**：AGENTS.md 改「播种 book.yaml + 六域骨架（`设定/` 按需创建，其余五域即建）」。
- P3-6（context_manager 数据标签）、P3-7（scale_drill）按建议不动。

删除后验证：pytest **1504 passed** / 覆盖率 **82.47%**（-6 测试恰为两删测试文件之和），打包校验 / 发版说明 / 版本一致 / 引用接线 drift=0 全绿。`references/author_error_catalog.json` 中 `prewrite_validator_blocking` 字样为历史错误码目录数据（活模块 error_catalog.py 消费该文件本体），按最小改动原则保留未动。
