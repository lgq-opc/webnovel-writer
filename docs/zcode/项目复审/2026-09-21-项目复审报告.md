# 2026-09-21 项目全面复审报告（ZCode 宿主）

> 状态：v1.2（定稿。R1 12 处修正 + R2 10 条意见已全数修订，R3 验证复审 0 blocker / 0 major 通过，3 条 minor 已随手修；审阅处置记录见 §7）
> 基线：`v8-author` @ `91ccc2d`，与 `origin/v8-author` 同步；领先 `origin/master` 1 笔（`91ccc2d`）；除本报告文件（untracked）外工作树干净。
> 方法：3 个并行只读审计子代理（插件组件面 / CLI 脚本面 / 文档-代码对账）+ 主会话本机硬证据（全量 pytest、三校验脚本、远端 tag 与 Release 页核验）+ R1/R2 两轮独立审阅复核。全程未修改任何生产文件。

## 0. 结论摘要

**已扫描范围内代码实现与功能面全绿，文档对账无 P0/P1 级失配**。`docs/reports/2026-09-20-项目全面审阅报告.md` 所列 P1-1/2/3（CI 红根因、发版链断链、路径穿越）的修复均确认落地；看板 ai-webnovel 工作区无未领/进行中/待拍板卡。

本旬发现的问题集中在**文档状态滞后**（4 份计划与 1 份台账未回写、2 份 README 共 5 处失配、1 处悬空引用、1 处死错误码、1 处过期计数）与**真正未完成的队列项**（S11 模型路由待作者输入便宜模型标识，及若干已触发/待排期项）。最高为 P2 级，无一阻塞 v7 写链。

硬证据：

| 项 | 结果 |
|---|---|
| 全量 pytest | **1530 用例全绿**（exit 0），覆盖率 82.62% ≥ 80% 门槛 |
| validate_plugin_package.py | OK，errors 0 / warnings 0 |
| validate_release_notes.py | OK，version 8.1.1 / previous_tag v8.1.0 |
| sync_plugin_version.py --check | Versions are in sync: 8.1.1 |
| 行为评测 fast 套件 | 21/21 PASS、0 FAIL（实测，README 所写 23 已过期，见 P3-⑦） |
| 版本一致性 | plugin.json / 双 marketplace.json / SERVER_VERSION / releases/v8.1.1.md / README 徽章五方均 8.1.1；README 分支表例外（v8.1.0，见 P3-④） |
| 远端 tags | v8.1.0、v8.1.1 均在（v8.1.1 → `0461637`，与本地一致） |
| 远端 Releases 页 | v8.1.1 / v8.0.0 / v7.0.0 三份齐；**v8.1.0 Release 仍缺**（P3 运维项，见 §5） |

## 1. 绿面清单：代码-文档一致项

### 1.1 插件组件面（manifest vs 磁盘，零差异）

- **组件计数与 AGENTS.md 宣称逐一相符**：skills 8（每目录均有 SKILL.md）、agents 4、commands/webnovel 13、hooks 脚本 4 + hooks.json、mcpServers 1、userConfig 3（`bookProjectRoot` 带 `"default": ""`，即 v8.1.1 修复项）。
- **MCP 恰 12 个只读工具**（`webnovel-writer/mcp/server.py:192-306` TOOLS 列表）：where / project_status / doctor / setting_read / timeline_check / meter / knowledge / materials_status / materials_assemble / power_check / foreshadow_scan / reader_signals；全部经 `run_webnovel_cli`（server.py:315）映射到只读子命令，`foreshadow-scan` 显式 `--no-apply`，全文件 grep `rag_search`/`"context"` 零命中（14→12 撤出属实）。
- **hooks 接线规范**：5 条 hook 记录（UserPromptSubmit→chapter_meter_hook、SessionStart→session_start、PreToolUse×2→guard_runtime_write 复用、PostToolUse→chapter_body_trace），全部 `${ZCODE_PLUGIN_ROOT}` 引用、无硬编码路径、4 脚本均在盘。
- **marketplace.json 双位置**（外层根 + 外层 `.claude-plugin/`）`diff` 逐字节相同，均 8.1.1、source 指 `./webnovel-writer`；内层无 `.claude-plugin/`（与 AGENTS.md「双位置在外层根」口径一致）。
- **dashboard 预打包产物**在 `webnovel-writer/dashboard/frontend/dist/`（19 文件，git 跟踪）；references/ 12 项、templates/ 4 项、evals/ 2 项均在。
- **引用完整性（R2 补强核验）**：skills 交叉引用闭环——`skills/webnovel-write/SKILL.md:127`（reviewer 写 `.webnovel/tmp/review_results.json`）与 `scripts/v7_write.py:609-612`（settle 门①读该文件）互相咬合；`references/index/reference-loading-map.md` 40 个文件引用 37 在盘、3 个缺失项位于 `:118`「退役登记」刻意注记，非失效；`evals/agents/evals.json` 引用的 fixture 均在盘，fast.json 引用的 8 个 skill 名全部存在；fast 套件实跑 21/21 PASS。

### 1.2 CLI 脚本面（文档引用 vs 实际命令面，零失配）

- `scripts/webnovel.py`（薄壳）→ `data_modules/webnovel.py`：**52 个顶层子命令**（`--help` choices 与 `sub.add_parser(` 双口径计数一致），其中 **19 个含二级动作或二级子命令组**（16 个 choices 型 + meter/run-ledger/knowledge 3 个嵌套子命令）。v7 写链四命令 `v7-write decision/pack/check/settle`（webnovel.py:1118）、`book-init`、`preflight --all`、`materials` 12 动作、独立脚本 `migrate_v6_to_v7.py` 全部在位且 live `--help` 通过。
- **组件文档 CLI 样例全量对照零失配**：8 个 SKILL.md + 4 个 agents/*.md + 13 个 commands/webnovel/*.md 共 25 个组件文档中，`python -X utf8 … webnovel.py` 调用样例共 **83 行**（`grep 'python -X utf8.*webnovel\.py'` 可复现），另 hooks 脚本内 3 处调用点；逐条与 argparse 定义比对**零失配**。SKILL 样例省略 `--repo` 属正确——桥接逻辑自动把 `--project-root` 注入为 `--repo`（`data_modules/webnovel.py:393`）。
- **data_modules 生产模块 94 个（顶层 86 + memory 子包 8，含两级 `__init__.py`）无孤儿**：初扫 prod=0 的 22 个模块经动态分发（`_run_data_module`/importlib/material_store 二次分发）复核全部找到功能性消费者。上轮删净验证：`prewrite_validator.py`/`author_glossary.py` 磁盘不存在、全仓零代码引用；`3b7b5e0` 实证删除 5 个文件（2 模块 + 2 测试 + 1 个 references/author_glossary.json）。

### 1.3 文档-代码对账（AGENTS.md 当前状态 14 项抽验全一致）

- 版本 v8.1.1 与 tag 推送、MCP 14→12、组件计数、v6 冻结 + book-init/migrate 入口、CI 四特性（`plugin-tests.yml:49` fetch-tags、`:117` ::error 注解、`:85-86` tests-windows 刻意不设 PYTHONUTF8、requirements.lock 锁定 + `scripts/conftest.py:26-30` 8.3 短路径 resolve 修复 + `runtime_compat.py:73-79` argv0 判据）、三份旧待办入口确已清空/降级（TODO-from-v8-author-review 24 条目级 `[x]` 零 `[ ]`；v8-gap-review-3rounds P1-1~P4-3 全 ✅；08-25 状态文档双层 `[superseded]`）、三份现状截面文件在位——全部与代码一致。
- **上轮 P1 及关联修复落地确认**：materials discard 路径穿越净化（`_resolve_batch_path`，material_intake.py:39）、MCP 参数类型校验（`_validate_arguments`，server.py:373）、发版链 `.zcode-plugin` 路径（plugin-release.yml/plugin-version.yml 已改指新路径）、conftest resolve、guard 复合命令逐段判定（`hooks/guard_runtime_write.py:107-133`）。
- releases/v8.1.1.md 关键宣称抽查（12 工具、bookProjectRoot default、guard 逐段判定、context 删除零残留、workflow paths 扩充）全部与代码一致。
- **README 对账（R2 补审，此前漏项）**：外层 README 徽章/装机指引/上游警示/工具面描述与现状一致（装机指 `lgq-opc/webnovel-writer` 且明示上游勿用，README.md:111）；内层 README 存在 1 处 P2 失配 + 外层另 3 处 P3 失配，见 §3 P2-5 与 §4。

## 2. 真正未完成的队列项（文档自认、代码确认未做）

来源：`docs/plans/2026-08-30-v7-单线执行队列.md`（自称「唯一执行顺序清单」，无 superseded 声明）、`docs/plans/2026-08-30-v7-开发方案.md`、`docs/tasks/architecture-audit-fix-ledger.md`。

| 项 | 位置 | 内容 | 性质 |
|---|---|---|---|
| **S11 [~]** | 单线队列 :44 | D5 模型路由——操作卡已交付（`docs/guides/model-routing.md` 在盘），**待作者输入便宜模型标识**；未盲改原因：模型标识不被宿主接受会导致子代理派发失败，现状 `model: inherit` 可用 | 唯一真功能缺口，阻塞于用户输入 |
| S13a [ ] | 单线队列 :46-47 | C 追加优化——**回插条件已触发**（S13「C 后 ≤150 万」未达标，按规则回插 S13a），待排期执行；其①即 D5 路由与 S11 同源 | 已触发待排期 |
| D2 [ ] | 开发方案 :166 | 合并只读往返 | 规划优化项，未排期 |
| D3 [ ] | 开发方案 :167 | 大输出外置化 | 同上 |
| D4 [ ] | 开发方案 :168 | 子代理并行 | 同上 |
| D5 [ ] | 开发方案 :169 | 模型路由（与 S11 同源） | 同上 |
| E2 [ ] | 开发方案 :176 | index.db 全量重建 | 同上 |
| B8 [~] | 开发方案 :149 | 死 reference 清理与 loading map 复核（剩余部分） | 无后续对账记录（R2 抽验 loading map 40 项引用现状健康，见 §1.1，B8 剩余项或可销） |
| 台账 5 项 | `docs/tasks/architecture-audit-fix-ledger.md:132/139/146/153/164` | 三项 `[~]` + 两项 `[ ]`，尾注均「留 v7」；v7 已上线但无吸收记录；且 `:7` 状态指针仍指向已 `[superseded]` 的 `2026-08-25-status-and-pending-work.md` | 待确认吸收后回写或封存 |

## 3. 不匹配项：P2 级（功能与文档相反，或有行为影响）

1. **P2-1 `docs/plans/2026-08-31-S16-v6v7迁移器-plan.md:17-21`**：T1~T5 五个任务全 `[ ]`，但迁移器已交付且有测试锁定——`scripts/migrate_v6_to_v7.py` 在盘（`--help` exit 0，`--project-root/--output/--no-git/--link-back` 与文档一致）、`data_modules/tests/test_migrate_v6_to_v7.py` 11 用例全过（R2 实测），AGENTS.md:55 已将其列为正式迁移入口。
2. **P2-2 `docs/plans/2026-09-01-S19-垂直切片-plan.md:24-27`**：T1~T4 全 `[ ]`，但 `scripts/v7_write.py` 在盘、`v7-write` 子命令已注册（`data_modules/webnovel.py:1117`）且四子动作 live `--help` 通过。
3. **P2-3 `docs/plans/2026-09-13-reader-signals-v7-plan.md`**：35 个 `- [ ]` 零 `[x]`、零回写，但功能已落地且有测试锁定——`v7_cache.py:222` `chapter_reading_power` 建表、`:241` INSERT、`:332-371` 查询、`v7_write.py:455-513` settle 对缺失 hook_type 显式拦截、`:672` 后置钩子三件套；`scripts/tests/test_reader_signal.py` 10 用例中 `:118 test_v7_repo_reads_reading_power_from_cache`、`:129 test_v7_reminder_from_cached_hooks`、`:165 test_cli_index_get_reader_signals_reads_cache_for_v7` 直接锁定行为；`releases/v8.1.1.md:25` 亦已对外宣称该设计单落地。计划头部自述「完成即勾选」，实际零回写。
4. **P2-4 `docs/plans/2026-08-30-v7-开发方案.md:175` E1 `[ ]`**：已被现实推翻——E1（v6→v7 迁移器）已交付（同 P2-1 证据），属「文档说没做、代码已做」的反向失配。
5. **P2-5 内层 `webnovel-writer/README.md:52` 装机指引误指上游**：教用户添加 marketplace 用 GitHub 仓库 `lingfengQAQ/webnovel-writer`，与外层 `README.md:111`「上游 `lingfengQAQ` 仓库没有 ZCode 版本，勿用（应指 `lgq-opc/webnovel-writer`）」直接矛盾，会把 ZCode 用户装机导向错误仓库——本旬 5 处 README 失配中唯一有行为影响者。

## 4. 不匹配项：P3 级瑕疵

1. **P3-① 悬空引用**：`docs/zcode/项目复审/2026-09-20-项目复审报告.md:8`「两项均已列入当日修复批（**见 §十一**）」，但该报告小节止于「## 十、处置记录」，§十一 不存在；修复批实际落在提交 `ecfa624`/`2b644b9`/`8caf461`/`a5db636`。
2. **P3-② 死错误码**：`webnovel-writer/references/author_error_catalog.json:21` `match.codes` 仍含 `"prewrite_validator_blocking"`——发射方 prewrite_validator.py 已于 `3b7b5e0` 删除，全仓无代码再产生该码；该 JSON 是活文件（error_catalog.py:51 读取），属向后兼容死数据。
3. **P3-③ 过期计数**：AGENTS.md:58「11 脚本补垫片」为 09-15 历史批次计数；现状 scripts/ 顶层 24 个脚本 import runtime_compat，连本体共 25 个文件涉及——方向一致但数字不再对应现状。
4. **P3-④ README 版本自相矛盾**：外层 `README.md:23` 分支表 `v8-author | v8.1.0`，与同文件 `:4` 徽章 `version-8.1.1` 及全仓五方 8.1.1 不一致。
5. **P3-⑤ README 本机绝对路径泄漏**：外层 `README.md:367` 版本历史表「v8.1.1 (当前)」一行内容为 `D:/lgq/ai-workspace/projects/zcode-plugins/webnovel-writer/releases/v8.1.1.md`——本机绝对路径进入对外文档，应为仓内相对链接。
6. **P3-⑥ README RAG 节未标 v6-only**：外层 `README.md:143-163`「### 4. 配置 RAG」整节仍按现行功能口吻教学（.env/Embedding/Rerank），未标注仅 v6 仓适用；与 `:79`「RAG 已于 2026-09-18 对 v7 正式下线」同文冲突；`:338` 排查清单仍要求检查 RAG API Key（注：`:262` CLI 表已正确标注 rag 为 v6 legacy 冻结）。
7. **P3-⑦ README 评测数过期**：外层 `README.md:319`「`run_behavior_evals.py --suite fast` 的 23 个 case」——实测 fast.json 为 **21** 个 case、实跑 21 PASS 0 FAIL。
8. **P3-⑧ 「唯一清单」措辞**：`docs/plans/2026-08-30-v7-单线执行队列.md` 自称「唯一执行顺序清单」但 27 项已完成仅余 §2 所列，无「已基本收官」注记，现状读者难以判断清单是否仍在役。

## 5. 已知残留运维项

- **v8.1.0 Release 缺失（P3，运维项）**：远端 tags 有 v8.1.0（`21a0980`），但 Releases 页仅 v8.1.1/v8.0.0/v7.0.0 三份（本旬经公开 Releases 页 WebFetch 核验）；`.github/workflows/plugin-release.yml:17` 已支持 `workflow_dispatch`，补建需 Actions 手动 dispatch。本机无 gh CLI，Release 面状态以页面核验为准。
- **master 同步（信息项）**：`v8-author` 领先 `origin/master` 1 笔（`91ccc2d`，第 2 轮子代理审阅可选改进）；是否合回 master 由维护节奏定。

## 6. 处置建议

**需拍板项（建议两张 decision 卡）**：
- D-a1 **S11 便宜模型标识**：单独立卡等作者输入（输入后即可按 `docs/guides/model-routing.md` 操作卡落地 D5 路由）。
- D-a2 **队列剩余 7 项归属**（S13a/D2/D3/D4/D5/E2/B8 + 台账 5 项）：排期继续、归档封存、还是封版到 v8.1.x 后再启，三选一（S13a 已触发，若封存需显式改写回插规则）。
- （D-b 已并入 D-a2：文档滞后批次若确认回写收官，即按下条 E-a 执行，无需单独拍板。）

**可直接执行项（E-a 一处 docs 提交可清账；E-b 独立评估后单独提交）**：
- E-a **文档回写批**（全部限 docs/ 与 AGENTS.md，一处提交）：P2-1/2/3 三份 plan 补 `[x]` + 完成证据（commit 号/测试名）；P2-4 E1 过期行修正；P2-5 内层 README 装机仓改 `lgq-opc/webnovel-writer`；P3-① §十一 改指实际提交；P3-③ 垫片计数改口径；P3-④⑤⑥⑦ README 四处（分支表版本、绝对路径改相对链接、RAG 节标注 v6-only、23→21）；P3-⑧ 单线队列加收官注记；台账指针改指现行入口。
- E-b **死错误码清理**（独立一笔）：先在真实书仓（如 fantasy01_v8）grep `prewrite_validator_blocking` 确认无历史数据依赖 + 作者确认后，再删 `references/author_error_catalog.json:21` 该别名。

## 7. 审阅记录（多轮闭环留档）

- **R1（一致性/准确性，独立子代理）**：报 12 处需修 + 1 处存疑。全部采纳修正——计数四处（51→52 子命令、17→19 含二级、9→8 SKILL.md、95→94 模块）、行号一处（--repo 注入 ：397→:393）、定性一处（S13a「未到条件」→「已触发待排期」，依据单线队列 :46 回插规则）、口径改写两处（删除文件 4→5、CLI 样例「约 70 处」→83 行可复现口径）、指代一处（「P1 三项」明确为 docs/reports 报告 P1-1/2/3）、措辞两处（25 个 import 脚本 vs 本体、§3 引导句 3 份→4 项）、时点一处（工作树干净加 untracked 例外）。存疑项（v8.1.0 Release）已由主会话 WebFetch 第一手核验坐实。
- **R2（事实支撑/完备性，独立子代理）**：报 10 条。采纳——补 README 对账节（5 处失配，其中 P2-5 为行为级）、补台账 ledger 登记、S16/reader-signals 补功能性测试证据、「零 P0/P1」加「已扫描范围内」限定、§5 两项补级、E-b 拆出独立口径、D-a 拆两张卡、evals/skills 交叉引用核验结果升格进 §1.1。无驳回项。
- **R3（验证复审，独立子代理）**：验证通过——R1 12/12 核销 + 存疑项坐实（v8.1.0/v8.1.1 tag 哈希 `21a0980`/`0461637` 第一手核验一致）、R2 10/10 核销、22 条新增断言独立抽验零失实（含 fast 套件复跑 21 PASS、S16 迁移 11 用例复跑全过、1530 collected 复现）、内部一致性通过；0 blocker / 0 major，3 条 minor（§0 计数措辞、loading map 40 项口径注记、evals fixture 去数）已随手修正，准予定稿 v1.2。

## 附录 A：本旬硬证据命令

```
PYTHONUTF8=1 python -X utf8 -m pytest -q        # 1530 passed, 82.62%（门槛 80%），exit 0
PYTHONUTF8=1 python -X utf8 -m pytest --collect-only -q --no-cov   # 1530 collected
PYTHONUTF8=1 python -X utf8 webnovel-writer/scripts/validate_plugin_package.py   # OK, errors 0 warnings 0
PYTHONUTF8=1 python -X utf8 webnovel-writer/scripts/validate_release_notes.py    # OK, 8.1.1 / prev v8.1.0
PYTHONUTF8=1 python -X utf8 webnovel-writer/scripts/sync_plugin_version.py --check  # in sync: 8.1.1
PYTHONUTF8=1 python -X utf8 webnovel-writer/scripts/run_behavior_evals.py --suite fast  # 21 PASS / 0 FAIL
git ls-remote --tags origin "v8.1.*"            # v8.1.0 + v8.1.1（v8.1.1^{commit}=0461637 与本地一致）
# 公开 Releases 页（WebFetch）：v8.1.1 / v8.0.0 / v7.0.0，无 v8.1.0
python D:/lgq/a-hermes-space/scripts/board.py list --target ai-webnovel   # 待领/进行中/待拍板全空
grep -rn 'python -X utf8.*webnovel\.py' <skills|agents|commands>   # 83 行调用样例（对照口径）
PYTHONUTF8=1 python -X utf8 webnovel-writer/scripts/migrate_v6_to_v7.py --help      # exit 0
PYTHONUTF8=1 python -X utf8 -m pytest webnovel-writer/scripts/data_modules/tests/test_migrate_v6_to_v7.py -q --no-cov  # 11 passed
```

## 附录 B：证据口径与排除项

- 全部审计/审阅子代理均为只读模式，未创建/修改/删除任何仓库文件；写状态子命令（book-init/settle 等）一律未执行，仅 `--help` 与源码比对。
- 计数口径：52 顶层子命令 = `--help` choices 组与 `sub.add_parser(` 双口径一致；19 含二级 = 16 choices 型 + meter/run-ledger/knowledge 嵌套型；94 生产模块 = data_modules 顶层 86 + memory/ 8（含两级 `__init__.py`），tests/ 90 个文件不计；CLI 样例 83 行 = 25 个组件文档 grep 'python -X utf8.*webnovel\.py' 计数。
- 「不存在」类结论均以命令空输出作证（如 `ls webnovel-writer/scripts/data_modules/ | grep -iE "prewrite|author_glossary"` exit 1、内层 `.claude-plugin` ls 报 No such file、server.py grep rag_search 零命中）。
- 排除项说明：`__pycache__/` 本地编译产物（gitignore 覆盖、不入库）；dashboard「dist」按 AGENTS.md 口径指 `dashboard/frontend/dist/`（全仓唯一 dist 目录）。
- R2 复现记录：S16 迁移器 `--help` exit 0 + 11 用例通过；fast 套件 21 PASS；references loading map 40 项逐一验盘（37 在盘 + 3 退役登记）。口径注记：40 = 加载图中表格行的具体文件引用数（不含通配符/占位符与书仓侧相对路径，粗提取口径会显著多计）。
