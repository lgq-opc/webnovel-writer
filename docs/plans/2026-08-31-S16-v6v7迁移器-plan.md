# S16 · v6→v7 只读迁移器 实施计划

> **执行者注意：** 按任务逐个实施，TDD 红灯先行；步骤用 checkbox 跟踪。
> **Spec：** [story-repo-spec-2026-06-10.md](../architecture/story-repo-spec-2026-06-10.md)（0.4 冻结）。本计划是其 E1 迁移器的落地切片；loom-books/fantasy01 为 loom-1 原型，**不是**本迁移目标格式。
> **目标：** 一条命令把 v6 书项目只读迁移为 spec 0.4 的 v7 story-repo（book.yaml + 定稿/ + 大纲/ + 文风/ + .gitignore + git 初始提交），v6 源零写入。

## 全局约束

- 迁移器对 v6 源**只读**；输出写入 `--output` 新目录（已存在则拒绝，防覆盖）。
- 无损优先：v6 文件内容原样保留在 v7 对应位置（正文/设定/大纲/摘要），不重写内容，只加 front matter 与目录映射。
- 防呆方言（spec §2.2）：book.yaml 平铺、列表块格式、危险值加引号、UTF-8 无 BOM。
- git：`init` + `core.quotepath false` + 初始提交 `ch: v6 迁移导入`；`.gitignore` 含 `.cache/` 与 `工作区/`。
- 真书只读验证：fantasy01 迁移输出到临时目录，真书零改动。

## 任务清单

- [x] **T1 红灯**：`test_migrate_v6_to_v7.py`——最小 v6 夹具（正文 2 章/设定集 3 件/大纲 3 件/summaries 2 条/state.json）；断言：book.yaml 字段、正文 front matter（章号/标题/卷/字数）与正文无损、设定与大纲映射、摘要落位、.gitignore、源未动。✅ 已交付：`data_modules/tests/test_migrate_v6_to_v7.py` 11 用例全过（2026-09-21 复审 R2 实测复核）。
- [x] **T2 实现**：`scripts/migrate_v6_to_v7.py`——`migrate_project(project_root, output, use_git=True) -> MigrationReport`；卷号从 `大纲/第N卷-时间线.md` 行解析（`第36章`→卷 N），缺省 1；`名册.md` 从 `.webnovel/index.db` entities 生成（库缺失则跳过并记 warning）；`风格宪法.md` 仅当源有 `设定集/风格契约.md`。✅ 已交付；S17 顺带改进 index.db 缺失时名册保底收录主角。
- [x] **T3 CLI**：`--project-root --output --no-git`；输出 MigrationReport（章数/字数/skips/warnings）。✅ 已交付，`--help` exit 0（2026-09-21 复审核验），另增 `--link-back`。
- [x] **T4 集成验证**：fantasy01 → 临时目录真实迁移：36 章、book.yaml 书名/类型断言、git log 含初始提交、v6 源 mtime/内容未变。✅ 已实测：36 章/36 摘要/8 设定/3 大纲、git 初始提交、真书零改动。
- [x] **T5 收尾**：全量 pytest + evals、队列 S16 回写、CHANGELOG、提交推送、指针同步、自检。✅ 已完成：队列 S16 `[x]`（2026-08-31）；AGENTS.md 已列正式迁移入口；开发方案 E1 行 2026-09-22 同步核销。

## 映射表（v6 → v7）

| v6 | v7 | 备注 |
|---|---|---|
| 正文/第NNNN章-标题.md | 定稿/正文/NNNN-标题.md | +front matter（章号/标题/卷/字数），正文原文保留 |
| 设定集/世界观.md、力量体系.md、反派设计.md | 定稿/设定/同名.md | 原样复制 |
| 设定集/主角卡.md | 定稿/设定/角色/<主角正名>.md | 正名取 state.protagonist_state.name |
| 设定集/女主卡.md、配角卡.md | 定稿/设定/角色/同名.md | 卡内正名非结构化，保留原名并记 warning |
| .webnovel/summaries/chNNNN.md | 定稿/记忆/章摘要/NNNN.md | |
| .webnovel/index.db entities | 定稿/设定/名册.md | 正名/别名/首现章（库缺失跳过） |
| 大纲/总纲.md、第N卷-详细大纲.md、第N卷-节拍表.md、第N卷-时间线.md | 大纲/总纲.md、大纲/卷纲/第N卷.md、第N卷-节拍表.md、定稿/设定/时间线.md | 时间线合并为 append-only 表 |
| state.project_info.title/genre/genre_tags | book.yaml 书名/类型 | 类型取 canonical genre |
| （无对应） | 文风/、工作区/、.cache/(gitignore) | 目录占位 |

**范围外（记入迁移报告的「未迁移」清单）**：承诺/（需 LLM 从总纲抽取，属 S19 后置）、审查报告/（留 v6 或手工取舍）、增强设定/（卡片契约属 v6 侧，v7 用角色卡/世界观承接）。
