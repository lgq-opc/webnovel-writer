# webnovel-writer v7 开发方案：未完成事项收编 + Token 消耗优化

> 日期：2026-08-30
> 分支基线：本地分支 `fix/temp`（3977a68）已改名 **`v6.3.0`**；开发分支 **`v7-tmp`** 自 `v6.3.0` 切出（本方案即提交于 `v7-tmp`）。
> 书稿进度基准（2026-08-30 核实）：实际写作项目为 `C:\lgq\workspace\opc_space\projects\webnovel-projects\fantasy01`（v6 格式，独立 git 仓，master 与 origin/master 同步），已写至 **第 34 章**（`project-status`：latest_accepted_chapter=34 / target_chapter=35，phase=plan_in_progress）；ai-workspace 侧 `projects/loom-books/fantasy01` 为 v7 目标格式实验仓（book.yaml/定稿/大纲/文风）。本文所有章号以此进度为准。
> 权威待办源：[docs/plans/2026-08-25-status-and-pending-work.md](2026-08-25-status-and-pending-work.md)（本方案收编其全部未完成项，不与其冲突；执行完成后回写该清单状态）。
> 参考输入：
> - 架构审计两轮：`projects/webnovel-projects/docs/research/architecture-audit{,-round2}-2026-08-23/`（P0/P1/P2 分级建议）
> - 代码库分析四篇：`projects/webnovel-projects/docs/codebase-analysis/`
> - 记忆方案调研：`projects/webnovel-projects/docs/research/{ai-agent-context-databases,memory-plugins-full-comparison,claude-code-memory-integration}/`
> - 工具对比：`projects/webnovel-projects/docs/research/对比分析-webnovel-writer-vs-AI_NovelGenerator.md`
> - v7 规格（已冻结 0.4）：[docs/architecture/story-repo-spec-2026-06-10.md](../architecture/story-repo-spec-2026-06-10.md)

**目标：** 把「规划了但没做」的事项与「每章 ~750 万 token」的消耗优化，统一收编为 v7 分阶段开发方案；token 优化在 v6 架构上立即生效（当前写作直接受益），v7 Story Repo 主轴按已冻结规格推进。

**执行者注意：** 按阶段逐任务实施；每个任务动工前先写失败测试（TDD），完成即勾选 checkbox 并独立 commit。改动面较大的任务（尤其 Phase E 里程碑）动工时再按 writing-plans 拆独立 `-plan.md`。

---

## 1. 现状诊断：每章 750 万 token 是怎么烧掉的

实测基线（作者 2026-08 口径）：**每章约 750 万 tokens，成本 1-2 元**（宿主：Claude Code / ZCode 均同量级）。

诊断结论：**不是某个单一巨型文件，而是三个因子相乘**。单独看每个注入物都不致命，乘上「对话历史逐轮全量重发」与「缓存失效重读」后就变成 750 万。

### 1.1 因子一：主代理对话累积 × 无预算的 CLI 大输出

主代理跑完 `/webnovel-write` 全程约 35-60 次工具往返，会话历史只增不减，其中三次 CLI 把「大 JSON 全文」直接打进对话，此后每一轮都重复计费：

| 命令 | 注入内容 | 量级（估算） | 证据 |
|---|---|---|---|
| `write-gate --stage prewrite --format json` | `story_runtime` = 4 份合同全文 + latest_commit（最多 2 份）+ 校验详情，indent=2 | 3-15k | `write_gates/prewrite.py:130` |
| `review-pipeline` | 审查结果 + 指标全量 payload | 2-5k | `review_pipeline.py:215` |
| `chapter-commit` | 完整 commit payload，内嵌 4 份 artifact 全文 | 3-10k | `scripts/chapter_commit.py:39` |

主代理会话稳态体积 30-80k tokens；70 轮 × 平均 60-100k ≈ **4-7M tokens**，是最大乘数。

### 1.2 因子二：同一信息多份复制注入

| 重复项 | 份数 | 位置 | 单章浪费（估算） |
|---|---|---|---|
| 4 份合同全文 | ×2 | `memory_contract_adapter.py:163-164`（`story_contracts` + `runtime_status` 再嵌一遍） | 5-20k/次调用 |
| commit payload | ×3-4 | `runtime_status` 内、独立 `latest_commit`、（accepted 时再 +1） | 同上 |
| 审查结果 JSON | ×3 | reviewer 回传 → 主代理 Write 落盘（`SKILL.md:136` 一带，reviewer 不持 Write）→ review-pipeline stdout 再打印 | 3-9k |
| 章纲 + 近章摘要 | ×2 | extract-context 降级路径顶层与 `core.*` 重复（`extract_chapter_context.py:325-333 vs :352`）；load-context 的 memory_pack 内又一份 | 2-5k |
| 正文全文 | ×3 读 | 主代理 Write → reviewer Read → data-agent Read，polish 再多次 Edit | 8-12k |
| memory_pack 内部 | ×1.5-2 | `long_term_facts`=`semantic_memory` 原样复制、`active_constraints` 子集复制、`recent_changes` 与 episodic 重复（`memory/orchestrator.py:74-81`）；`state_export` 携带全量 `plot_threads` 与 `disambiguation_pending`（上限可至 1000 条，`config.py:208`）无截断 | 5-15k |

### 1.3 因子三：子代理串行 × 缓存 TTL 失效

每章固定 3 次子代理（context-agent / reviewer / data-agent），串行硬门槛（`SKILL.md:18-27`）+ 各次数分钟生成时间，使主代理在每次子代理返回后的首轮超过 5 分钟缓存 TTL，触发 50-100k+ 的全量 cache-miss 重读。每章约 4-6 个这样的 resume 点 ≈ **1-3M tokens**。此外三个子代理上下文彼此独立，同一批合同内容在 4 个上下文中分别读取、分别计费（子代理冷启动固定 ~10-20k）。

### 1.4 预算机制的四个「有形无实」缺口

1. `load_context(budget_tokens=4000)` 参数**无任何实现消费**（`memory_contract.py:120`、`memory_contract_adapter.py:159`，签名存在、函数体不读）；
2. `ContextManager.build_context(max_chars)` 形参**死参数**（`context_manager.py:102-118`，函数体零引用）；
3. `context_ranker.apply_budget` 只截 3 个小字段，**结构性大 section（合同/runtime_status/commit/genre_profile/memory_pack）明确豁免**（`context_ranker.py:58-93`）；genre_profile_excerpt 无字符上限（未命中题材时 fallback 取前 80 行）；
4. 章纲 1500 字按**字符硬切**（`chapter_outline_loader.py:149`），被切掉的恰是后段 CEN/禁区等关键约束；拆分章纲文件命中时**绕过上限全量返回**（`:152-154`）；`load_chapter_plot_structure` 用 `max_chars=None` 全量（`:380-382`）。

另：`data-agent` 每章必调 `index get-core-entities` **无上限全量加载**（`index_entity_mixin.py:244-256`，注释自述"全量加载"），实体数随章节线性增长，300 章后单次可达 20-50k。

### 1.5 缓存友好性

无显式 prompt-cache 设计（仓库 grep 零命中），也未发现头部时间戳/随机 ID 这类经典破坏模式；真正的敌人是 1.3 的流程节奏与 1.1 的历史膨胀。**改进原则：让稳定内容前置、大输出外置、resume 点变少。**

---

## 2. 未完成事项收编（谁进 v7、进哪个阶段）

以下全部来自权威待办清单中 `[ ]` / `[~]` 未闭合项，逐条映射到本方案阶段（§5）：

| # | 事项 | 状态 | v7 去向 |
|---|---|---|---|
| 1 | v6.3.0 发版：三处版本号统一 + release notes | `[~]` 差执行 | **Phase A** |
| 2 | 真实宿主会话级端到端验证（author-friendly Phase 5C） | `[~]` | **Phase A**（作为发版验证项） |
| 3 | v6→v7 只读迁移器（book.yaml/定稿/大纲/文风/初始提交） | `[ ]` | **Phase E**（E1） |
| 4 | `.cache/index.db` 全量重建 + 删缓存可查询 | `[ ]` | **Phase E**（E2） |
| 5 | fantasy01 垂直切片（顺延为下一章实跑；S19 实跑**第 37 章**） | `[x]` 2026-09-01 | **Phase E**（E3） |
| 6 | v6/v7 双格式唯一写入路径 | `[x]` 守卫已建 | **Phase E**（E4，已提前落地） |
| 7 | token 三漏洞（L0 摘要层 / 死配置截断 / JSON 冗余） | `[ ]` 两轮审计一致 | **Phase B+C**（本方案主体） |
| 8 | 题材 taxonomy 收敛到单一 index（6-Phase 方案已写好） | `[ ]` | **并行池**（P-1，可先行） |
| 9 | 设定增强通用化（Markdown 卡契约抽象，暂不引入 JSON Schema） | `[x]` | **并行池**（P-2）：契约已固化 [setting-card-contract](../guides/setting-card-contract.md)，S15 决策不引入 Schema |
| 10 | fantasy01 设定卡效果验证（30-36 章回看，2026-08-31 完成） | `[x]` | **并行池**（P-2）：验证报告 [2026-08-31-S15](../reports/2026-08-31-S15-设定卡验证与通用化决策.md)，三目标维度均有命中 |
| 11 | 上下文减负收尾（死 reference 清理、loading map 复核、行为契约断言） | `[ ]` | **Phase B**（B8 一并做） |
| 12 | 审计 P2 残留：latest.json 指针、大纲字段名统一、LLM 别名预注册、graph FTS 下推、题材数据补充、文本侧保护 hook | `[ ]` 留 v7 | **并行池**（P-3 小任务池） |
| 13 | 多宿主适配（单宿主 adapter + support.md + 生成器 + smoke test） | `[ ]` 显式门控 | **v7 后期**（E5 之后，本方案不展开） |

已明确放弃项（`[superseded]`：audit-fix-plan Task 8-34 大部、Graphiti/Letta 作事实主库、直接重写全部 v6 代码）本方案不再收录，遵循「v6 稳定收尾 + v7 绞杀式迁移」既定路线。

---

## 3. 外部研究给 v7 的四个可移植模式

1. **L0/L1/L2 分层加载**（OpenViking，自报输入 token ↓34%-91%）：设定/合同先给 ~200 字摘要层，命中实体再下钻原文——即两轮审计 P1-4 的「L0 摘要层」，两者互为佐证。
2. **抽取而非压缩的记忆**（Mem0/Supermemory/Zep）：存原子事实 + 矛盾消解 + 自动遗忘；对应把记忆四态中空转的 `contradicted/tentative` 做实（round2 审计 P1-3），陈旧事实及时降级出上下文本身就是省 token。
3. **大输出外置化**（OpenViking tool_output >20k 落盘 + 存根引用）：CLI 大 JSON 统一「落盘 + 一行摘要」，主对话只留结论。
4. **分阶段模型路由**（AI_NovelGenerator `choose_configs`）：提取/审查/摘要等结构化任务用便宜模型、正文用当前模型——宿主侧配置即可，不动插件代码。

---

## 4. v7 目标与验收口径

**token 目标（以实测为准，计量方法见 D5）：**

| 里程碑 | 单章总 token 目标 | 说明 |
|---|---|---|
| 基线 | ~750 万 | 当前实测 |
| Phase B 完成 | ≤ 300 万 | 输出瘦身 + 去重 + get-core-entities 子集化 |
| Phase C 完成 | ≤ 150 万 | 预算做实 + L0 摘要层 |
| Phase D 完成 | ≤ 100 万 | 往返压缩 + 缓存节奏（长期维持） |

**S13 实测回填（2026-08-31，fantasy01 第 36 章「放话」，v6.5.0 全链路首次真实计量）：**

| 指标 | 数值 | 对照 |
|---|---:|---|
| 本章总消耗（主会话 + 4 子代理，含缓存读） | **2,853,338** | 基线 750 万 **−62%**；「B 后 ≤300 万」**达标**；「C 后 ≤150 万」**未达标** |
| 其中主会话 | 1,296,104（11 轮；new 仅 130,088——大头是缓存读） | §1 因子一（主会话累积）仍是残余主体 |
| 其中子代理（4 会话） | 1,557,234（new 430,578） | S11 模型路由未应用（均 inherit），是成本下降最大杠杆 |
| 去缓存新增合计 | 560,666 | 真实增量成本口径 |

未达标处置：按队列规则回插 **S13a**（D5 模型路由实施 + 主会话缓存读成本换算说明）；另发现并修复 D1 推断缺陷——写作会话首 turn 完成前会话推断指错导致漏计主会话（本次低估 129.6 万），聚合语义改为「窗口内全部轮次 + 主会话清单透出 + 并行 WARN」，真值重放 MATCH。

**质量红线（每阶段收尾必须全过）：**
- 任务书质量不回退：同一书同一章，优化前后任务书的「事实/约束/伏笔覆盖」逐项一致、仅 token 下降（沿用 round1 审计 P1-4 验收口径）；
- `python -m pytest -q` 全绿 + `run_behavior_evals.py`（当前 22/22）+ `validate_plugin_package.py` 通过；
- 对真实书项目跑 `doctor` + `preflight` 双检查。

---

## 5. 分阶段任务

### Phase A：v6.3.0 发版收尾（半天，先关账）

- [x] **A1 三处版本号统一**：`.claude-plugin/marketplace.json`、`webnovel-writer/.claude-plugin/plugin.json`、README badge：6.2.1 → 6.3.0（用现成发版工具 `sync_plugin_version.py --version 6.3.0` 完成，README 更新简介表补 v6.3.0 当前行）。验证：`--check --expected-version 6.3.0` 全绿 + `validate_plugin_package` 0 错误 0 警告。commit：`5eb749e`（2026-08-30，v6.3.0 分支）。
- [x] **A2 release notes**：`releases/v6.3.0.md` 按 v6.2.x 既有格式编写，验证段回填实测结果（`validate_release_notes` OK）。commit：`5f6fe38`（2026-08-30）。
- [x] **A3 本地 tag** `v6.3.0` + 真实宿主会话冒烟：tag 已打（annotated）；冒烟于本会话（ZCode 宿主）用 **v6.3.0 检出代码**在临时 scratch 项目走完全链路——init → story-system persist → preflight → prewrite gate → 草稿落盘（守卫钩子放行正文）→ `load-context`（73.8KB 输出，坐实 §1 诊断）→ reviewer 子代理（合规 JSON）→ review-pipeline → data-agent 子代理（3 artifacts）→ precommit gate → **chapter-commit accepted，五投影全 done** → postcommit → backup（git commit + `ch0001` tag）→ user-report → doctor。两个 v6.3.0 新机制现场生效：`extraction_warnings` 抓到 2 个新实体缺 aliases（P0-4）；无 API Key 时 vector 投影仍 done（P0-2 正文入库 + 隐私守卫零出网）。scratch 已删、项目指针已恢复指向 fantasy01。**完整 5C（真实长篇章节 + 交互裁决回放）仍开放**；push 与远端发布**待作者确认**（AGENTS.md 红线）。

### Phase B：输出瘦身与去重（预期收益最大，先做）

原则：**主对话只留结论；原文落盘供程序读；同一信息只进上下文一次。**

- [x] **B1 write-gate 三连改一行结论**：`--format` 默认从 json 改为新增的 `compact`（一行结论：OK/ERROR + 错误码 + `detail=` 快照路径）；新增 `externalize_gate_report` 把全量报告（含 story_runtime）恒落盘 `.webnovel/tmp/last_gate_<stage>.json`；SKILL 三处调用点切换并加「禁止 --format json 进对话」红线。commit：`870a5ea`。
- [x] **B2 chapter-commit / review-pipeline 输出紧凑化**：新增 `summarize_commit_payload` / `summarize_review_payload`，stdout 只打一行结论（完整 payload 已由 persist_commit 落真源、review artifact 已落盘，无新文件）。commit：`7c18bf3`。
- [x] **B3 load-context 去重**：`runtime_status` 瘦身为状态字段 + commit 单份（latest 与 accepted 相同时 accepted 侧降级为章号标记），顶层 `latest_commit` 删除；合同全文只在 `story_contracts` 一份。旧形状钉子测试按新契约更新。commit：`db16376`。
- [x] **B4 memory_pack 去重与截断**：删除 `long_term_facts`（=semantic_memory 复制）与 `active_constraints`（子集复制）；episodic 不再重复包装 `recent_changes` 的 state_change 行；`state_export` 的 `plot_threads` 列表与 `disambiguation_pending` 限幅（新配置 `memory_state_export_pending_limit` 默认 10，0=不限）。commit：`60cb412`。
- [x] **B5 extract-context 降级路径去重**：`core.chapter_outline`/`core.recent_summaries` 与顶层 `outline`/`previous_summaries` 同源时去重；顶层 `latest_commit` 与 `runtime_status.latest_commit` 相同时置空。commit：`16e2fa2`。
- [x] **B6 审查结果单份化**：`agents/reviewer.md` 持受限 Write（唯一允许写入 `.webnovel/tmp/review_results.json`），最终回复只给一行汇总；webnovel-write / webnovel-review 两份 SKILL 所有权说明同步；行为 eval 探针按新契约反转（artifact_ownership）。commit：`c13f219`。
- [x] **B7 load-context 输出紧凑化**：`memory_cli._json_out` 改紧凑 JSON（键名不动，下游解析不受影响），round-trip 钉子测试锁定。commit：`5293def`。
- [x] **B8 data-agent 实体子集化 + 上下文减负收尾**：`get-core-entities` 增加 `--limit`（SQL LIMIT 下推），data-agent 契约改「定点 get-by-alias 优先 + `--limit 40` 兜底，禁止无 limit 全量」。commit：`aa61fd7`。**收尾核销（2026-09-22）**：死 reference 清理已由单线队列 S5 完成（删 4 个空壳，desire-description 与 genre-hook-payoff-library 经 CSV 复核保守保留）；loading map 复核经 2026-09-21 复审 R2 抽验 40 项文件引用全部在盘健康——B8 无剩余项，销项。

**Phase B 收尾**：全量 pytest（941 用例 EXIT=0）+ 22/22 behavior evals 通过（见 §8 证据）。**真书副本实测（2026-08-30，fantasy01 第 35 章「不去」，v7-tmp 链路全程）**：三个子代理合计 1,347,676 tokens（context-agent 618,707 / reviewer 261,500 / data-agent 467,469）；同项目 A/B 对比 `load-context` 输出 **6.2.1 旧版 309,801 字节 → v7-tmp 86,931 字节（−72%）**，B3/B4/B7 的瘦身在 35 章真实数据规模上兑现；B1/B2 一行输出在真实流程中生效（prewrite 门禁与 chapter-commit 均单行）。流程产出：章纲照写正文 2,498 字，reviewer 抓出 6 问题（1 阻断：新安城「零星接济」与第 34 章首次接触矛盾）全部定点修复，chapter-commit accepted、五投影 done、备份 tag `ch0035`。主代理（编排侧）消耗以下一轮 hook 统计为准。**注意**：当前宿主安装的插件缓存仍为 6.2.1，B6 的「reviewer 直写」机制需待新版发布安装后才能端到端生效（本次实测 reviewer 仍走 JSON 回传 + 主流程落盘的 6.2.1 契约）。

### Phase C：预算做实 + L0 摘要层（结构性省 token）

- [x] **C1 实装 budget_tokens**：新增 `context_budget.py`——section 配额（story_contracts 8,300 / runtime_status 700 / memory_pack 8,500 等，键级优先级丢弃 source_trace 等）+ `DROP_ORDER` 总预算丢弃（硬约束类永不整体丢弃）+ `shrink_latest_commit`（提交全文仅留 meta+摘要）。默认总预算经 [S1 配额分析](../reports/2026-08-30-S1-预算配额分析.md)（fantasy01 实测）定为 **20,000 字符**（原签名默认 4000 仅为现体积 7%，已弃用为显式覆盖入口）；`budget_used_tokens` 返回真值。fantasy01 实测：ch35 基础包 59,977 → **19,241 字符（−68%）**。commit：`feat: enforce load-context token budget`（2026-08-30）。
- [x] **C2 复活死参数**：`build_context` 的 `max_chars` 落地为总预算（超限按 `PAYLOAD_DROP_ORDER` 低价值先弃：long_term_memory/scene/alerts → reader_signal/writing_guidance → …，meta/core/story_contract/runtime_status/latest_commit 受保护，`meta.budget_dropped` 留痕）；`apply_budget` 新增 `SECTION_TEXT_BUDGETS` 递归文本上限（genre_profile 1500 / reader_signal 800 / writing_guidance 1200 / plot_structure 2500，只截字符串不破结构）；runtime_status 瘦身（B3/S1 同款 + `shrink_latest_commit`，**先比较后收缩**避免 accepted 全文回灌）。fantasy01 ch36 extract-context 实测：39,914 → **17,304 字符**（runtime_status 23,611 → 1,001）。commit：`feat: slim context-manager runtime status and enforce payload budget`（2026-08-30）。
- [x] **C3 L0 摘要层**：新增 `settings_digest.py`——确定性 ~240 字结构摘要（主题行 + ## 骨架 + 首要点）+ sha256 陈旧自愈（源文件变更就地重建，**比原设计更进一步：不依赖 init/plan 重跑**），落 `.webnovel/settings_digest/`（派生物可重建，避免污染设定集内容目录）；`_load_setting` 默认注入摘要（`context_settings_digest_enabled` 可回滚，旧 4000 截头保留为回退路径）；CLI 新增 `setting-read`（L2 原文按需展开）；context-agent 增加「命中才展开」指引。fantasy01 实测：6 设定文件 9,008 → **601 字符（−93%）**（该书文件均 <4000 截断线，节省随书与增强卡增长复利）。commit：`feat: L0 settings digest layer`（2026-08-30）。
- [x] **C4 章纲按字段边界截断**：主体 v6.3.0 落地；**残留两处已修（S4，2026-08-30）**——拆分章纲命中路径不再绕过 max_chars（超限走字段边界优先截断）；`load_chapter_plot_structure` 默认限量 4000（`PLOT_STRUCTURE_DEFAULT_MAX_CHARS`，新增 `max_chars` 参数，CBN/CPNs/CEN/禁区等标签行截断时优先保留、字段解析不受影响）。
- [x] **C5 记忆四态做实**：**勘误（2026-08-30）**——v6.3.0 已落地（contradicted/tentative 生产写入路径，CHANGELOG v6.3.0 P1-3；`test_memory_store/test_memory_writer` 覆盖），本项从实施任务降级为验证后关闭（单线队列 S5）。

**Phase C 收尾**：同书同章任务书覆盖一致性比对（优化前后 diff 留档）+ 计量更新。

### Phase D：缓存节奏与往返压缩

- [x] **D1 章级 token 计量**：数据源定为 ZCode 本地用量库 `~/.zcode/cli/db/db.sqlite` 的 `turn_usage` 表（只读，子代理消耗也在表内）。方案 A+B 均已落地：A = `meter start/stop/report` 子命令（标记文件 `.webnovel/tmp/chapter_meter.json`，`meter stop` 输出一行「本章总消耗」+ 结果文件；会话推断 = 最近完成的非子代理轮，锚点为其完成时刻）；B = `hooks/chapter_meter_hook.py`（UserPromptSubmit，标记 open 时每轮注入「本章累计」）。口径：时间窗内主会话 + 全部 `sess_subagent%` 轮次（子代理与主会话无父子关联，必须按时间窗一并计入），缓存读单列、新增 = input−cache_read+output。真库实测：聚合与直接 SQL 完全一致（第 35 章实测轮窗口 4 轮 1,753,676 tokens，去缓存新增 288,012）；非 ZCode 宿主优雅降级。**注意**：写章期间并行跑其他重会话会串入其消耗。
- [x] **D2 合并主代理只读往返**：run-ledger 记账批量化（一条命令记多步）；preflight/where/placeholder-scan 合并为一次 `preflight --all`。commit：`feat: batch run-ledger recording`。**已由单线队列 S9（2026-08-30）交付**：`preflight --all` 三查合一 + `run-ledger record-write-steps --steps-json` 批量冲账，fantasy01 实测一次往返三查全过。
- [x] **D3 大输出外置化通用机制**：CLI 输出 >20k 字符自动落盘 + 返回摘要存根与引用路径（对标 OpenViking tool_output externalization）。commit：`feat: externalize oversized cli output`。**已由单线队列 S10（2026-08-30）交付**：`output_guard.py` 超 20k 字符（`WEBNOVEL_OUTPUT_EXTERNALIZE_CHARS` 可调）落盘 `.webnovel/tmp/cli_out/` + 存根返回，集成测试经子进程实测。
- [跳过] **D4 子代理并行（可选，权衡后定）**：reviewer 与 data-agent 均只依赖正文+索引，可并行以缩短缓存失效窗口；代价是 review 出 blocking 时 data-agent 白跑。默认维持串行，仅在实测串行 resume 重读 > 并行浪费时启用。验证：计量对比两种编排各 2 章。**决策封存（2026-09-22）**：维持默认串行，原权衡结论未出现反转证据，不启用；未来启用需先补计量对比验证。
- [跳过] **D5 模型路由（宿主侧，不动插件代码）**：在 ZCode/Claude Code 的子代理模型配置中，把 reviewer/data-agent/context-agent 指到便宜模型，正文保持当前模型（借鉴 AI_NovelGenerator `choose_configs`）。验证：审查/提取质量抽检 + 成本对比。**放弃（2026-09-22 作者拍板）**：不输入便宜模型标识，维持 `model: inherit`（现状可用，避免标识不被宿主接受导致派发失败）；操作卡 [model-routing.md](../guides/model-routing.md) 留档备用。

### Phase E：v7 Story Repo 主轴（按已冻结 spec 0.4，里程碑级）

> 规格与验收标准见 [docs/architecture/story-repo-spec-2026-06-10.md](../architecture/story-repo-spec-2026-06-10.md)；每个里程碑动工时按 writing-plans 拆独立 `-plan.md`。

- [x] **E1 v6→v7 只读迁移器**：生成 `book.yaml`、`定稿/`、`大纲/`、`文风/`、初始迁移提交；v6 数据原样保留可回退。**已由单线队列 S16（2026-08-31）交付**：`scripts/migrate_v6_to_v7.py`（`--project-root/--output/--no-git/--link-back`）+ `data_modules/tests/test_migrate_v6_to_v7.py` 11 用例锁定，fantasy01 实测 36 章真书零改动；AGENTS.md 已列正式迁移入口。
- [x] **E2 `.cache/index.db` 全量重建**：删除缓存后查询可用的 CI 验收项（v7 不变量「派生物可丢弃」）。**已由单线队列 S17（2026-08-31）交付**：`scripts/v7_cache.py` rebuild/verify/snapshot，`verify_rebuild` 删缓存→重建→查询快照等价，fantasy01 迁移仓实测 36 章 verify equal=True。
- [x] **E3 fantasy01 垂直切片**（2026-08-30 进度修正顺延为下一章实跑；S19 于 2026-09-01 实跑**第 37 章**《暗纸》）：决策卡 → 上下文包 → 草稿 → 机检 → 作者验收 → settle → Git commit，全链闭环（原子 commit `30744af`，真书零写入）。上下文包原生吸收 Phase B/C 成果（20,000 字符预算、紧凑输出、v7_cache 查询）。复盘与计量对比见 [S20 报告](../reports/2026-09-02-S20-v7垂直切片复盘.md)。
- [x] **E4 v6/v7 双格式唯一写入路径**（提前落地，2026-08-30）：新增 `dual_format_guard.py`——落定判定（v6=accepted commit；v7=`定稿/正文/NNNN-*.md` per spec 0.4 §4.1）+ `check_unique_write_path` 阻断（另一格式已落定 → blocker `dual_format_write_blocked`）；接入 prewrite gate（经项目 `.env`/环境变量 `STORY_REPO_ROOT` 配置 v7 仓库根，缺省空 = 零行为变化）。**范围说明**：v7 侧的对称调用由 S19 settle 流程接入（当前为规格兼容的前置脚手架，合成夹具测试）；S16 迁移器落地后回填真实映射。commit：`feat: dual-format unique write path guard`。
- [x] **E5 垂直切片稳定后**（2026-09-02，S20 收尾）：设定卡效果验证由 S15 完成（30-36 章回看口径，[验证报告](../reports/2026-08-31-S15-设定卡验证与通用化决策.md)）；切片复盘（含计量对比 v6 链路）见 [S20 报告](../reports/2026-09-02-S20-v7垂直切片复盘.md)；多宿主单宿主 adapter 立项决策完成（[ADR](../decisions/2026-09-02-多宿主适配立项决策.md)：立项，排 v7.0.0 后独立开队列，首期 ZCode）——实施另立队列，不在本方案展开。

### 并行池（不阻塞主线，按人力穿插）

- **P-1 题材 taxonomy 收敛**：完整 6-Phase 方案已在 [docs/operations/genre-taxonomy-convergence-plan-2026-06-04.md](../operations/genre-taxonomy-convergence-plan-2026-06-04.md)，可独立先行（待办 #8）。
- **P-2 设定增强验证与通用化**：依赖 E3/E5 的 fantasy01 数据；验证窗口改用已写的 30-34 章回看比对 + 第 35 章前瞻（原 23-25 章区间已在 `设定集/增强设定/` 卡片在位状态下写完，但从未做过对照核账）；先抽象 Markdown 卡契约，再决定是否引入 Pydantic 子模型（待办 #9/#10）。
- **P-3 审计 P2 残留小任务池**（待办 #12）：`latest.json` 指针、大纲字段名统一（mandatory_nodes vs must_cover_nodes）、新实体 LLM 别名预注册、graph 候选 FTS 下推、题材参考数据补充（tropes 5→更多 / profiles 13→更多）、正文目录 PostToolUse 留痕 hook。每项独立 commit，随手领取。

---

## 6. 执行顺序建议

> **2026-08-30 起：改为单线顺序执行（Human 指示，不做并行）。当前唯一有效顺序见《[2026-08-30-v7-单线执行队列.md](2026-08-30-v7-单线执行队列.md)》S0-S21；下图仅保留最初的多线脉络，仅供参考。**

```
Phase A（半天） ──→ Phase B（1-2 天，收益最大） ──→ Phase C（2-3 天）
                         │                              │
                         └── D1 计量提前到 B 之前做 ──┘
                                        ↓
                              Phase D（1-2 天） → Phase E 主轴（按 spec 迭代）
并行池 P-1/P-3 随时可穿插；P-2 等 E3。
```

理由：作者当前在持续写书，**B/C 的每一项节省都会立即作用于每一章**，优先于 v7 迁移主轴；E 主轴规格已冻结、与 B/C 无代码冲突（v6 绞杀式迁移），可由另一条分支并行推进。

## 7. 风险与权衡

1. **省 token ≠ 砍信息**：所有截断/摘要必须过 §4 覆盖一致性红线；L0 摘要层失败模式是「摘要丢了关键约束」→ 用「章纲命中实体才展开」兜底。
2. **reviewer 持 Write 的权限放宽**（B6）：仅白名单单文件，且 precommit gate 仍校验 artifact；evals 覆盖「reviewer 越权写」用例。
3. **B/C 改的是 v6 代码**，v7 迁移后部分补丁作废——可接受的重复劳动，换来当前写作立即降本；v7 上下文包设计（E3）必须吸收同等机制，避免二次实现走样。
4. **token 目标数值是估算**（基于 §1 诊断模型），以 D1 计量实测为准，里程碑目标允许 ±30% 修正。
5. 发版 push / 远端 tag 属远程操作，按工作区红线须作者确认（A3 仅做本地部分）。
6. **进度真源唯一**：写作项目实际位于 `C:\lgq\workspace\opc_space\projects\webnovel-projects\fantasy01`（独立 git 仓，随写作持续推进），ai-workspace 侧的 `loom-books/fantasy01` 是 v7 格式实验仓——两者角色不同，禁止互相覆盖或把对方当备份；本文所有章号以 opc_space 项目的 `project-status`（latest_accepted_chapter）为准，E1 迁移器运行前需先核对该章号是否又已前进。

## 8. 证据与引用索引

- 浪费点诊断：本文 §1 全部条目均经代码抽查（2026-08-30，`v7-tmp` @ 3977a68）——关键验证点：`write_gates/prewrite.py:130`、`scripts/chapter_commit.py:39`、`scripts/memory_cli.py:35-37`、`memory/orchestrator.py:74-81`、`memory_contract_adapter.py:159`、`context_manager.py:106,746-749`。
- 书稿进度核实（2026-08-30）：opc_space 写作项目正文 34 章（第0001章-天裂 → 第0034章-新安城），`project-status` 报 latest_accepted_chapter=34 / target_chapter=35；`设定集/增强设定/` 实验卡目录在位；本会话中 PreToolUse 守卫钩子（`guard_runtime_write.py`）成功拦截了对 `.webnovel/index.db` 的直接访问，守卫层在 ZCode 宿主上实测有效。
- Phase A 发版收尾（2026-08-30，v6.3.0 分支）：版本号 5eb749e、release notes 5f6fe38、AGENTS.md 同步 d9a2154（已 cherry-pick 至 v7-tmp 为 c498be2）、本地 tag `v6.3.0`；发版校验四件套 + `pytest -q --no-cov`（924 用例）全绿；宿主级冒烟通过（详见 A3 勾选项）。另：空模板 scratch 项目上 `load-context` 实测输出 73.8KB——§1「无预算重复注入」诊断的活体数据点，Phase B 的 B3/B4/B7 可据此设基线。
- Phase B 输出瘦身与去重（2026-08-30，v7-tmp 分支）：B1 `870a5ea`、B2 `7c18bf3`、B7 `5293def`、B3 `db16376`、B4 `60cb412`、B5 `16e2fa2`、B6 `c13f219`、B8 `aa61fd7`（B8 剩死 reference 清理留 Phase C）。验证：全量 `pytest -q --no-cov` 全绿 + 22/22 behavior evals（artifact_ownership 探针已按 reviewer 直写新契约反转）。v6.3.0 分支已推送 `origin/v6.3.0`（未推 tag）。
- Phase B 真书副本实测（2026-08-30）：fantasy01 复制到临时目录跑通第 35 章全链路（commit accepted / tag `ch0035`），真书未动；`load-context` A/B 实测 −72%（309,801→86,931 字节），子代理合计 134.8 万 tokens。实测发现的真实缺陷已于当日修复：①中文别名 CLI 查询乱码——根因是 v6.3.0 的 `_fix_argv_mojibake` 启发式误报（正常中文的 GBK 编码恰为合法 UTF-8，`钱平`→`Ǯƽ`），argv 自动修复改为 `WEBNOVEL_FIX_ARGV_MOJIBAKE=1` 显式开启，端到端复验通过；②review-pipeline/chapter-commit 对损坏 JSON 抛 traceback，改为友好报错 + 退出码 2。
- 未完成事项全景：`docs/plans/2026-08-25-status-and-pending-work.md`；审计台账 `docs/tasks/architecture-audit-fix-ledger.md`。
- 外部佐证：OpenViking/Supermemory/MemOS 对比与 Claude Code 集成方案见 `projects/webnovel-projects/docs/research/`（三目录）；工具对比见 `对比分析-webnovel-writer-vs-AI_NovelGenerator.md`。
