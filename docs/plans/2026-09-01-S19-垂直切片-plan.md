# S19 · v7 垂直切片实施计划（第 37 章）

> Spec：[story-repo-spec 0.4](../architecture/story-repo-spec-2026-06-10.md)；队列：[单线执行队列 S19](2026-08-30-v7-单线执行队列.md)
> 切片对象：**迁移仓 `projects/loom-books/fantasy01`**（S16 产物）第 **37** 章（真书 36 章已接受，37 未写）。
> 真书（v6）零写入；切片产出全部落在迁移仓（git 已初始化，可回退）。

## 目标

v7 写路径最小可运行闭环：**决策卡 → 上下文包 → 草稿 → 机检 → 作者验收（Human 门）→ settle → Git commit**。上下文包必须吸收 S1-S4 成果（20,000 字符总预算、紧凑输出、去重、时间窗新鲜度）。

## 结构约定（跟随迁移器实际产物，spec 命名漂移记为已知问题）

| 产物 | 路径（迁移器实际） | 说明 |
|---|---|---|
| 章节正文 | `定稿/正文/chNNNN.md`（front matter 英文键：spec_stage/chapter/title/time_anchor/word_count/contract_digest/entry_changes） | 与现有 36 章一致；spec 的 `NNNN-标题.md`+中文键漂移记入 §6 |
| 章摘要 | `定稿/记忆/章摘要/NNNN.md`（≤200 字） | **v7_cache 唯一认的路径**；迁移器散写的 `记忆/chNNNN.md` 为待清理漂移 |
| 名册 | `定稿/设定/名册/<正名>.md`（每实体一文件：正名/别名/类型/首现章） | 目录为空，新实体由 settle 按验收卡创建 |
| 决策卡 | `工作区/决策卡-NNNN.md` | 作者界面单位；含承诺豁免勾选位 |
| 上下文包 | `工作区/上下文包-NNNN.md` | 总预算 20,000 字符（context_budget 执行） |
| 草稿 | `工作区/草稿-NNNN.md` | 工作区默认不入 git |

## 任务

- [x] **T1 v7_write.py**：`decision`（从参数生成决策卡）/ `pack`（v7_cache 查询 + 决策卡 + 名册 → 预算内上下文包）/ `check`（机检：字数、占位符、标题一致、承诺或豁免、名册外「」新名 advisory）/ `settle`（前置=机检过+唯一写入路径过；写正文/章摘要/名册新实体 → 原子 git commit）。✅ 已交付：`scripts/v7_write.py` 四子动作，`v7-write` 子命令已注册（`data_modules/webnovel.py:1117`），live `--help` 通过（2026-09-21 复审核验）。
- [x] **T2 测试**：临时 v7 仓夹具覆盖 pack 预算、机检五项、settle 成功/双格式阻断/缺草稿拒绝原子性。✅ 已交付：TDD 10 用例含原子性回滚（队列 S19）。
- [x] **T3 实跑第 37 章**：决策卡（基于 36 章钩子：熊铁山「血洗仓库」+ 内患收拢 + 暗哨情报）→ 上下文包 → LLM 草稿 ~2400 字 → 机检 → 呈现验收卡。✅ 已实跑（2026-09-01）：首稿 1,670 验收打回 → 四因诊断修复（见诊断附录）→ 二次稿 2,599 机检过 → 呈现验收。
- [x] **T4 settle**：Human 验收通过后执行（本计划范围内只到 T3 呈现；settle 是下一动作）。✅ 已执行：作者验收通过 → settle 原子 commit `30744af`（正文/章摘要/名册新增 赵姓汉子+熊铁山）→ 缓存重建 37 章可查询；复盘见 [S20 报告](../reports/2026-09-02-S20-v7垂直切片复盘.md)。

## 机检清单（机检先于模型评审，spec §1.5）

1. 非空且字数 ∈ [800, 6000]；2. 无占位符（`[待…]`/`TODO` 等）；3. 正文标题与决策卡一致；4. 承诺结转：决策卡承诺列表非空或显式豁免（理由必填——切片章以「迁移仓无承诺档案」豁免）；5. 名册 advisory：正文「」内两到四字候选名 ∉ 名册 → 列入验收卡请作者确认。

## 已知漂移（不在本切片修）

1. 正文文件名 `chNNNN.md` vs spec `NNNN-标题.md`；2. front matter 英文键 vs spec 中文键；3. 迁移器把章摘要散写 `定稿/记忆/chNNNN.md` 而 cache 只认 `章摘要/NNNN.md`（S17 快照自洽但查询面不可见）。统一决策留给 spec 对齐任务（S20）。


## 诊断附录：首稿字数不足的四因与修复（2026-08-30，Human 验收打回后）

首稿 1,670 字（书史均值 2,680 的 62%）。核账发现四因，均已修复或入工具：

1. **字数契约缺失**：决策卡/上下文包无任何字数目标（v6 任务书明确「约 2800—3000 字」）。修复：`v7_write` 新增 `book_word_stats`（定稿正文直算），决策卡带「目标字数 = 书史均值 + 下限 75%」，上下文包新增 `length_contract` 节。
2. **上下文包薄**：首跑仅 1,102 字符（预算 5.5%）——迁移仓停在 34 章（真书已 36），35/36 章内容不在仓内；且迁移器把章摘要散写 `定稿/记忆/chNNNN.md` 而 cache 只认 `章摘要/NNNN.md`。修复：迁移仓重刷至 36 章（工作区产物保留；迁移器拒绝已存在目录，走「临时目录迁移→并入保留 .git」路径）；仓刷新后 words/summaries 全部就位（此前 words 全空是旧仓残留数据，非重建 bug）。
3. **决策卡粒度粗**：3 个裸节点 vs v6 章纲 12+ 结构化字段（阻力/代价/爽点/钩子/场景节拍）→ 扩写把节点展开为场景级节拍（内患收拢/防谍实操/物资清点/轻糖）。
4. **机检下限脱钩**：拍脑袋 800 vs 书史中位 2,336。修复：`run_checks` 下限 = 目标字数 × 0.75（可显式覆盖）。

**护栏正反馈**：校准后的机检把二次稿（1,897 字）正确拦下，扩写至 2,599 字后放行——「机检先于模型评审」按设计工作。

**顺手核销的漂移**：旧实验残留 `chNNNN.md`/英文键 front matter 已被 S16 正式迁移产物取代（spec 命名 `NNNN-标题.md` + 中文键），v7_write/夹具随之对齐。
