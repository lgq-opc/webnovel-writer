# v7 单线执行队列（2026-08-30）

> 性质：**唯一执行顺序清单**（Human 指示：不做并行，单线顺序，所有剩余任务合理排序）。
> **收官注记（2026-09-22）**：S1-S21 主轴全部完成，v7 写链已上线运行；S11（D5 模型路由）经作者拍板放弃（维持 `model: inherit`），S13a 回插项随之封存。本清单不再新增任务、不再作为执行顺序依据，现行待办以编排台看板（`python D:\lgq\a-hermes-space\scripts\board.py list --target ai-webnovel`）为准。
> 本队列取代《[2026-08-30-v7-开发方案.md](2026-08-30-v7-开发方案.md)》§6 的多主线/并行池结构；任务规格以该方案为来源，顺序与取舍以本队列为准。冲突时以本队列为准。
> 状态标记：`[ ]` 未开始 / `[~]` 进行中 / `[x]` 完成 / `[跳过]` 条件触发未触发。

## 0. 排序原则

1. **每写一章都在花钱** → 直接降本、影响日常写作的任务排最前；
2. **依赖优先**：验收基础设施（D1 计量）已就位 → 先拿基线，再深化优化（C），最后架构迁移（E）；
3. **风险递增**：小清理 → 上下文深化 → 发版 → 大重构；
4. **发版节奏**：每积累一批可验证改动就发一版装上（6.4.0 已装 → 6.5.0 → 7.0.0），让日常写作持续吃到优化；
5. 条件触发项（D4）排在队尾，默认跳过；
6. 写章是 Human 动作：计量由 D1 自动采集，不阻塞代码任务（见 S12 说明）。

## 1. 当前快照（2026-08-30）

- 已完成：Phase A（v6.3.0 发版+推送）；Phase B（B1-B7 全部 + B8 实体子集化）；D1（章级计量 A+B，真库验证通过）；真书副本实测（35 章，load-context −72%）；两项实测修复（argv 门控、损坏 JSON 友好报错）；v6.4.0 已发版并安装（缓存含 D1）。
- 真书：fantasy01 已写至 **35 章**（Human 已自行落地第 35 章），下一章 36。
- 基线：优化前 ~750 万 tokens/章；D1 已可自动采集章级消耗（总计 + 去缓存新增）。
- 待推送：`v7-tmp` 领先 `origin/v7-tmp` 2 个提交（D1 功能 + 文档）。

## 2. 勘误（建队列时核对存量发现）

1. **C5 记忆四态 → 已由 v6.3.0 落地**（contradicted/tentative 生产写入路径 + `test_memory_store/test_memory_writer` 覆盖），从 Phase C 实施项降级为「验证后关闭」（S5 内完成）。
2. **C4 字段边界截断 → 主体已由 v6.3.0 落地**（`_truncate_outline_by_field_priority`），残留仅两处：拆分章纲文件绕过 max_chars（`chapter_outline_loader.py:152-154`）与 `load_chapter_plot_structure` 无限量（`:381`）。
3. **E3 切片章号顺延**：第 35 章已用 v6 链路写完并落地真书，切片改为「执行时的下一章」（约第 38 章）。

## 3. 单线队列（严格按序）

| # | 状态 | 任务 | 内容与验收 | 预估 |
|---|---|------|-----------|------|
| S0 | `[x]` | 仓库卫生 | `v7-tmp` 已与 origin/v7-tmp 同步（a52850f）；临时副本 `Temp\fantasy01-ch35-test` 已删除（删除前核验：真书 `latest_accepted_chapter=35` 且 `正文/第0035章-不去.md` 在位，副本无独有内容；`.env` 密钥副本一并清除）。完成于 2026-08-30 | 10min |
| S1 | `[x]` | C1 预算实装 | 新增 `context_budget.py`（section 配额 + 子配额 + 键优先级 + `DROP_ORDER` 总预算丢弃 + `shrink_latest_commit`）；`load_context(budget_tokens=None)` 默认读配置 `context_load_total_budget=20000`（分析推导值），`budget_used_tokens` 返回真值。配额推导见 [S1 预算配额分析](../reports/2026-08-30-S1-预算配额分析.md)。fantasy01 实测：ch35 基础包 59,977 → **19,241 字符（−68%）**。完成于 2026-08-30 | 0.5d |
| S2 | `[x]` | C2 ContextManager 瘦身 | runtime_status 瘦身（B3/S1 同款 + `shrink_latest_commit`，先比较后收缩；两处旧钉子随新契约更新）；`apply_budget` 新增 `SECTION_TEXT_BUDGETS` 递归文本上限（genre_profile 1500 / reader_signal 800 / writing_guidance 1200 / plot_structure 2500）；`build_context(max_chars)` 总预算落地（低价值先弃序 + `meta.budget_dropped` 留痕）。fantasy01 ch36 extract-context 实测：39,914 → **17,304 字符**（runtime_status 23,611 → 1,001）。完成于 2026-08-30 | 1d |
| S3 | `[x]` | C3 L0 摘要层 | 新增 `settings_digest.py`（确定性 ~240 字结构摘要 + sha256 陈旧自愈——比原设计更进一步，不依赖 init/plan 重跑；落 `.webnovel/settings_digest/` 派生物可重建）；`_load_setting` 默认注入摘要（`context_settings_digest_enabled` 可回滚，旧 4000 截头保留为回退路径）；CLI `setting-read`（L2 按需展开，已入注册表）；context-agent「命中才展开」指引。fantasy01 实测：6 设定文件 9,008 → 601 字符（−93%）。完成于 2026-08-30 | 1-1.5d |
| S4 | `[x]` | C4 残留 | 拆分章纲命中路径纳入 max_chars 预算（超限走字段边界优先截断，不再绕过）；`load_chapter_plot_structure` 默认限量 `PLOT_STRUCTURE_DEFAULT_MAX_CHARS=4000`（新增 `max_chars` 参数，字段解析不受影响）。fantasy01 复验：无拆分章纲文件、extract-context 总量 17,304 不变（无行为回退）。完成于 2026-08-30 | 0.5d |
| S5 | `[x]` | B8 收尾 + C5 验证关闭 | 死 reference 清理：删除 4 个已迁移空壳（combat-scenes/dialogue-writing/emotion-psychology/scene-description，正文早在 CSV、零引用）；desire-description 与 genre-hook-payoff-library 经 CSV 复核确认覆盖不成立，按方案规则保守保留（loading-map 注记复核结论与日期）。loading-map 复核：新增 S1-S4 加载方式变化表（L0 摘要/setting-read/预算/限量）。行为契约改造：`write_blocking_gate` 探针从纯文案断言改为运行时验证（临时项目真跑三道闸，断言失败关闭 + SKILL 门禁顺序保留）。C5 关闭：`test_memory_store` 有 contradicted 专项测试（story_fact/world_rule 状态迁移断言）+ tentative 覆盖，28 记忆测试全绿。完成于 2026-08-30 | 0.5-1d |
| S6 | `[x]` | P-3a 大纲字段名统一 | 准绳名定为 **`must_cover_nodes`/`forbidden_zones`**（fantasy01 35 章真书合同与 directive 解析侧在用）。统一七处：loader `_PLOT_SECTION_FIELD_MAP` 与 plot 解析尾部、`_append_plot_value` 列表字段判定、directive 兜底对改恒等、prewrite_validator（fulfillment_seed 键随改）、state_validator、runtime_contract_builder、prewrite.py `_plot_structure` 简化为只读准绳名（blocking_rules 兜底保留）、context_budget review 优先级表；SKILL 文案 :79-80 本就是准绳名。fantasy01 复验：真书合同（canonical）门禁正常，无行为回退。**顺带发现存量限制（非本次引入）**：大纲用 `- CBN：` 列表行时 plot 解析不识别（`_clean_plot_line` 只剥 `#` 标题头不剥列表符），本书一直走 `chapter_focus` 路径未受影响；记入 P-3 池备查。完成于 2026-08-30 | 0.5d |
| S7 | `[x]` | P-3b 性能残留 | ①latest.json 指针：`persist_commit` 维护（max 语义，回头补写不回退；accepted 单独记），读侧「指针范围内直达 + 文件存在校验 + 失效/越界回退线性扫描自愈」——指针语义明确为**章号最大**，与线性扫描语义一致，跳章/指针失效/尾部 rejected/回头补写四场景测试锁定（行为化探针证明直达：查询序 latest.json→chapter_NNN，未回扫）。②graph 候选下推：`_collect_graph_candidate_chunk_ids` 的 term 命中过滤下推 SQLite（vectors.content LIKE OR + ESCAPE + chapter 过滤），无命中的行不再拉进 Python，embedding 失败 chunk（content 行）同样参与；行为等价测试锁定。fantasy01 复验：无指针文件时回退扫描正确（latest=35 accepted）。完成于 2026-08-30 | 0.5d |
| S8 | `[x]` | P-3c 杂项 | ①别名预注册：commit 轻校验新增 `new_entity_few_aliases`（新实体 1-2 个别名提示 3-5 个，不阻断），data-agent 契约明确预注册要求与示例（周建军→老周/周建军/周总务）——直指真书实测反复出现的 missing_aliases。②题材数据：tropes 6→9 节（补悬疑推理/科幻/历史穿越），profiles 13→14（补 2.14 科幻/未来，`extract_genre_section` 实测可加载）；复核修正审计口径：悬疑/历史 profiles 早已存在，实际缺口是科幻。③正文 PostToolUse 留痕 hook：`chapter_body_trace.py` 写入 `正文/` 时记 JSONL 到 `.webnovel/logs/chapter_body_trace.log`（不阻断、异常静默），hooks.json 接入。完成于 2026-08-30 | 0.5-1d |
| S9 | `[x]` | D2 主代理往返压缩 | ①`preflight --all`：三查合一（preflight + `PROJECT_ROOT=` 行 + placeholder-scan 计数与明细；占位符存在退出码 1；不带 `--all` 行为不变），SKILL 三条命令并作一条 compound。②`run-ledger record-write-steps --steps-json`：批量冲账多步；SKILL 记账分层——崩溃粒度仍由 `run-log --append` 每步保证，ledger 台账在收尾一次冲账（崩溃缺冲账属预期，续跑以 run_last.log 为准）。fantasy01 实测 `--all` 一次往返三查全过。完成于 2026-08-30 | 0.5d |
| S10 | `[x]` | D3 大输出外置化 | 新增 `output_guard.py` + `main()` 包装：进程内命令 stdout 超 20k 字符（`WEBNOVEL_OUTPUT_EXTERNALIZE_CHARS` 可调）自动落盘 `.webnovel/tmp/cli_out/<tool>.txt`（同名覆盖），对话只留存根（EXTERNALIZED 标记 + tool/chars + 600 字符预览 + full-output 路径）；`WEBNOVEL_OUTPUT_EXTERNALIZE=0` 整体关闭；`_run_script` 子进程转发类命令天然豁免（extract-context/memory-contract/story-system 等已在 S1-S9 紧凑化）。集成测试经子进程实测 placeholder-scan 600 条占位输出被外置。完成于 2026-08-30 | 0.5d |
| S11 | `[跳过]` | D5 模型路由 | **核实结论**：宿主侧无按 agent 覆写模型的配置 schema（config.json 无此键、官方指南无此机制），唯一路由点是插件 agent frontmatter 的 `model:` 字段（现均为 inherit）。已交付[模型路由操作卡](../guides/model-routing.md)：一行切换/回滚步骤、冒烟验证、质量抽检协议（data-agent 优先，阻断问题漏检即回滚）、成本预期（参考三代理 134.8 万/章）。**2026-09-22 作者拍板放弃**：不输入便宜模型标识，维持 `model: inherit`（现状可用，避免模型标识不被宿主接受导致子代理派发失败、写章断链）；操作卡留档备用，未来成本压力重现可显式重开 | 0.5d |
| S12 | `[x]` | 发版 v6.5.0 | 四件套校验全绿（release notes OK/previous_tag v6.4.0、版本 sync 6.5.0、插件包 0/0、diff-check）+ pytest 1023 用例 EXIT=0（v6.4.0 时 949）+ 22/22 evals；commit ff2f677（bump）/e0ebe15（notes）、本地 tag v6.5.0、v7-tmp 已推。安装：市场克隆 detach 至 e0ebe15、缓存 6.5.0 与检出 IDENTICAL、installed_plugins 指向 6.5.0、缓存路径 preflight --all/meter 冒烟通过。**教训登记**：安装后立即删旧版缓存（6.4.0）导致当轮会话守卫钩子断链（钩子路径随会话启动固定）——旧版缓存清理须留到下一次会话或重启后。收尾（队列回写/指针提交/自检）于重启后完成于 2026-08-30 | 0.5d |
| S13 | `[x]` | C 阶段验收章 | **自然触发完成**（2026-08-31，fantasy01 第 36 章「放话」，v6.5.0 全链路 + D1 首次真实计量）：本章总消耗 **285.3 万**（主会话 129.6 万 + 4 子代理 155.7 万；去缓存新增 56.1 万）。对照：基线 −62%、「B 后 ≤300 万」**达标**、「C 后 ≤150 万」**未达标** → 按规则回插 S13a。**顺带发现并修复 D1 推断缺陷**：meter start 在写作会话首 turn 完成前会把会话推断到别的会话导致漏计主会话（本次低估 129.6 万）——聚合语义改为「窗口内全部轮次 + 主会话清单透出 + 并行主会话 WARN」，真值重放 MATCH | — |
| S13a | `[跳过]` | C 追加优化（S13 未达标回插） | ①D5 模型路由实施：按 [操作卡](../guides/model-routing.md) 切 data-agent→reviewer→context-agent 到便宜模型（子代理 155.7 万/章是最大杠杆，预期砍至 1/3-1/5）；②主会话 129.6 万中缓存读占大头（new 仅 13.0 万），按缓存计价换算真实成本后评估是否需要追加主会话优化；③下一章复测。**2026-09-22 封存**：随 S11 拍板放弃，①失去前置，②③失去对照前提；S13 回插规则保留原文备查，若未来 token 成本重新成为痛点需显式重开本项 | 0.5d |
| S14 | `[x]` | P-1 题材 taxonomy 收敛 | **核账结论：实施已在库（早期会话落地，本次为完成标准逐条核账）**。八项全过：①三硬编码 dict（PLATFORM_TO_CANONICAL/_LEGACY_GENRE_MAP/GENRE_INPUT_ALIASES）grep 零残留；②genre-index.csv 57 行在位、validate_csv 含双向校验且全量 0 错 0 警；③resolver（`resolve_genre_input`/`resolve_template_stems`）八用例全过且历史行为保留（玄幻→修仙.md、复合输入→悬疑+双模板）；④四个 SKILL 均 project_info 优先；⑤真实 CSV route rows 端到端测试在库（test_story_system_engine）；⑥模板 H1 已中文化；⑦GENRE_PROFILE_KEY_ALIASES 归属注释明确；⑧fantasy01 复合题材路由 canonical=都市 ✓ + 全量 pytest EXIT=0 + 22/22 evals | 0.5d（核账） |
| S15 | `[x]` | P-2 设定卡验证 | 30-36 章回看比对完成（36 份审查报告全量抽取）：卡后检出能力增强（setting 实体级漂移从 critical 暴露前移到 low/medium 即检出；ch26 引用卡片证据拦无条件全力一击、ch25 战力锚点核验成标准动作、ch31 资产漂移、ch33 两个 critical 全修复），三目标维度均有命中。决策：**Markdown 卡契约为正式能力**（[契约文档](../guides/setting-card-contract.md)），不引入 JSON Schema/Pydantic。验证报告：[2026-08-31-S15](../reports/2026-08-31-S15-设定卡验证与通用化决策.md)。完成于 2026-08-31 | 1d |
| S16 | `[x]` | E1 v6→v7 迁移器 | 计划：[2026-08-31-S16-v6v7迁移器-plan](2026-08-31-S16-v6v7迁移器-plan.md)；实现 `scripts/migrate_v6_to_v7.py`（`migrate_project` + CLI `--project-root/--output/--no-git`）：book.yaml（spec 7.0 平铺防呆方言）、定稿/正文（front matter 章号/标题/卷/字数 + 正文无损，卷号从时间线解析）、设定（角色卡按正名/世界观/时间线合并/名册从 index.db 生成带别名）、记忆/章摘要、大纲（总纲+卷纲零填充）、.gitignore（.cache/ 工作区/）、git init + quotepath false + `ch: v6 迁移导入` 初始提交；源零写入（单测 mtime 断言）；范围外显式 SKIP（承诺/审查报告/增强设定）。fantasy01 实测：36 章/36 摘要/8 设定/3 大纲、git 初始提交、真书零改动。完成于 2026-08-31 | 2-3d |
| S17 | `[x]` | E2 缓存重建 | 新增 `scripts/v7_cache.py`：`rebuild_cache` 从源文件（book.yaml + 定稿正文 front matter/名册/章摘要）全量重建 `.cache/index.db`（chapters/entities/summaries/meta 四表，每次重建重读源、缓存不遮蔽真相）；查询面 `get_chapter/find_entity/get_summary`；CLI `rebuild/verify/snapshot`。`verify_rebuild` = 删缓存→重建→查询快照等价（CI 验收项），源文件变化后重建快照自洽。fantasy01 迁移仓实测：36 章 verify equal=True。顺带改进迁移器：index.db 缺失时名册保底收录主角。完成于 2026-08-31 | 1d |
| S18 | `[x]` | E4 唯一写入路径 | `dual_format_guard.py`：落定检测（v6 accepted commit / v7 定稿正文）+ `check_unique_write_path` 阻断 + prewrite gate 接入（`STORY_REPO_ROOT` 配置，缺省零行为变化）；10 个用例（含双向阻断/未落定放行/rejected 不算落定/集成）。**提前执行的范围说明**：v7 侧对称调用由 S19 settle 接入，E1 落地后回填真实映射。完成于 2026-08-30 | 0.5d |
| S19 | `[x]` | E3 垂直切片 | v7_write.py 写路径闭环（decision/pack/checks/settle，TDD 10 用例含原子性回滚）；第 37 章《暗纸》全流程实跑：决策卡→上下文包（2,952/20,000 字符）→草稿（首稿 1,670 验收打回→四因诊断修复→二次稿 2,599 机检过）→**作者验收通过**→settle（原子 commit `30744af`，正文/章摘要/名册新增 赵姓汉子+熊铁山）→缓存重建 37 章可查询。真书（v6）零写入。完成于 2026-09-01 | 1-2d |
| S20 | `[x]` | E5 收尾 | 切片复盘：[2026-09-02-S20 报告](../reports/2026-09-02-S20-v7垂直切片复盘.md)——计量对比（去缓存新增 64.4 万 vs v6 56.1 万同量级且含开发混杂；子代理 0 个；上下文包 2,691 字符 −84%；总量因大会话形态不可比），M3 判定「有条件达标」，确认动作 = 第 38 章独立新会话干净计量回填；spec 命名三项漂移正式核销 + v7_write 注释对齐。多宿主立项决策：[ADR](../decisions/2026-09-02-多宿主适配立项决策.md)——立项，排 S21 后独立开队列，首期单宿主 ZCode。完成于 2026-09-02 | 0.5d |
| S21 | `[x]` | 发版 v7.0.0 | 本地发版完成（2026-09-02）：releases/v7.0.0.md 覆盖 v6.5.0..v7.0.0 全量（S13-S26，含另一会话的 S25/S26 增量审阅修复批）；版本同步 7.0.0、CHANGELOG 转正式段；四件套全绿 + 全量 1110 过（覆盖率 81.86% 门槛 80 生效）+ evals 22/22；commit `636ce24` + 本地 annotated tag v7.0.0。作者确认后推送完成（2026-09-02）：v7-tmp（18 提交）+ tag v7.0.0 + master 快进 2041aba→9da3c4d，Plugin Release 工作流 success，GitHub Release v7.0.0 产出（releases/v7.0.0.md 为正文）。**装机留待下一会话启动时**（S12 教训：钩子路径随会话固定，当轮换缓存断链）。 | 0.5d |
| — | `[跳过]` | D4 子代理并行 | **条件触发，默认跳过**：仅当 S13 验收显示「串行缓存 TTL 重读」仍是最大残余因子时才评估；与单线执行原则不冲突（这是运行时编排选项，非任务并行） | — |
| S22 | `[x]` | 分析：section 配额应该在何时确定 | [分析报告](../reports/2026-09-02-S22-配额确定时机分析.md)：分层结论——默认值=构建期静态（维持）；按书校准=书构建期一次预填（book.yaml 覆盖入口，数据源=书史统计而非 D1，D1 量的是消耗不是 section 需求且无历史归档）；按题材默认不采纳（体量不是题材函数）；运行期只做 DROP_ORDER 降级不重定配额。**队列注记的数据前提不成立**（D1 仅 fantasy01 一书且 meter 无历史归档），静态默认在单书现实下即最优；修订触发 = 第二本真实书 v7 链路 3-5 章的包占用分布（无需先建 D1 归档）。实施（book.yaml 覆盖入口+init/迁移预填+截断汇总信号，Bounded 档）**未排期待 Human 决定**。完成于 2026-09-02 | 届时定 |
| S23 | `[x]` | S22 结论实施：配额按书覆盖 | **Human 指示 2026-09-02「确定入队并完成」插队先行，S21 发版顺延其后**。①v7 `build_context_pack` 读 book.yaml 可选 `context_budget:` 节（`load_context_budget` 防呆方言解析器；优先级 显式参数 > book.yaml > 常量；v6 侧无 book.yaml，走原生 per-book 面：`context_load_total_budget` env 化 `WEBNOVEL_CONTEXT_LOAD_TOTAL_BUDGET`）。②迁移器预填：≥3 章按书史字数（book_word_stats 同源口径）写 `prev_chapter_tail = clamp(mean×0.5, 1200, 3000)`——实测唯一触顶节；其余节无数据支撑不预填，<3 章不写。③截断/占用信号：v7 pack stats 增 `truncated_sections`/`budget_used_ratio`（sections 键即生效配额表），v6 `enforce_budget` stats 增 `truncated_sections`。④测试 8 条全绿（覆盖优先级/缺省零变化/预填口径/截断信号），全量 1062 过 + 22/22 evals；fantasy01 冒烟：现有 book.yaml 零行为变化，重迁移将预填 1,339（解 1,207/1,200 触顶）。完成于 2026-09-02 | 0.5d |
| S24 | `[x]` | 审阅修复批（按优先级） | **Human 指示 2026-09-02「按优先级修复」**，范围=[全面审阅报告](../reports/2026-09-02-项目全面审阅报告.md) §7。①P1 名册双落点：`v7_cache` 兼读 `名册.md` 单表 + `名册/` 目录（同名目录优先），entities 表增 `first_chapter` 列，`find_entity` 返回首现章；3 条新测试含 v7 原生新书场景；fantasy01 真仓重建后 熊铁山/赵姓汉子 以正名+别名+首现章入查询面。②P2 覆盖率门槛：`.coveragerc` 钉 `source=webnovel-writer/scripts`（omit tests），门槛 90→80（实测 81.59%，18,068 语句）；`--no-cov` 与门槛的失真交互消除。③P2 spec 0.5 变更草案：六项偏差显式登记（名册双落点/commit 格式/front matter 键/决策卡形态/book.yaml 嵌套例外/承诺系统 v7.1），**待作者批准**，不改冻结条文原文。④P2 settle 回滚补 `git reset` 清 index。⑤P3：v7 三脚本使用指南一页（[v7-write-path](../guides/v7-write-path.md)）、SKILL 裸 python 3 处补 `-X utf8`、genre_taxonomy 入口测试 6 条。**明确延后**：承诺系统（v7.1，spec 草案第 6 条）、H2 total_words 对账（v7 迁移后消亡）、死代码删除（探查代理清单不可靠——cli_output 四定义经 print_success/print_error 传递使用，需逐符号人工核验）、BOM/docstring 卫生批。完成于 2026-09-02 | 0.5-1d |
| S25 | `[x]` | 增量审阅 P1 修复批 | **Human 指示 2026-09-02「P1-3 都处理」**，范围=[增量审阅报告](../reports/2026-09-02-增量代码审阅报告.md) §3 三个 P1。①P1-1 迁移器名册列序：`migrate_v6_to_v7` 元组按消费端 `(名,别名,首现章)` 对调；修复中同族新发现——`v7_cache` 单表解析器硬编码 `first_chapter=""` 不读第三列（S24 双落点修复只覆盖目录形态），一并修复并保持两列旧表兼容；迁移查询面断言（find_entity 别名+首现章）锁定。②P1-2 缓存生命周期：settle 尾迹自动 `rebuild_cache`（best-effort 不回滚事务，结果透出 `cache_rebuilt`）；`_conn` 增四表健康检查，缓存零字节/坏库/缺表首查自动重建（spec §1.2 派生物可丢弃补全）。③P1-3 `extract-context` 包装层 `--format` 对齐底层 json-only（default/choices 均改 json）。TDD 7 条回归（Red→Green 全过程留档）；全量 1078 绿 / 覆盖率 81.69% / 行为评测 22/22；同批回写 v7-write-path 指南已知边界、流程数据流全景（P1 标记移除，P2/P3 保留）、CHANGELOG。**明确未动**：报告 P2/P3 全部维持排队（S21 前修复批剩余 P2-1/P2-2/P2-3）。完成于 2026-09-02 | 0.5d |
| S26 | `[x]` | 增量审阅 P2/P3 修复批 | **Human 指示 2026-09-02「修复p2 和 p3 所有」**，范围=[增量审阅报告](../reports/2026-09-02-增量代码审阅报告.md) §3 P2 九项 + P3 七项。P2-1/2/3 settle 三重加固（章号前缀判重/回滚 unlink 逐文件保护+reset 范围化/git 身份兜底+stderr 透出）；P2-4 守卫接线（迁移器落 `dualformat.v6root` git config + settle 兜底读 + precommit 补挂 + 可选 `--link-back` 写 v6 .env，零写入契约保持）；P2-5 名册目录单文件容错；P2-6 dashboard mode=ro + 非回环需 `--allow-nonlocal`；P2-7 rejected 单调状态机对齐；P2-8 doctor 新增 total_words 双源对账 + word_counted_chapters 死分支清理（三路写入语义不动，v7 后消亡）；P2-9 决策卡渲染推进承诺段（承诺系统本体 v7.1）；P3-14 SKILL -X utf8 全仓清零；P3-15 三处文档漂移（reviewer/query/chapter-commit 必填化）；P3-17 guard 名单补 cp/mv/tee/sed -i/dd/git checkout（修复中被 guard 自拦一次，实证有效）；P3-18 watcher 兼听 WAL + SSE 心跳；P3-19 回滚删 tag 后新增文件（`-z` 规避 quotepath）；P3-20 run_ledger 原子+锁化；P3-21 state_changes 唯一索引；P3-22a memory 批量 upsert（writer 收集-flush、bootstrap 单次落盘）。TDD 32 条新回归（各批 Red→Green）；全量 1110 绿 / 覆盖率 81.86% / 行为评测 22/22。**延后（理由见报告 §4）**：P2-8 写入语义、P2-9 承诺本体、P2-6 Token、P3-18 前端重连（dist 预打包）、P3-22 b/c/d。完成于 2026-09-02 | 1d |

**S13 说明**：D1 已在每次写章自动出数，因此本项不阻塞队列推进——S12 发版若先于下一章写完，S12 照常执行，验收数字在数据可得时补录。

## 4. 每项固定动作（工作契约）

1. 动工：TodoWrite 建清单；bug 类先 systematic-debugging；TDD 红灯先行。
2. 收尾：全量 `pytest -q --no-cov` + 22 项 behavior evals → Conventional commit → 回写本队列状态 + 方案 checkbox + CHANGELOG → 主仓库子模块指针同步 → `check-workspace.ps1` 全 clean。
3. 涉及 SKILL/agent 文案的任务：同步 behavior eval 探针与 `test_prompt_integrity` 的 CLI 注册表（meter 已入册）。
4. 发版类任务（S12/S21）：push 与远端发布前须 Human 确认。
5. **收尾话术规则（Human 指示 2026-08-30）**：任务完成后向 Human 报告的结尾固定为「说『继续XX』接续」，XX 填队列中的**下一项任务编号与名称**（如「说『继续S10』接续」）。

## 5. 验收里程碑

| 里程碑 | 内容 | 依据 |
|---|---|---|
| M1 | S1-S3 完成后：下一章计量 vs 第 36 章基线 | §4 表（B 后 ≤300 万） |
| M2 | S13：C 阶段验收「≤150 万」 | §4 表；未达标则回插 C 追加任务 |
| M3 | S19：v7 垂直切片通过且计量不劣于 v6 链路 | story-repo spec 0.4 |
| M4 | S21：v7.0.0 正式版 | — |

## 6. 变更记录

- 2026-08-30 建队列：收编方案 Phase B 尾巴（B8）、Phase C（含勘误）、Phase D（D1 已完成）、Phase E、并行池（P-1/P-2/P-3）为单线序列；D4 降级为条件触发。
- 2026-08-30 追加 S22（Human 指示）：记录「section 配额应该在何时确定」分析待办，置于队列最末，暂不展开。
