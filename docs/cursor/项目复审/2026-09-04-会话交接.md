# 会话交接文件（W4）——项目复审 → 修复执行

> 用途：下会话开局**第一件事读本文件**，再按 `2026-09-04-项目复审报告.md` §9 从步骤 3 继续。
> 位置说明：模板建议放 gitignored 工作区；按 Human 2026-09-04 指示，Cursor 产出统一归 `docs/cursor/<任务名>/`，故随仓提交。
> 红线：左栏每个事实附出处；写不出出处的放右栏当假设。
> 关联：执行状态同步记在报告 §9 各步骤末尾。

## 会话 2026-09-04（Cursor，分支 `v8-author`）

### 本会话已完成（5 任务，W11 预算用满）

| # | 任务 | commit | 证据 |
|---|------|--------|------|
| 1 | 全项目复审报告 | `6711708` → 移至 `0a4f762` | `docs/cursor/项目复审/2026-09-04-项目复审报告.md`；全量 pytest 1452 绿 / 82.05%；四校验器 OK |
| 2 | 报告移至 `docs/cursor/项目复审/` | `0a4f762` | 三条相对链接 `Test-Path` 全 True |
| 3 | AGENTS.md 增「宿主产出文档归目录」约定 | `4181f15` | `AGENTS.md` 注意事项一节 |
| 4 | **§9 步骤 1**：`sync_plugin_version.py` 纳入 `mcp/server.py` SERVER_VERSION（P1-7）+ `.gitignore` 加 `.env`（P1-8） | `1cc23ce` | RED：`--check` 报 `mcp/server.py SERVER_VERSION=7.1.0` exit 1 → 同步 → `Versions are in sync: 8.0.0`；新增 `scripts/tests/test_sync_plugin_version.py` 3 例；全量 **1455 passed / 82.39%** |
| 5 | **§9 步骤 2**：README / AGENTS.md / docs/README / commands.md / 旧待办清单 superseded / .gitignore 去 AGENTS.md（P0-1/P0-2/P0-3/P1-3/P1-4） | `d726307` | `sync --check` / `validate_plugin_package` / `validate_release_notes` 全 OK；`scripts/tests` 全绿；docs/README 相对链接 0 断链；`git check-ignore AGENTS.md` exit 1 |
| 6（Human「继续」覆盖预算） | **§9 步骤 3**：F→T 规格对账表 + 08 计划覆盖声明/T34、T7 勘误 + v8-gap-review P4-0 前置与 §2.1 勘误 | `13545dd` | 29 个引用 hash `git cat-file -e` 校验，唯一不存在的 `8f0a5c9` 已改 `78a8a16` |
| 7 | **§9 步骤 4**：本地 annotated tag `v8.0.0` @ `13545dd` | tag（未推送） | `git describe --tags` → `v8.0.0`；`validate_release_notes` OK |
| 8 | **§9 步骤 5**：工作区 `.gitmodules` 补映射 + 指针同步 | 工作区 `1ec886e` | `git submodule status` → ` 13545dd… (v8.0.0)`；旧克隆未动 |

### 已验证事实（附出处）

| 事实 | 出处 |
|------|------|
| 当前版本 8.0.0，plugin.json / marketplace 双位置 / README 徽章+表 / mcp SERVER_VERSION 四处一致 | `python -X utf8 webnovel-writer/scripts/sync_plugin_version.py --check` → `Versions are in sync: 8.0.0`（commit `1cc23ce` 后） |
| 本仓 `git tag` 仅 `v7.0.0`，无 v7.1.0 / v8.0.0 | `git tag` 输出 |
| `v8-author` 无 upstream，领先 master 57+ 提交（本会话又 +5） | `git for-each-ref --format='%(refname:short) %(upstream:short)' refs/heads` |
| 工作区对本仓路径**有 gitlink 指针但原无 `.gitmodules` 映射**（初稿误判「未跟踪」，已勘误）；旧路径 `projects/claude-plugins/webnovel-writer`（HEAD `9ae508c`，v7-tmp）仍在 `.gitmodules` | `git -C c:\lgq\ai-workspace ls-tree HEAD projects/zcode-plugins/webnovel-writer` → `160000 commit 0b0db95…`；映射已在工作区 `1ec886e` 补上 |
| 07 方案 F-13 在 08 计划零引用；F-01 由 T4 覆盖但未标 ID | 正则抓取 07 标题 vs 08 全文 `F-\d+` 差集 = `['F-01','F-13']` |
| T34 证据行「三处同步含 mcp SERVER_VERSION」不实 | `git show --stat cff0ef0` 仅改 plugin.json / reference-loading-map.md / validate_reference_wiring.py |
| `build_context_pack` 7 section、`self_check_batch` 4 项、settle 零后置钩子、learn CLI 冗余位置参数 | 复审报告 §6 表（file:line） |
| doctor 在纯 v7 书仓解析不到项目根 → 12 组 v6 检查整体跳过 | 复审报告 §6 引申发现；08 计划 T1 证据行「phase 解析限制」 |
| **`v7_write.py` 没有任何 skill / command / agent 调用**：全仓 `*.md` grep `v7_write|v7-write` 仅命中 `migrate_v6_to_v7.py` 与自身；`/webnovel:write` SKILL 全程 v6 链（`state.json` / preflight / review-pipeline / chapter-commit）；`settle` 只有 Python API 无 CLI 子命令（`v7_write.py main` 仅 decision / pack / check）；`pack` 子命令构造的 decision `entities=[]`，`entities` section 经 CLI 永远为空 | `v7_write.py:483-516`；`docs/guides/v7-write-path.md` §3「settle 为 Python API」；步骤 6 brainstorming 探索阶段 grep |

### 假设 / 待确认

- 假设：23 条 sqlite `ResourceWarning` 来自四个返回裸连接的工厂函数（`db.connect` / `dashboard/app._get_db` / `draft_selection._conn` / `v7_cache._conn`）的调用方漏关——**待验证**：`python -X tracemalloc=5 -W error::ResourceWarning -m pytest <单用例>`。
- 假设：`06-data-design.md §12` 六条不变量仅 1 条实现（来自 v8-gap-review §2.2，本会话未逐项复核）。
- 假设：README 中 trendshift / star-history 徽章仍指上游是作者有意保留的致谢，未改。

### 会话 2026-09-04 后半（同会话续，Human 批复后）

Human 批复原文：「批准，默认值你先设置，2可以，3，可以 4不用」→ ① spec 批准（默认值：6 项 v6-only section 不移植、prose 门全 flagged 阻断）；② push + tag + Release 批准；③ 删旧克隆批准；④ 不补 `v7.1.0` tag。

| 已验证事实 | 出处 |
|---|---|
| `v8-author` 已推送并设 upstream；tag `v8.0.0` 在远端 `6889927…`；GitHub Release 已建 | `git push -u origin v8-author` → `[new branch]`；`git ls-remote --tags origin v8.0.0`；`gh release view v8.0.0 --json url` → https://github.com/alittleseven/webnovel-writer/releases/tag/v8.0.0 |
| 旧克隆删除无损：三分支均与 origin 同步、工作树干净、HEAD `9ae508c` 存在于本仓历史 | `git -C <旧克隆> status --porcelain` 0 行；`branch -vv` 全部 `[origin/*]` 无 ahead；本仓 `git cat-file -e 9ae508c^{commit}` exit 0 |
| 旧克隆 `.git` 为嵌入目录，`submodule deinit` 迁移失败（Permission denied）；改手工：`.gitmodules` 去节 + `git rm --cached` + Python `\\?\` 长路径 rmtree（pytest 残留 >260 字符路径） | 工作区 commit `34bc2a4` |
| 阶段一实现 7 任务全部完成：`d645d70`（pack 10 section + DROP + `--json`）/ `d404a29`（三门禁 + bypass + settle CLI）/ `73b70a7`（`webnovel.py v7-write` 转发）/ `841831b`（SKILL 分流 + eval + 指南）/ `06a9ed1`（prompt 注册表） | `git log --oneline 4d0bc9e..HEAD` |
| 全量回归 `1483 passed`（此前 1455，+28）；覆盖率 82.67%；四个 validator 全 OK；`run_behavior_evals --suite fast` 23/23 | `pytest -o addopts="" -q` 末行；`--cov` 表 `TOTAL … 83%` |
| fantasy01 ch42 真仓冒烟（只读副本）：包含 决策卡 / 本章应推进（承诺账本）/ 作者修改未消费（stale，5 条）/ 前情摘要 / 上一章结尾 / 素材装配 / 作者模型 / 书级元信息；`section_errors={}`；缺 文风宪法（无 `文风/宪法.md`）/ 章纲节选（无 `第02卷*` 大纲）/ 主角卡（book.yaml 无 `主角:`）/ 读者信号（无 index.db）均为数据缺失 | `v7-write pack --chapter 42` → `OK … used=5,774`；`$env:TEMP\wn_smoke.log` |
| 设计偏差（实现期决定，已写进 spec/plan 与 guide）：`pov_discipline` 只在 book.yaml 声明 `主角` 且 pov 不同时注入 §一（未声明 → 省略，避免恒截断噪音）；legacy `TestSettle` 用 autouse 夹具供 0-blocking 审查文件并打桩 `_prose_flagged`，门禁单独在 `test_v7_write_gates.py` 覆盖 | `v7_write.py::_sec_pov_discipline` docstring；`test_v7_write.py::TestSettle::_gates_green` |

- 假设：fantasy01 `book.yaml` 加 `主角: 苏小白` 后主角卡与视角纪律即生效（未在真仓改动验证；书仓归 loom-books，非本任务范围）。

### 交接给下一会话

- **进行到**：复审报告 §9 步骤 1-6 全部完成；Human 待决三项已全部执行（push/Release ✅、旧克隆删除 ✅、v7.1.0 不补 ✅）。
- **下一步**：`v8-gap-review-3rounds` **阶段二**（P2-1 章纲卡标题/账本/人物/时间锚一致性闸 …，见该 README）。依赖图 P1-2 → P3-1（settle 后置钩子）已可开工。阶段二/三仍走 Architectural：spec → plan → TDD，spec 放 `docs/cursor/<任务名>/`。
- **未推送**：本会话后半的项目仓提交（`4d0bc9e..06a9ed1` + 收尾 docs commit）与工作区提交均只在本地；push 需 Human 再批。
- **坑**：
  - PowerShell 内联 Python 含 `[a-z]`、反引号会被 PS 解析炸掉——复杂脚本写到 `$env:TEMP\*.py` 再跑。
  - 中文 commit message 必须 UTF-8 文件 + `git commit -F`；`git add` 多路径时任一路径不存在会整条失败（本会话踩过一次，靶向 `git add` 后 `--amend` 补救）。
  - `sync_plugin_version --check` 现在会检查 `mcp/server.py`；改版本号一律用脚本 `--version X.Y.Z`，不要手改四处。
  - 全量 pytest 约 110s；`-q` 下最终 summary 行被覆盖率表吞掉，用进度 `.` 计数或 `--no-cov` 看 passed 数。

### 会话 2026-09-04 续（Cursor，「继续」= 阶段二 P2-1/P2-2）

点名技能：`dev-workflow` + `test-driven-development` + `systematic-debugging`（时间线回归）+ `commit-discipline`。spec/plan 已在他会话批准（`2651d32` / `5a787f4`）。

| 已验证事实 | 出处 |
|---|---|
| 规范路径解析优先 `大纲/卷纲/第NN卷-详细大纲.md` | `test_outline_paths.py`；commit `948bf79` |
| 迁移器不再剥 `-详细大纲` 后缀 | `test_migrate_v6_to_v7.py`；`06028ac` |
| v7/v6 读链复用共享解析器 | `0ea89e3` |
| 承诺不存在 / 跨批时间倒流 → `consistency_gate` 零落盘；标题 mismatch warning 仍写卡 | `a939ef7`；`TestConsistencyGate` 定点全绿 |
| fantasy01 ch43 标题「夜袭」与 **第01卷** 详细大纲一致；错标题 warning 仍写 | `$env:TEMP\wn_p2_smoke.py` → `MATCH_OK True` / `WRONG_OK True codes ['outline_title_mismatch']` |
| 全量 `1505 passed`，cov 82.72% | `pytest -o addopts=""` 末行 `1505 passed in 100.27s`；cov 跑 `Total coverage: 82.72%` |
| 时间线 4 例曾红：夹具引用 `F-003` 未入账，闸拒写后视图无 `\| 39 \|` | 失败栈 `test_timeline_view.py:68 ValueError`；补种 `create_entry(..., entry_id="F-003")` 后 6/6 绿 |

- 假设：P2-3 Inv-5 在 fantasy01 有 `.story-system` 时可跑合同对账（未在本切片验证）。

### 交接给下一会话（已被后续 P2-3 切片覆盖）

- 当时进行到：P2-1/P2-2 完成；P2-3 Task 1 `c304aa9`。下一步曾是 Inv-1/Inv-2。

### 会话 2026-09-04 续（Cursor，「继续」= 阶段二 P2-3）

点名技能：`dev-workflow` + `test-driven-development` + `commit-discipline`（收尾 `finishing-a-development-branch`）。spec/plan 已批准：`docs/cursor/阶段二-数据不变量/`。

| 已验证事实 | 出处 |
|---|---|
| Inv-1 journal + Inv-2 轨迹 | commit `5cdbb20`；`test_invariant_check.py` |
| Inv-3 力量 + Inv-4 条目/retcon | `fe5e7fc` |
| stale `since_chapter` + Inv-6 | `873fa21`；`max_settled_chapter` 在 `dual_format_guard.py` |
| Inv-5 重建对账：skip/pass/篡改 fail/无 review warn/坏 MASTER 不抛 CLI | `f4a8c50`；`TestContractRebuildInvariant` |
| fantasy01 六项：inv-1 fail / inv-2 pass / inv-3 pass / inv-4 pass / inv-5 skip / inv-6 warn；exit 1 | `webnovel.py --project-root $env:TEMP/fantasy01-invariants invariants --format json` |
| 空 v7 合同 skip，CLI exit 0 | `test_cli_json_empty_v7_exits_zero`；summary 4 pass / 2 skip |
| 全量 `1544 passed`，cov 82.82%；evals 23/23；三校验器 OK | `pytest -o addopts="" -q` 末行；`Total coverage: 82.82%`；`run_behavior_evals.py --suite fast`；`validate_plugin_package.py` / wiring drift=0 / `Versions are in sync: 8.0.0` |

- 假设：fantasy01 journal 的 `action=add` 与 `domain=其他` 属历史脏数据，不在 P2-3 内自动修复（校验器只读）。

### 交接给下一会话

- **进行到**：v8-gap-review **阶段二全部完成**（P2-1/P2-2 章纲闸 + P2-3 六项不变量）。P2-3 计划 Task 1–6 已勾选。
- **下一步**：阶段三 **P3-1** v7 settle 后置钩子（materials log → settle_style_domain → reading_power），见 `docs/zcode/v8-gap-review-3rounds/README.md`。仍走 Architectural：spec → plan → TDD，文档放 `docs/cursor/<任务名>/`。
- **未推送**：`v8-author` 阶段一后半 + 阶段二全部只在本地；push 需 Human 再批。
- **坑（P2-3 新增；P2-1/P2-2 仍有效）**：
  - 闸上线后，既有测试若 `承诺推进` 引用账本没有的 ID，`create_chapter_batch` 会整批拒绝。
  - error 门禁不要断言 `journal.jsonl` 不存在——骨架会建空文件；比内容。
  - fantasy01 第43章在第01卷，不要按 README 草稿里的「第02卷」造文件。
  - 计划里的 `test_runtime_contract_builder.py` 在 `scripts/data_modules/tests/`，不在 `scripts/tests/`。
  - `validate_chain` 参数是 project_root，不是 anchor dict。
  - Inv-3 无 `设定/力量锚点.yaml` → skip，不要用 `load_anchor` 空骨架当缺文件。
  - Inv-5 有 `.story-system` 无 review → **warn** 不是 pass；无该目录才 skip。
  - 全量 `-q` 时 summary 会被 cov 表吞，用 `-o addopts=""` 看 passed 数。
  - **不要清 fantasy01 重复大纲（P3-3）；不要动 v6 `context_manager` / write-gate。**

### 会话 2026-09-04 续（Cursor，「先收尾，再继续」= 闭合 P3-1）

点名技能：`finishing-a-development-branch` + `commit-discipline`。实现期已用 `dev-workflow` + `test-driven-development`。spec/plan 已批准：`docs/cursor/阶段三-settle后置钩子/`。无 ledger（未走 SDD）。

| 已验证事实 | 出处 |
|---|---|
| 实现 `feat(settle): 成功后落账轨迹、指纹与追读力` | `git log -1 --oneline` → `6ed016f` |
| spec §4.5：真仓无 `0043-` 定稿，冒烟用 ch43 | `projects/loom-books/fantasy01/定稿/正文` 无 `0043-*`；副本 `$env:TEMP/fantasy01-p3-1-smoke` |
| CLI 一行后置 | `OK v7-write settle chapter=43 committed=False bypassed=False file=…\0043-夜袭.md post=materials:ok/2 style:fp=42,samples=0 reading:ok` |
| 三表 | 轨迹 `SP-001`+`WT-001`；指纹 `fp=42,samples=0`；`get_chapter_reading_power(43)` `hook_type=悬念` |
| 全量回归 | `1550 passed in 120.18s`；`Total coverage: 82.85%`；evals 23/23；三校验器 OK（package / wiring drift=0 / versions 8.0.0） |
| git add 中文路径 | 单测必须 `git -c core.quotepath=false ls-files`，否则 octal 误判未 add |

- 假设：真仓 `素材/活/` 无 `场景写法.csv` 属历史缺表，不在 P3-1 内补真仓（副本补种仅冒烟）。

### 交接给下一会话

- **进行到**：v8-gap-review **阶段三 P3-1 完成**（settle 后置：轨迹 / 指纹 / 追读力）。计划 Task 1–3 已勾选。
- **下一步**：阶段三 **P3-2** 工坊同步执行器（`forge-sync` 消费 `power_anchor_sync:required` / `contract_rebuild:required`）。仍走 Architectural：brainstorming → spec → 批准 → plan → TDD，文档放 `docs/cursor/<任务名>/`。
- **未推送**：`v8-author` 阶段一后半 + 阶段二 + P3-1 只在本地；push 需 Human 再批。
- **坑（P3-1 新增；前序仍有效）**：
  - settle 后置在 `git add` **之前**；`--no-commit` 仍跑后置。
  - v7 审查无 `overall_score` → 只更新指纹、`recorded=0`。
  - 摘要无 `hook_type` → 追读力 skipped（不阻断）。
  - `git ls-files` 默认 quotepath 把中文打成 octal。
  - 章纲卡 `素材引用` 指向不存在表/ID → 门③拒绝，后置根本不会跑。
  - 不要清 fantasy01 重复大纲（P3-3）；不要动 v6 `context_manager` / write-gate / `chapter_commit.py`。

### 会话 2026-09-04 续（Cursor，「收尾」= 闭合 P3-2）

点名技能：`finishing-a-development-branch` + `commit-discipline`。实现期已用 `dev-workflow` + `test-driven-development`。spec/plan 已批准：`docs/cursor/阶段三-工坊同步执行器/`。无 ledger（未走 SDD）。

| 已验证事实 | 出处 |
|---|---|
| 实现 `feat(forge): 扫描并消费工坊同步标记` | `git log` → `f23eeb5` |
| confirm 功法 → 两种 pending；法宝仅合同 | `test_confirm_gongfa_lists_both_kinds` / `test_confirm_fabao_only_contract` |
| mark-cleared 后空；CLI 走通 | `test_mark_cleared_all_after_gongfa`；`test_webnovel_cli_forge_sync` |
| `forge-sync -h` | `usage: webnovel.py forge-sync [-h] [--kind KIND] [--format {text,json}] [{status,mark-cleared}]` |
| 全量回归 | `1558 passed in 125.12s`；`Total coverage: 82.85%`；evals 23/23；三校验器 OK |

- 假设：fantasy01 真仓 journal 若无工坊 confirm 标记，`forge-sync` status 为空属数据现状，不在 P3-2 内补种。

### 交接给下一会话

- **进行到**：v8-gap-review **阶段三 P3-1 + P3-2 完成**。下一步 **P3-3** 写回 JSON 播种 + `learn` CLI 修正 + fantasy01 重复大纲去重。
- **下一步**：P3-3 仍走 Architectural：brainstorming → spec → 批准 → plan → TDD，文档放 `docs/cursor/<任务名>/`。
- **未推送**：`v8-author` 阶段一后半 + 阶段二 + 阶段三 P3-1/P3-2 只在本地；push 需 Human 再批。
- **坑（P3-2 新增；前序仍有效）**：
  - 标记生产者是 `forge confirm`，不是 `adopt`。
  - `webnovel.py --project-root` 宽松解析需要 `book.yaml`（或 v6 `.webnovel/state.json`）；纯 `init_domain_skeleton` 不够。
  - `--kind all` 只清当前非空 kind；两队列都空或缺 `--kind` → exit 2、不写盘。
  - 多余 `cleared` 不吞后续 `required`。
  - 不要清 fantasy01 重复大纲（P3-3）；不要动 v6 `context_manager` / write-gate / `chapter_commit.py`。

### 会话 2026-09-04 续（Cursor，「收尾」= 闭合 P3-3）

点名技能：`finishing-a-development-branch` + `commit-discipline`。实现期已用 `dev-workflow` + `test-driven-development`。spec/plan 已批准：`docs/cursor/阶段三-写回播种与CLI/`。无 ledger（未走 SDD）。

| 已验证事实 | 出处 |
|---|---|
| 实现 `feat(cli): 写回播种账本并修复 learn 默认动作` | `git log` → `cd6016d` |
| 播种 7 条 / 幂等 skip / 不读开放环 | `test_writeback_seed.py`：`test_seeds_seven_foreshadow_entries` / `test_idempotent_skips_duplicate_names` / `test_ignores_open_loop_writeback` |
| `learn --from-journal` 可跑 | `test_learn_from_journal_without_nested_action`；`learn -h` → `[{learn,apply,show}]` |
| 书仓去重 | `e28e7c5`；`resolve_detailed_outline(..., 1)` → `第01卷-详细大纲.md`；`dup_exists False` |
| 全量回归 | `1568 passed, 23 warnings in 127.90s`；`Total coverage: 82.97%`；evals 23/23；三校验器 OK |

- 假设：真仓 fantasy01 若日后手跑 `seed-from-writeback --volume 1`，会新增 7 条 F-00x（与现有 F-001～F-003 名称不同，不会幂等跳过）。

### 交接给下一会话

- **进行到**：v8-gap-review **阶段三全部完成**（P3-1 settle 后置 / P3-2 forge-sync / P3-3 写回播种 + learn CLI + 大纲去重）。
- **下一步**：阶段四 **P4-0** doctor 在纯 v7 书仓解析项目根（P4-1 前置）。仍走 Architectural：brainstorming → spec → 批准 → plan → TDD，文档放 `docs/cursor/<任务名>/`。
- **未推送**：`v8-author` 阶段一后半 + 阶段二 + 阶段三全部只在本地；fantasy01 `e28e7c5` 亦未推送。push 需 Human 再批。
- **坑（P3-3 新增；前序仍有效）**：
  - 写回 JSON 实际 7 条伏笔，不是验收原文的 8；不要造第 8 条。
  - 真仓不要默认跑 seed（会污染账本 + 再写 `action=add` journal）。
  - `learn` action 已可省略；`apply` / `show` 仍须显式。
  - fantasy01 仍有预先存在的脏文件（journal、author_model、对账报告等），P3-3 只删了 `第01卷.md`。
  - 不要动 v6 `context_manager` / write-gate / `chapter_commit.py`。
  - 全量 `-q` 时 summary 会被 cov 表吞，用 `-o addopts=""` 看 passed 数。

### 会话 2026-09-04 续（Cursor，「继续」= 闭合 P4-0）

点名技能：`dev-workflow` + `test-driven-development` + `finishing-a-development-branch` + `commit-discipline`。spec/plan 已批准：`docs/cursor/阶段四-doctor根解析/`。无 ledger（未走 SDD）。

| 已验证事实 | 出处 |
|---|---|
| 实现 `feat(doctor): 纯 v7 书仓解析项目根并按定稿体检` | `git log` → `47e661c` |
| tmp `book.yaml` → `v7_story_repo`，定稿 42，忽略 v6 `正文/` | `test_v7_book_yaml_is_story_repo_phase` / `test_v7_ignores_v6_zhengwen_flat_dir` |
| state 优先 / 空目录仍 no_project | `test_state_json_wins_over_book_yaml` / `test_empty_dir_without_state_or_yaml_is_no_project` |
| v7 file 组 + 缺定稿 error + 无设定集 INIT 行 | `test_doctor_v7_runs_project_groups_not_only_python` / `test_doctor_v7_missing_finalized_dir_errors` |
| CLI 十二组 + preflight ok | `test_doctor_cli_v7_emits_twelve_groups` |
| fantasy01 真仓 | `phase=v7_story_repo`；`group_hits 12 / 12`；`check_count=32`；`preflight.project_root` ok；`file.v7.*` 四条；`file.dir.设定集` 空；`ok True`；exit 0 |
| 全量回归 | `1575 passed in 122.50s`；`Total coverage: 82.99%`；evals 23/23；三校验器 OK |

- 假设：fantasy01 `target_chapter` 等于 `max_settled_chapter`（测试用 42 夹具覆盖；真仓未单独打印 expected_profile.target_chapter）。

### 交接给下一会话

- **进行到**：v8-gap-review **阶段四 P4-0 完成**（纯 v7 书仓 doctor 根解析 + 十二组既有检查跑到）。
- **下一步**：阶段四 **P4-1** doctor 八组治理检查（F-13）。仍走 Architectural：brainstorming → spec → 批准 → plan → TDD，文档放 `docs/cursor/<任务名>/`。
- **未推送**：`v8-author` 阶段一后半 + 阶段二 + 阶段三 + P4-0 只在本地；fantasy01 `e28e7c5` 亦未推送。push 需 Human 再批。
- **坑（P4-0 新增；前序仍有效）**：
  - 有 `state.json` 仍走 v6 INIT 清单；state + yaml 并存 = v6。
  - v7 file 只查 `book.yaml` / `定稿/正文` / `大纲` / `作者`，不要再输出 v6 `设定集/` 行。
  - 不改 `project_locator.resolve_project_root` 默认严格语义；doctor/preflight 才走 `_resolve_root_lenient`。
  - 不强制 `.story-system`；不在真仓补 `state.json`。
  - P4-1 治理组未建；§2.1 完成记录只写「12 组已跑到」。
  - 不要动 v6 `context_manager` / write-gate / `chapter_commit.py`。
  - 全量 `-q` 时 summary 会被 cov 表吞，用 `-o addopts=""` 看 passed 数。

### 会话 2026-09-04 续（Cursor，「继续」= 闭合 P4-1）

点名技能：`dev-workflow` + `test-driven-development` + `finishing-a-development-branch` + `commit-discipline`。spec/plan 已批准：`docs/cursor/阶段四-doctor治理检查/`（方案 A）。无 ledger（未走 SDD）。

| 已验证事实 | 出处 |
|---|---|
| 实现 `feat(doctor): 接入八组治理体检` | `git log` → `c0c5981` |
| tmp v7 恒 8 个 `gov.*`，无 blocker，ok True | `test_doctor_v7_emits_eight_governance_checks` |
| journal `domain=其他` → inv-1 warning 且 doctor ok | `test_doctor_maps_invariant_fail_to_warning_not_blocker` |
| 素材衰减 / 画廊两卷 / 缺组 skip | `test_doctor_materials_health_warns_on_decayed_active` / `test_doctor_gallery_backlog_warns_after_two_volumes` / `test_doctor_gallery_and_materials_skip_when_absent` |
| fantasy01 真仓 | `returncode 0`；`ok True`；`phase=v7_story_repo`；`gov_present 8 / 8`；inv-1 warning；inv-2/3/4 ok；inv-5 skipped；inv-6 warning；materials warning；gallery skipped；无 gov blocker |
| MCP 仍转 `doctor --format json` | `mcp/server.py` `_build_doctor`；`test_server.py` `webnovel_doctor` 映射 |
| 全量回归 | `1580 passed in 129.05s`；`Total coverage: 83.03%`；evals 23/23；三校验器 OK |

- 假设：fantasy01 素材 warning 来自活层 decayed+active，未在本切片改真仓 CSV。

### 交接给下一会话

- **进行到**：v8-gap-review **阶段四 P4-0 + P4-1 完成**（v7 根解析 + 八组治理体检）。
- **下一步**：阶段四 **P4-2** dashboard 承诺账本视图（`governance.py` 各状态计数+逾期列表；GovernancePage 第七段）。仍走 Architectural：brainstorming → spec → 批准 → plan → TDD，文档放 `docs/cursor/<任务名>/`。
- **未推送**：`v8-author` 阶段一后半 + 阶段二 + 阶段三 + P4-0/P4-1 只在本地；fantasy01 `e28e7c5` 亦未推送。push 需 Human 再批。
- **坑（P4-1 新增；前序仍有效）**：
  - 治理 `fail`/`warn` → doctor `warning`，**不是** blocker；fantasy01 Inv-1 fail 时 doctor 仍可 `ok=True`。
  - 不做 git dirty vs journal/watermark 未留账（方案 A）。
  - 不改 P2-3 六项语义、不改 `invariants` CLI 退出码。
  - v6 INIT 清单 + 8 组 gov 会把 doctor JSON 顶过 20000，`output_guard` 外置 stdout；CLI 测需认 `EXTERNALIZED` / `full-output:`，不要为过线砍治理组。
  - `WEBNOVEL_OUTPUT_EXTERNALIZE=0` 可关外置，默认不要关。
  - §2.1 历史「零治理检查」实测句不要改写成从未缺过。
  - 不要动 v6 `context_manager` / write-gate / `chapter_commit.py`。
  - 全量 `-q` 时 summary 会被 cov 表吞，用 `-o addopts=""` 看 passed 数。
