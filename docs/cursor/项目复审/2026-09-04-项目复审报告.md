# 项目复审报告（2026-09-04，v8.0.0 发版后）

> 审阅对象：webnovel-writer 全项目，分支 `v8-author` @ `0b0db95`（v8.0.0 已发版，T1-T34 全勾）
> 方法：主代理亲自跑全量测试 / 四个校验器 / git 分叉与工作区集成核查 / 文档漂移比对，两个只读探查子代理分别做「上一轮缺口复盘抽样复核」与「代码质量与安全扫描」。所有结论附命令实际输出或 file:line。
> 关联：[2026-09-02 项目全面审阅报告](../../reports/2026-09-02-项目全面审阅报告.md)（v7 发版前）、[v8 迁移缺口审计](../../zcode/v8-migration-gap-audit/README.md)、[v8 缺口三轮复盘](../../zcode/v8-gap-review-3rounds/README.md)（同日，41 项缺口 + 4 阶段修复计划）
> 技能：code-review（本报告按其 P0/P1/P2 分级输出）

## 1. 总评

**代码面健康、治理面失真。** 全量 1452 用例全绿、覆盖率 82.05%（门槛 80）、四个校验器（版本三处同步 / 插件包 / 发版说明 / 引用接线 drift=0）全部 OK，工作树干净。但 v8.0.0 发版后**面向用户与协作者的四层文档全部停在 v7 或更早**（项目 AGENTS.md / README / docs/guides/commands.md / docs/README.md 与「唯一待办入口」），且 08 实施计划 34/34 勾选的同一天，缺口复盘报出 41 项缺口——其中最重的 F-13「doctor 治理层体检」在 08 计划里**根本没有对应任务**，属于 spec→plan 追溯断裂（工作区 W1 类失效），不是实现失误。仓库层还有 v8.0.0 未打 tag、`v8-author` 无上游、工作区子模块指针仍指向旧路径的陈旧克隆三项。

上一轮（2026-09-02）报告的 P1「名册双落点」与「承诺系统缺位」已核销（承诺账本 T27 落地 `promise_ledger`）；本轮**新增 P0 4 项、P1 9 项、P2 若干**。无代码级 P0；代码级 P1 三项：`mcp/server.py` 的 `SERVER_VERSION` 仍是 7.1.0 而 T34 证据行声称已同步（`git show --stat cff0ef0` 证明该提交未碰 server.py）、`.env` 未进 `.gitignore`、doctor 对纯 v7 书仓解析不到项目根导致 12 组检查整体跳过。

## 2. 事实基线（命令实际输出）

| 检查 | 命令 | 结果 |
|---|---|---|
| 全量测试 | `python -X utf8 -m pytest -q -p no:cacheprovider` | 1452 用例全绿（20×72+12 个 `.`，无 F/E）；`Required test coverage of 80% reached. Total coverage: 82.05%`；23 条 `ResourceWarning: unclosed database`（见 §5） |
| 版本三处 | `sync_plugin_version.py --check` | `Versions are in sync: 8.0.0` |
| 插件包 | `validate_plugin_package.py` | `OK plugin package errors: 0 warnings: 0` |
| 发版说明 | `validate_release_notes.py` | `OK release notes version: 8.0.0 previous_tag: v7.0.0` |
| 引用接线 | `validate_reference_wiring.py` | `OK reference wiring: assets=63 consumers=53 drift=0` |
| 工作树 | `git status --porcelain` | 空（干净） |
| 组件盘点 | `Get-ChildItem` | skills 8 / agents 4 / commands 13 / hooks 4 脚本 + hooks.json / mcpServers `webnovel` / userConfig 3 项（bookProjectRoot, draftsPerChapter, materialTopK） |
| 分叉 | `git rev-list --count` | `v8-author` 领先 `master` 57 提交、领先 `tmp/zcode` 49 提交；`master` 领先 `origin/master` 1 提交；`v8-author` **无上游** |
| 标签 | `git tag` | 本仓仅 `v7.0.0`；**无 `v7.1.0`、`v8.0.0`**（`validate_release_notes` 因此报 previous_tag=v7.0.0） |
| 工作区集成 | `git -C ai-workspace submodule status projects/zcode-plugins/webnovel-writer` | `fatal: no submodule mapping found in .gitmodules`。**勘误（步骤 5 执行时核实）**：`git ls-tree HEAD` 显示该路径已是 160000 gitlink 且逐提交同步（`283fa61` 等），缺的只是 `.gitmodules` 映射；`.gitmodules` 另登记着旧路径 `projects/claude-plugins/webnovel-writer`（指针 `9ae508c`，分支 `v7-tmp`） |

## 3. P0 — 必须修复

### P0-1 README 装机指引指向上游仓库，用户按文档装不到 v8

- `README.md:99` marketplace 源写 `lingfengQAQ/webnovel-writer`；`README.md:108` requirements 拉 `raw.githubusercontent.com/lingfengQAQ/...`。全文 `lingfengQAQ` 6 处、`alittleseven` 0 处。上游仍是 v6 Claude Code 插件，**没有 `.zcode-plugin/`、MCP、`/webnovel:*`**，按 README 装出来的不是本仓的 v8.0.0。
- `README.md:17-25`「版本导览（2026-08-19 更新）」表整段来自上游：声称 `master` 是 v6、`v8` 是「基于 DeepSeek Harness 的写作工作台，开发中」——与同文件第 4 行徽章 `version-8.0.0` 及第 11 行「v8.0.0 作者主权+300章连贯」直接矛盾。
- `README.md:58-67`「核心能力」表仍用 v6 的 `/webnovel-init` 写法且只列 8 条；v8 新增的 `/webnovel:materials|forge|power|style|status` 五条命令在 README 中 0 次出现。
- 修法：整段重写「版本导览」为本 fork 的分支表（master=v6 基线 / v7-tmp / tmp/zcode / v8-author）；装机源改本仓；「核心能力」表换 `/webnovel:*` 13 条。

### P0-2 项目 AGENTS.md「当前状态」停在 v7.1.0，与仓库事实四处冲突

`AGENTS.md:50-53`：
- 「当前版本：v7.1.0」→ 实际 8.0.0（`sync_plugin_version.py --check` 输出）
- 「tmp/zcode 分支」→ 实际 `v8-author`（`git branch --show-current`）
- 目录结构注释「9 个 /webnovel:* 斜杠命令」「MCP 只读查询 ×9」→ 实际 13 条命令、14 个 MCP 工具（CHANGELOG v8.0.0 段自述「MCP 9→14；/webnovel:* 9→13」）
- 「OpenCode 工作区规则」一节指定 `docs/plans/2026-08-25-status-and-pending-work.md` 为「当前待办的唯一入口」→ 见 P0-3

按 code-review 技能第 3 步「文档漂移反向核查」，AGENTS.md 声称与实现不一致即 P0。修法：更新「当前状态」四行 + 目录树两处计数；把「唯一入口」改指向 `docs/zcode/v8-gap-review-3rounds/README.md` 的修复计划，或新建一份 v8 后待办清单。

### P0-3 「唯一待办入口」文档已失效，仍被 AGENTS.md 与 docs/README.md 引用

`docs/plans/2026-08-25-status-and-pending-work.md`（最后提交 2026-08-31）：
- 第 4 行「适用范围：`projects/claude-plugins/webnovel-writer`」——旧路径
- 第 69-72 行 P2「v7 Story Repo 迁移」四项仍为 `[ ]`：迁移器 / 缓存重建 / 垂直切片 / 唯一写入路径——四项均已在 v7.0.0 交付（`migrate_v6_to_v7.py` 存在且 87% 覆盖；2026-09-02 报告 §6 已核销）
- 第 78 行「设定增强通用化 `[ ]`」与第 45 行同项已 `[x]` 自相矛盾
- 第 82 行「多宿主适配 `[ ]`」——v7.1.0 ZCode 原生化已完成

`docs/README.md:31` 仍称其为「唯一待完成清单」。真正的当前待办在 `docs/zcode/v8-gap-review-3rounds/README.md`（4 阶段 11 任务），但该目录在 `docs/README.md` 中 0 次出现。修法：把该清单标 `[superseded]` 并加指针；或就地重写为 v8 后清单；`docs/README.md` 增补 `zcode/`、`reports/`、`decisions/`、`tasks/` 索引。

### P0-4 spec→plan 追溯断裂：F-13 无任务却宣告「全队列完毕」

- `07-feature-flows.md` 列 F-01~F-14 十四项功能；`08-implementation-plan.md` 引用了 F-02~F-12、F-14 共 12 项，**F-13 零引用**（F-01 author-sync 由 T4 覆盖，仅未标 ID）。命令：正则抓取 07 标题与 08 全文 `F-\d+` 差集 → `['F-01','F-13']`。
- 08 计划 34/34 `[x]`、0 个 `[ ]`/`[~]`，commit `c05decc` 宣告「T1-T34 全队列完毕，v8.0.0 发版」。同日 `v8-gap-review-3rounds` §2.1 把「doctor 治理检查组完全未建」列为最严重新发现——它不是漏做，是**从未被排进计划**。
- 这正是工作区 AGENTS.md「W1 验收引用方案条目」要防的事：每个 T 的验收都引用了测试数与 commit，但没有人反向核对「07 的每个 F 是否都有 T」。工作区已有 `docs/templates/spec-traceability-template.md` 未在本项目使用。
- 修法：在 `docs/zcode/webnovel-copilot-300/` 补一份 F→T 追溯表（F-13 标「未排期 → 转 v8-gap-review P4-1」）；08 计划顶部加「本计划不覆盖 F-13」的显式声明，消除「全队列完毕」的误导。

## 4. P1 — 建议修复

### P1-1 v8.0.0 / v7.1.0 未打 tag，`v8-author` 无上游

`git tag` 仅 `v7.0.0`；`releases/v8.0.0.md` 与 `CHANGELOG` 均声称 8.0.0 已发版。`git for-each-ref` 显示 `v8-author` 无 upstream，57 提交只存在本机。远程操作需 Human 确认，本报告只登记：建议打 annotated tag `v8.0.0`（本地即可），推送与 GitHub Release 由 Human 决定。

### P1-2 工作区子模块指针指向旧路径的陈旧克隆

`ai-workspace/.gitmodules` 登记 `projects/claude-plugins/webnovel-writer`（同一远程，分支 `v7-tmp`，HEAD `9ae508c` 2026-09-02）；本仓 `projects/zcode-plugins/webnovel-writer` **有 gitlink 指针但无 `.gitmodules` 映射**（初稿误写为「未跟踪」，已勘误）——指针随每次提交同步，但 `git submodule` 系列命令对它报错，`submodule update --init` 换机时拉不到。两份同源克隆并存易误改。旧克隆的处置（删除 / 归档）属不可逆操作，需 Human 决定；补映射可先做。

### P1-3 `docs/guides/commands.md` 完全未覆盖 v8 命令面

239 行文件中：`/webnovel:` 形式 0 次（仍全是 `/webnovel-*`）；MCP 0 次；v8 新增 11 个 CLI 子命令（materials / style-domain / power / forge / prose-check / drafts / foreshadow-scan / promise-ledger / name-check / volume-reconcile）除 `learn` 外**全部缺席**。2026-09-02 报告 §4 曾判「guides/commands 与代码一致」——那是 v7 口径，v8 后失效。

### P1-4 `.gitignore` 忽略 `AGENTS.md` 但该文件已被跟踪

`.gitignore:6` 有 `AGENTS.md` 条目（引入于 `fd803ab`），`git ls-files AGENTS.md` 返回已跟踪。现状下改动仍会被 `git status` 看到，不影响提交，但任何 `git rm --cached` / 重克隆后 `git add` 都会静默丢掉它。删除该行即可。

### P1-5 测试套件 23 条 `ResourceWarning: unclosed database`

来源测试：`test_dashboard_app.py`（4 用例）、`test_doctor.py`、`test_domain_contract.py::TestDoctorWiring`、`test_prompt_integrity.py`、`test_webnovel_unified_cli.py`。§5.5 逐点核对生产代码连接点后**未定位到明确的开而不关路径**，疑点集中在四个「返回裸连接的工厂函数」的调用方；根因待确认。不影响通过，但 doctor / dashboard 若真有漏关，在长会话 MCP 进程里反复调用会累积句柄，所以列 P1 而非 P2。

### P1-6 41 项缺口修复计划尚无任何 T 落地，且 P2-3「六条数据不变量」在 v8.0.0 宣称范围内

`06-data-design.md §12` 六条不变量仅 1 条实现（`v8-gap-review` §2.2，本轮未复核该表逐项，标「待确认」）。发版说明未标注这一已知缺口。建议在 `releases/v8.0.0.md` 或 CHANGELOG 追加「已知限制」段，引用 41 项清单。

### P1-7 `mcp/server.py` `SERVER_VERSION="7.1.0"`，T34 证据行不实

详见 §5.2。这是本轮唯一一条「计划证据与 git 事实矛盾」的记录，按工作区 W3 应单独登记：T34 勾选所依据的「三处同步含 mcp SERVER_VERSION」在 `cff0ef0` 的 `--stat` 里不存在。修法两步：`sync_plugin_version.py` 纳入 `server.py`；bump 并在 08 计划 T34 证据行加勘误。

### P1-8 `.gitignore` 未忽略 `.env`

详见 §5.1。当前无 `.env` 被跟踪，属预防性红线；加 `.env` / `.env.*`（保留 `!.env.example`）一行即可。

### P1-9 doctor 对纯 v7 书仓解析不到项目根，12 组检查整体跳过

详见 §6 引申发现。08 计划 T1 证据行已登记「phase 解析限制」却未排任务；v8-gap-review P4-1 要在 doctor 上加 8 组治理检查，若根解析不先修，新检查在真书上同样不执行。建议 P4-1 前置一个「v7 书仓根解析」子任务。

## 5. 代码面（子代理扫描结果）

只读扫描范围：`webnovel-writer/scripts/`、`data_modules/`、`mcp/`、`hooks/`、`.zcode-plugin/`，以及仓库根 `.gitignore`。

### 5.1 安全：通过

- 非测试代码无硬编码密钥（`sk-` 字面量 / `api_key = "…"` / 密码字面量 grep 为空）；仅 `api_client.py:16,21` docstring 占位符 `<your-embed-api-key>`。密钥一律走环境变量（`config.py:157,167` `EMBED_API_KEY` / `RERANK_API_KEY`）。
- 全仓 `*.py` 无 `shell=True` / `eval(` / `exec(` / `pickle.load` / `yaml.load(` / `os.system(`；`subprocess.run` 全部 argv 列表（`mcp/server.py:362-370`、`hooks/session_start.py:30-36`、`security_utils.py:264-269`）。
- `git ls-files` 无 `.env` / `.pem` / `.key` 被跟踪。
- **P1**：`.gitignore` 未忽略 `.env`（只有 `:61` 的 `test_gemini.env`；`git check-ignore .env` 退出码 1）。工作区安全红线要求 `.env` 必须在 `.gitignore`。

### 5.2 MCP server（`mcp/server.py`）

- 14 个工具（`TOOLS` `:214-348`）：where / project_status / doctor / setting_read / timeline_check / meter / rag_search / knowledge / context / materials_status / materials_assemble / power_check / foreshadow_scan / reader_signals。纯标准库（`:18-23`）。
- JSON-RPC 健壮性：`JSONDecodeError → -32700`（`:485-488`）、非 dict → `-32600`（`:490-491`）、未知方法 → `-32601`（`:477`）、坏工具名/参数 → `isError`（`:406-418`）。
- 只读性：server 自身零文件 I/O，全部转发 CLI；`foreshadow_scan` 强制 `--no-apply`（`:178-187`）。**符合 AGENTS.md「只读查询」承诺**。
- **P1（新；计划证据与 git 事实不符）**：`mcp/server.py:26` `SERVER_VERSION = "7.1.0"`，而 plugin.json / marketplace 为 8.0.0。08 计划 T34 证据行（`:140`）写「版本 7.1.0→8.0.0 三处同步（plugin.json / marketplace 双位置 / **mcp SERVER_VERSION** / README…）」——`git show --stat cff0ef0` 只改了 `plugin.json`、`reference-loading-map.md`、`validate_reference_wiring.py`，**未触碰 `server.py`**；`git log -- mcp/server.py` 最后一次改动是 T31（`56ac680`）。`sync_plugin_version.py` 也不检查 `server.py`（grep `server.py|SERVER_VERSION` 0 命中），所以 `--check` 报 in sync 是假阴性。修法：把 `SERVER_VERSION` 纳入 `sync_plugin_version.py` 的同步与校验集合，再 bump 到 8.0.0。
- P2：未使用的 `Callable` 导入（`:23`）。

### 5.3 hooks（`hooks/hooks.json` + 4 脚本，全部 `${ZCODE_PLUGIN_ROOT}`）

| 事件 | 脚本 | 行为 |
|---|---|---|
| UserPromptSubmit | `chapter_meter_hook.py` | 静默：裸 `except Exception: pass`（`:89-91`），恒 exit 0 |
| SessionStart | `session_start.py` | 软失败（`:37-38`）；**会写盘**——调用 `author-sync` 写 journal / watermark / stale（`author_sync.py:24,209,299`） |
| PreToolUse (Write/Edit/MultiEdit, Bash) | `guard_runtime_write.py` | 可阻断：`_deny` → exit 2 + `permissionDecision: deny`（`:63-69,133-142`）；输入非 JSON 时按空 payload 放行（`:43-44`） |
| PostToolUse (Write/Edit/MultiEdit) | `chapter_body_trace.py` | 静默（`:56-67`）；写 `.webnovel/logs/chapter_body_trace.log`（`:41-48`） |

P2：两处「静默吞异常」是设计选择（hook 不能拖垮会话），但吞掉后无任何日志，排障时 hook 失效不可见——建议至少 stderr 一行。SessionStart 会写盘这一点 AGENTS.md「MCP 只读」的说法不覆盖，属预期行为，但应在 AGENTS.md 注明「hooks 会写 `.webnovel/`」。

### 5.4 plugin.json 对账

`skills / hooks / agents / commands` 均为目录声明（无逐文件列表），`mcpServers.webnovel`、`userConfig` 三项与磁盘一致；skills 8 / agents 4 与 AGENTS.md 一致，commands 13 / MCP 14 与 AGENTS.md 的 9 / 9 不一致（已归入 P0-2）。

### 5.5 sqlite 连接与 ResourceWarning

生产代码 `sqlite3.connect` / `db.connect` 调用点逐一核对（`doctor.py:311,396,435-439`、`chapter_meter.py:60-69,126-151`、`draft_selection.py:35`、`index_manager.py:653-661`、`knowledge_query.py:18-79`、`dashboard/app.py:184,326`、`v7_cache.py:168+`、`migrate_v6_to_v7.py:156-168`）均有 `with` / `closing` / `try-finally close`。**未找到明确的「开了不关」生产路径**；23 条 warning 更可能来自四个工厂函数（`db.connect`、`dashboard/app._get_db`、`draft_selection._conn`、`v7_cache._conn`）在测试中的高频开关 + 个别调用方漏关。P2：工厂模式本身是设计味道，建议统一为 contextmanager；精确定位需 `python -X tracemalloc=5 -W error::ResourceWarning` 逐用例跑，本轮未做（标「待确认」）。

### 5.6 死代码 / 技术债标记

- 非测试代码 `TODO|FIXME|XXX|HACK` 实际债务 0 处（`v7_write.py:23-24` 是占位符正则，非债）。
- 抽查五个低覆盖顶层脚本（`project_memory` / `status_reporter` / `update_state` / `runtime_compat` / `security_utils`）均有 CLI 或 hook 引用，无孤儿模块。

## 6. 上一轮缺口复盘抽样复核（子代理结果）

对 `v8-gap-review-3rounds` 的 41 项抽 5 项独立复核（只读 grep/读码，不信任原报告结论）：

| 原报告项 | 判定 | 证据 |
|---|---|---|
| N9 `learn learn --from-journal` 参数冗余 | **属实** | `data_modules/webnovel.py:997-1003`：`p_learn.add_argument("action", choices=["learn","apply","show"])` 无 `default`；`author_model.py:329` 镜像同样无默认 |
| D1/N10 v7 settle 不读 `review_results.json`、零后置钩子 | **属实** | `v7_write.py:368-480` settle 只做 run_checks → 双格式守卫 → 写正文/摘要/名册 → git commit → rebuild_cache；文件内 grep `review_results|settle_style_domain|reading_power|materials` 0 命中 |
| C 类 `build_context_pack` 仅 7 section | **属实** | `v7_write.py:141-190` 输出 decision_card / recent_summaries / entities / roster / prev_chapter_tail（条件）/ book_meta / length_contract 共 7；`V7_SECTION_QUOTAS`（`:29-36`）只有 6 键，`length_contract` 不受配额管 |
| B 类 `self_check_batch` 仅 4 项 | **属实** | `chapter_outline_batch.py:71-97`：节点非空 / 字数 500–10000 / 承诺前缀 F-S-R / 时间锚批内重复；无账本存在性、跨批单调、境界链、名册校验 |
| doctor「只有 8 个环境检查，零治理检查」 | **部分属实，措辞需修正** | 「零治理检查」属实：`doctor.py` grep `journal|stale|ledger|backlog|promise` 0 命中。「只有环境检查」**不成立**：`build_doctor_report`（`doctor.py:861-945`）在能解析项目根时会跑 preflight / file / json / story_runtime / sqlite / total_words / projection_log / extraction / contract_version / run_log / rag / domain_contract 共 12 组；`_python_checks` 为 version + 6 import = 7 项而非 8 |

**对 doctor 项的引申发现（新，P1 级）**：原报告在 fantasy01 真仓只看到环境组，说明 doctor 对**纯 v7 书仓解析不到项目根**，v6 的 12 组检查整体跳过——08 计划 T1 证据行已登记「doctor 对纯 v7 书的 phase 解析限制（`.webnovel/state.json` 依赖）」但没有排任务。这意味着 v7/v8 书的 doctor 实际有效面 ≈ Python 依赖 + domain_contract，比「缺 F-13 八组」更宽。建议在 P4-1 之前先补「v7 书仓根解析」，否则 F-13 新增的治理组在真书上同样跑不到。

抽样结论：5/5 方向正确，1 项措辞夸大；原报告可信度足以作为修复计划输入，但每条落地前仍需按 W3 复跑证据。

代码规模（子代理统计，`scripts/` 排除 `__pycache__`）：非测试 `.py` 145 文件 / 44,891 行；测试文件 143；最大五文件 `rag_adapter.py` 1625 / `state_manager.py` 1510 / `index_manager.py` 1442 / `data_modules/webnovel.py` 1417 / `status_reporter.py` 1248——较 2026-09-02 报告的 34,500 行增长约 30%，仍无 >2000 行巨型文件。

## 7. P2 — 可选优化

- `docs/` 根下散落三份 2026-08-24 分析（`code-review-2026-08-24.md`、`full-project-analysis*.md`），按工作区文档分级应归 `docs/reports/`。
- `docs/zcode/` 是本项目独有的方案集目录，与工作区约定的 `research/reports/plans/decisions` 四类并列；可接受，但需在 `docs/README.md` 登记其语义（「按主题成套的 spec/plan/ledger 方案集」）。
- `webnovel-copilot-300` 方案集没有 ledger（`zcode-native-adaptation` 有），T-任务偏离方案的 Ruling 只散落在 08 计划证据行里。
- `status_reporter.py` 34% / `update_state.py` 40% / `sync_plugin_version.py` 27% / `security_utils.py` 53% 覆盖率偏低（pytest 输出）；其中 `security_utils` 承担原子写入与长路径保护，属高价值低覆盖。
- README 版本导览日期「2026-08-19」与 `CHANGELOG` v8.0.0 时间线不一致（随 P0-1 一并处理）。

## 8. 与 2026-09-02 报告的核销对照

| 上轮项 | 本轮状态 | 证据 |
|---|---|---|
| P1 名册双落点 | 已修（S24） | 上轮报告 §7 状态更新；v7_cache 兼读 |
| P1 承诺系统 v7 缺位 | 已落地账本，但**未接入 v7 上下文包/settle**（缺口复盘 E1/N10） | T27 `promise_ledger`；`v8-gap-review` §3 |
| P2 覆盖率门槛失真 | 已修（.coveragerc 钉范围，门槛 80） | 本轮 82.05% |
| P3 v7 三脚本无使用指南 | 已修 `guides/v7-write-path.md`；但 v8 命令面又形成新缺口 | 本轮 P1-3 |
| 延后：死代码 / BOM / docstring 卫生 | 未动 | — |

## 9. 建议执行顺序

1. **P1-7 + P1-8 一次小 commit**：`sync_plugin_version.py` 纳入 `server.py` → bump 8.0.0 → `.gitignore` 加 `.env`（有测试兜底，快模型档可做；先加 `sync --check` 对 server.py 的失败用例再修）。——**[x] 2026-09-04 `1cc23ce`**：RED（`--check` 报 SERVER_VERSION=7.1.0 exit 1）→ GREEN（`Versions are in sync: 8.0.0`）；新增 3 测试，全量 1455 passed / 82.39%。
2. **P0-1 / P0-2 / P0-3 / P1-3 / P1-4 一次文档同步 commit**（纯文档；W5 整段重写而非零星补丁）。——**[x] 2026-09-04 `d726307`**：README 版本导览/装机源/命令表整段重写，AGENTS.md 当前状态与待办入口更新，旧清单标 superseded，docs/README 补索引，commands.md 补 v8 命令面，.gitignore 去 AGENTS.md；四校验器 OK，docs/README 0 断链。
3. **P0-4 追溯表 + T34 勘误**（强模型档；同时把 F-13 挂到 v8-gap-review P4-1，P1-9 挂为 P4-1 前置）。——**[x] 2026-09-04 `13545dd`**：`2026-09-04-copilot-300-规格对账表.md`（F 层 ✅8 / 🔶5 / ❌1）；08 计划加「不覆盖 F-13」声明 + T34 勘误 + T7 证据 hash 勘误（`8f0a5c9` 不存在 → `78a8a16`，29 个引用 hash `cat-file -e` 全校验）；v8-gap-review 新增 P4-0 前置 + §2.1 勘误。
4. **P1-1 本地 tag `v8.0.0`**——须在第 1 步之后打，否则 tag 指向的仍是 SERVER_VERSION 不一致的提交；推送与 Release 等 Human。——**[x] 2026-09-04**：`git tag -a v8.0.0` 于 `13545dd`（`merge-base --is-ancestor 1cc23ce HEAD` exit 0）；`validate_release_notes` 仍 OK（previous_tag v7.0.0）。**已推送（Human 2026-09-04 批准）**：`git push -u origin v8-author` → `[new branch]`；`git push origin v8.0.0` → `[new tag]`；`gh release create v8.0.0 --notes-file releases/v8.0.0.md --target v8-author` → https://github.com/alittleseven/webnovel-writer/releases/tag/v8.0.0 。回溯 tag `v7.1.0` Human 裁决不补。
5. **P1-2 工作区子模块登记**（新增可做；旧克隆删除等 Human）。——**[x] 2026-09-04 工作区 `1ec886e`**：`.gitmodules` 补 `projects/zcode-plugins/webnovel-writer` 映射 + 指针同步到 `13545dd`；`git submodule status` → ` 13545dd… (v8.0.0)`。**旧克隆已删（Human 2026-09-04 批准，工作区 `34bc2a4`）**：删前核实其三分支均与 origin 同步、工作树干净、HEAD `9ae508c` 在本仓历史中（`cat-file -e` exit 0）；`.gitmodules` 去节 + `git rm --cached` + 长路径 rmtree。
6. 然后进入 `v8-gap-review-3rounds` 阶段一（P1-1 上下文包 14 section + P1-2 settle 三门禁）。——**[x] 2026-09-04 `d645d70` `d404a29` `73b70a7` `841831b` `06a9ed1`**：spec/plan 见 `docs/cursor/阶段一-写前链路补全/`；brainstorming 发现 `v7_write.py` 无任何调用点，Human 裁决方案 C（`/webnovel:write` 按 `book.yaml` 分流 + settle CLI + 直接调用 v8 域模块读函数）并批准 14→10 section 的范围调整；验收对照与证据写在 `v8-gap-review-3rounds/README.md` 阶段一完成表。回归 `1483 passed` / 82.67%；fantasy01 ch42 真仓冒烟含三段治理 section。
