# 项目复审报告（只读）

> **类型**：全项目只读审阅（Architectural 档）· 宿主：OpenCode
> **日期**：2026-09-13
> **分支 / HEAD**：`v8-author` / `63da9af chore(opencode): 新增项目级 opencode.json，回补工作区层继承`
> **工作区状态**：仅 `?? .omo/` 与 `?? .openviking/`（既有未跟踪目录，未碰）；其余干净
> **审阅范围**：本仓库根（含 `marketplace.json`、`opencode.json`）；插件本体在同名内层目录 `webnovel-writer/`
> **不计入**：v6 写链（frozen-legacy）、上游 lingfengQAQ 对照、`.tmp/`、`__pycache__/`、`.opencode/node_modules/`、dashboard 预打包 `dist/`
> **纪律**：除本报告外未改任何文件；未 `git add/commit/push`；未删文件
> **证据口径**：`【实跑】`= 本机实际执行并附输出；`【读码】`= 读源码/文档得出（附 `文件:行号`）；`【转引】`= 来自 todohub/交接文档，本次未独立复现；`【子代理】`= 子代理结论（已标注是否经我复核）

---

## 总体判断

**无 P0 阻塞，允许发版/合并**。v8.1.0 发版链全绿（pytest 1513 passed / 82.64%、四校验 + 引用接线 drift=0、纯 v7 冒烟 BREAK 0），文档 `[x]` 抽样对账**未发现「标了没做」**。卡点不是能否发版，而是三处 P1 与一批已登记的待决：① 纯 v7 仓在 git 不可用时 `_local_backup` 漏备真正的正文与域目录（数据面风险）；② 14 个 MCP 工具中 9 个插件内零 MCP 名引用、5 个完全零消费，且 rag/knowledge/context 在纯 v7 仓明示不支持，而 README 仍按 14 个承诺；③ W6 学习闭环、R16/R17/R18、D-2 乙剩余决策、N12 等自述未完成项仍挂账。

---

## 一、P0 阻塞

**无。** 判定依据（不是"应该没问题"，是实跑）：

| 校验 | 实际输出 | 退出码 |
|---|---|---|
| `$env:PYTHONUTF8=1; python -X utf8 -m pytest` | **1513 passed in 125.11s**；`Total coverage: 82.64%`（门槛 80） | 0 |
| `validate_plugin_package.py` | `OK plugin package / errors: 0 warnings: 0` | 0 |
| `validate_release_notes.py` | `OK release notes / version: 8.1.0 / previous_tag: v8.0.0` | 0 |
| `sync_plugin_version.py --check` | `Versions are in sync: 8.1.0` | 0 |
| `smoke_v7_newbook.py` | `PASS 18 ｜ BREAK 0 ｜ BIZ 2` | 0 |
| 〔补充〕`validate_reference_wiring.py` | `OK reference wiring: assets=63 consumers=53 drift=0` | 0 |
| 〔补充〕`coverage report --include=*security_utils.py --fail-under=90` | `security_utils.py 96%` | 0 |

「假 `[x]`」抽查全部属实（无 P0 级）。详见 §六 对账记录。

> 说明：`smoke` 的 2 条 BIZ 是业务级、非缺陷，且与交接文档记载一致：`settle` 被文笔门禁拒绝（fixture 命中 Anti-AI 词库）、`timeline-check` 返回非零（业务判定，已出 JSON）。

---

## 二、P1 应尽快修

| # | 位置（文件:行号） | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| P1-1 | `webnovel-writer/scripts/backup_manager.py:188-240`（清单在 `:214`、`:221`、`:231-236`；触发条件 `:277-279`；文案 `:258`） | **纯 v7 仓本地备份漏备正典**：`_local_backup` 只备份 `正文` / `大纲` / `设定集` / `.story-system` / `.webnovel/{state.json,index.db,vectors.db,memory_scratchpad.json}`。纯 v7 仓没有 `正文`/`设定集`，真正的正文在 `定稿/正文/`、设定在 `设定/`，另有 `素材/` `作者/` `文风/` `演化/` 全部漏备；警告文案本身也写「未找到正文/大纲/设定集」。仅当 git 不可用时走此路径。 | 【读码】`:214`/`:221`/`:231-236`/`:258`/`:277-279`。todohub `t-20260913-f847`（P3）同源。 | 备份清单加 v7 域：`定稿`、`设定`、`素材`、`作者`、`文风`、`演化`；文案改为按形态提示；补 `--no-git` v7 仓实跑用例。 |
| P1-2 | `webnovel-writer/mcp/server.py:216-343`；消费方 `commands/webnovel/status.md:10`、`power.md:8`、`materials.md:10`、`doctor.md:10`；闸门 `scripts/data_modules/webnovel.py:182-196`、`:1414-1418`；承诺文案 `README.md:77`、`docs/guides/commands.md:3,104` | **MCP 工具面「名实落差」**：14 个工具确实注册且与 README 点名的 14 个逐一对应；但**插件内只有 5 个工具被 MCP 名真实引用**（`doctor`/`project_status`/`power_check`/`materials_status`/`materials_assemble`）；**9 个在插件内零 MCP 名引用**（`where`/`setting_read`/`timeline_check`/`foreshadow_scan`/`meter`/`rag_search`/`knowledge`/`context`/`reader_signals`）——前四个另有 CLI 等价消费者，后五个（`meter`/`rag_search`/`knowledge`/`context`/`reader_signals`）**完全零消费**。其中 `rag`/`knowledge`/`context` 在纯 v7 仓返回 `unsupported`。对外仍按「14 只读工具」承诺。 | 【实跑】grep 全 14 名：`commands/webnovel/{status,power,materials,doctor}.md` 命中 5 个；其余 9 名仅出现在 `mcp/server.py` 与 `mcp/tests/test_server.py`。【读码】`webnovel.py:182-196` 定 `_V7_UNSUPPORTED={rag,knowledge,context}`，`:1414-1418` 拦截。 | 待 D-2 乙裁决：`rag_search`/`knowledge`「真砍出工具面 vs 在 v7 写链补生产者」+ `meter` 去留；若收缩则同步 `README.md:77`、`AGENTS.md`、`docs/guides/commands.md:3,104` 的承诺数。 |
| P1-3 | `docs/zcode/writing-quality-review/04-roadmap.md:18,28`（`08:4` 的「吸收并取代 W1-W10」） | **W6 / R9（学习闭环升级）真的未实现**，且原施工落点 `scripts/project_memory.py` 已被 `511531e` 删除（`evidence_excerpt`/`metrics_snapshot` 全仓零命中）。这是质量线唯一 fully-missed 项；D-7 裁决 B 只补 W8、W6 暂不动且未最终定性。 | 【实跑】`grep -n "evidence_excerpt\|metrics_snapshot"` 零命中；`04-roadmap.md:18` 显式 `❌ 未实现`、`:28` 标注施工指引失效；todohub `t-20260912-ac94`（P3）。 | 按 D-7 补做或正式记为「不做」并在 `04-roadmap.md`/README 定性。 |
| P1-4 | 纯 v7 仓写链落 v6 域文件 | **纯 v7 仓的写链仍会生成 v6 系统域文件**：`pack` 副作用建 `style_samples.db`、`index.db` 被 `settle` 纳入 git。 | 【转引】todohub `t-20260913-1072`（P2）、`docs/reports/2026-09-13-会话交接-v7路径收口.md:72`。**本次未独立复现**。 | 待确认范围后改落点或纳入 `.gitignore`。 |

---

## 三、P2 可选优化

| # | 位置（文件:行号） | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| P2-1 | `scripts/data_modules/domain_contract.py:21-30`（REQUIRED_DIRS）、`:39-44`（ADVISORY_FILES）、`:75`（DOMAIN_TOP_DIRS）、`scripts/init_book.py:142,149` | **`book-init` 实测不建 `设定/`**：`domain_dirs=8`（大纲×4 / 素材×2 / 作者 / 文风）+ 显式建 `定稿/正文`，六域**顶层目录实建 5/6**（缺 `设定`，因其仅属 advisory，不自动创建）。`is_story_repo_strict` 只要求六域之一存在，故不影响书仓判定；doctor 也不告警。`smoke_v7_newbook.py` 附近注释「book-init 只建空的 设定/」与实测不符。 | 【实跑】临时仓 `book-init`：`Test-Path` → 大纲✓ 定稿✓ 设定✗ 素材✓ 作者✓ 文风✓ 演化✗（`定稿/设定`✗、`素材/活`✓、`作者/journal.jsonl`✓）。【读码】`domain_contract.py:143-172` 只造 REQUIRED。 | 统一口径：AGENTS.md/README 若说「六域骨架」建议改为「必备目录骨架（设定为 advisory）」；修正 smoke 注释。 |
| P2-2 | `docs/zcode/writing-quality-review/04-roadmap.md:22,24` | 表内 W6 `❌`、W10 `⚠️ 部分`，但小结写「**合计 9/10 完成**」（把 W10 部分计入）。口径可再明确。 | 【读码】`:13-24` 表格 + `:24` 小结。 | 小结注明「8 完整 + 1 部分（W10）+ 1 未做（W6）」。 |
| P2-3 | `docs/plans/2026-09-10-v6线退役方案.md:204` | 快照漂移：记 Phase 1 时 `webnovel-write/SKILL.md` 499 → **275** 行；本次实测 **207** 行（后续提交继续精简）。属滚动快照，非缺陷。 | 【实跑】`(Get-Content …SKILL.md).Count = 207`。 | 可选：加「引用前重测」提示（该仓已有此类惯例）。 |
| P2-4 | `docs/zcode/v8-migration-gap-audit/README.md` | 文首自称「28 项」，逐类相加实际 29（U-5 记录、未追改）。 | 【转引】对账报告 `U-5`。 | 记录即可，不必回改。 |
| P2-5 | `.webnovel/tmp/` 无人清理/归档（N12） | v7 路径 settle 后 review/extraction artifacts 只增不减；不污染 git、不骗门禁，真实影响=磁盘累积+一处 v6 尾段分支。 | 【转引】`docs/zcode/v8-gap-review-3rounds/README.md:125`、对账 `U-3`、todohub `t-20260912-0cbc`（P3）。 | 需 Human 决定是否补清理步骤。 |
| P2-6 | `webnovel-writer/hooks/__pycache__/`、各处 `__pycache__/` | 工作树存在缓存目录；`validate_plugin_package.py` 仍 0 warning（未拦），但 clean-room 打包建议用 `git archive`/干净克隆（内层 README 已有提示）。 | 【实跑】目录列表；`webnovel-writer/README.md`「最小验证」节。 | 打包/装机流程固定用 `git archive`。 |
| P2-7 | `scripts/status_reporter.py:133` | 另有 v6 硬编码路径点 `self.chapters_dir = project_root / "正文"`（未在五个已知风险点内）。**未验证**对纯 v7 的 `project-status` 影响。 | 【读码】`:133`。 | 排查该字段在 v7 分支是否被读取后再定。 |

---

## 四、文档自述的未完成 / 待定 / 不做项（逐条原文编号，只提炼不评价）

**A. `docs/plans/2026-09-10-v6线退役方案.md`（状态 `[~]`）**
- §1.5 B 类「先拆后删」4 模块**未执行**：`continuity_check` / `artifact_validator` / `memory/writer`（归属待定）/ `context_weights`（→ 对账 `D-3(a)`）。
- §1.6：`contract_migrations` **暂缓，不随本轮删**，随 Phase 3 一并决定。
- §1.6 ⛔：Phase 2 第 2 条「删 v6 CLI 组」**大面积不可执行**，自荐重定性为「**冻结而非删除**」。
- §3 Phase 3：dashboard / RAG / 记忆 / `knowledge_query` / `reader_signals` 的功能归属**需 Human 决策**；MCP 工具面可能 **14 → 约 9**。

**B. `docs/reports/2026-09-12-需求与设计对账.md` §3 决策（D 系列）**
- `D-1` 六域口径：**已裁 A**（第 6 域=文风，演化=附带事件域），已回改 `04:31` / `05:6`。
- `D-2` 甲（根解析）：**已修**（`df65874`）。乙（v6 域数据）：第一批「三工具明示不支持」**已落地**（`b730d90`）；**剩余待决**：`rag_search`/`knowledge`「真砍 vs 补生产」+ `meter` 去留。
- `D-3`（v6 退役 Phase 2 剩余）：(a) 未执行；(c) 已定性为冻结而非删除。
- `D-4` zcode-native-adaptation 的 **7 项重启验证**——**只有 Human 能做**（需重启 ZCode）。
- `D-5` `R16`/`R17`/`R18` 是否补做——**未定论**。
- `D-6` `F` 系列文档回改——建议执行（F-1~F-4 已完成，F-5 索引）。
- `D-7` W6/W8：**已裁 B**（只补 W8；W6 暂不动，最终定性待定）。
- `D-8` `memory_pack` 是否进排序（R13 改动 3）：**已裁 A（先不做）**，留档待议。
- §6 未核项：`U-2`（R16/R17/R18 未定论）、`U-4`（A6/A8/B1-B4/W1-W10 无逐条来源）、`U-5`（28 vs 29 仅记录）、`U-9`（`context` 报 `CONTEXT_BUILD_FAILED` 未逐项定位）、`U-10`（`knowledge` 硬编码 `.webnovel/index.db`；现被明示不支持遮蔽，底层未修）。

**C. `docs/zcode/writing-quality-review/04-roadmap.md`**
- `W6` **❌ 未实现**（且原落点 `project_memory.py` 已被删，施工指引失效）。
- `W10` **⚠️ 部分**（仅 `R15`）；`R16`/`R17`/`R18` **未定论**。

**D. `docs/zcode/v8-gap-review-3rounds/README.md`「明确不做（本轮）」3 项**
1. 不重写 `v7_write` 为 v6 合同链；2. 不做 write-gate 对 v7 的全量移植（用 settle 门禁替代）；3. 不动 v6 路径任何行为。

**E. `README.md`「已知限制（自动化验证边界）」**
- `run_behavior_evals --suite fast` 23 个 case 为契约/结构层，**不覆盖生成内容质量**；`webnovel-write` evals 仅 3 条、`webnovel-review` 仅 1 条；生成质量主要靠真仓人工冒烟。

**F. todohub 未完成待办（7 条，2026-09-13 实查）**
`t-20260913-6202`(P2 reader_signals 换落点) · `t-20260913-1072`(P2 纯 v7 仍写 v6 域) · `t-20260913-ff22`(P3 U-14 幽灵 section) · `t-20260913-f847`(P3 backup_manager v7 缺口) · `t-20260912-ac94`(P3 W6/R9) · `t-20260912-8f3e`(P3 D-8) · `t-20260912-0cbc`(P3 N12)。

---

## 五、本次未能验证的部分

| # | 未验证项 | 原因 / 边界 |
|---|---|---|
| V-1 | 远端推送状态（`v8.1.0` tag / `v8-author` 是否已 push `origin`） | 未做网络核验；仅本地实测 `git tag --list "v8*"` → `v8.0.0`/`v8.1.0` 存在。 |
| V-2 | GitHub Actions 的真实 CI 结果 | 未抓取 run 记录；仅本地复现 CI 中的命令（全绿）。 |
| V-3 | ZCode 装机 7 项重启验证（`D-4`） | 需 Human 重启 ZCode 后按 runbook §4 确认。 |
| V-4 | 真书仓（fantasy01 / fantasy01-v2）端到端 | 该仓不在本仓库内；本次只跑 `smoke_v7_newbook.py` 的临时仓。 |
| V-5 | T1-T34 逐项实现审计 | 只抽验 5 处 F-闭合代码证据 + 34/34 勾选计数；未逐条重跑 T1-T34。 |
| V-6 | TODO-from-v8-author-review 的 24 项 `[x]` 逐条 | 只验证「0 未勾 / 24 已勾」与技术侧抽样，未逐条复跑其证据。 |
| V-7 | `/review` 命令的实际发现 | 需重启会话后 `/help` 确认；本次以二进制内嵌文档证明目录名兼容（见 §六）。 |
| V-8 | 性能维度 | 无性能承诺、无基准；未做基准测试/画像。 |
| V-9 | `U-14`（`length_contract` 幽灵 section）、`t-20260913-1072` | 仅登记在 todohub/交接，本次未独立复现。 |
| V-10 | 子代理部分行号 | Agent C（安全）/ Agent D（测试）给出的行号未逐条复核（其结论已用我自己的 grep 交叉验证，见 §六）。 |

---

## 六、核验记录（实跑 vs 读码推断）

### 6.1 对外承诺兑现（逐条）

| 承诺 | 实测 | 判定 |
|---|---|---|
| 8 个 skill | `skills/` 下 8 个目录 | ✅ 【实跑】 |
| 4 个 agent | `agents/` 4 个 .md + `evals/` | ✅ 【实跑】 |
| 13 条 `/webnovel:*` 命令 | `commands/webnovel/` 正好 13 个 .md；确为薄壳（`status.md` 16 行、`forge.md` 17 行） | ✅ 【实跑】 |
| 14 个 MCP 只读工具 | `mcp/server.py:216-343` 正好 14 个，与 README 点名清单逐一对应（名称 `webnovel_*`） | ✅ 名称/数量；⚠️ 消费者仅 5（见 P1-2） |
| 六域书仓 | 定义 `domain_contract.py:3,75`；骨架 `init_domain_skeleton:143-172` 由 `init_book.py:142` 调用；`定稿/正文` 由 `init_book.py:149` 建；各域均有消费者 | ✅（顶层 5/6，见 P2-1） |
| 13 个 CLI 治理子命令 / ~50 CLI 子命令 | 未逐个数；commands.md:3 口径与实现方向一致 | 【读码】 |

### 6.2 v6/v7 双布局路径收口（五个已知风险点 + 附加点）

| 模块 | v6 | v7 | 证据 |
|---|---|---|---|
| `scripts/chapter_paths.py:115-161` | ✅ 回落（`:136-161`，`正文/第NNNN章*` + 卷布局 + fallback 扫描） | ✅ 优先（`:127-134`，`定稿/正文/NNNN-*.md`） | 【实跑】读源确认；`U-12` |
| `scripts/data_modules/placeholder_scanner.py:42-55` | ✅ `大纲` + `设定集` | ✅ `设定`（`:45-47` 注释明写两代） | 【实跑】读源；`U-11` |
| `scripts/data_modules/settings_digest.py:69-79` | ✅ `设定集/` 兜底 | ✅ `设定/` 优先（B1） | 【实跑】读源；`U-8`（`config.settings_dirs` 未直接读，`【读码】`） |
| `scripts/project_locator.py:272-279` | ✅ `.webnovel/state.json`（`:273`） | ✅ `book.yaml` + 六域之一（`:277-279` 调 `is_story_repo_strict`，定义 `domain_contract.py:78-88`） | 【实跑】读源；甲-1 |
| `scripts/backup_manager.py` | 经 `resolve_project_root`（`:64`）定位根 | 定位层共享 | ⚠️ **`_local_backup` 备份清单仍为 v6（`:214-236`）→ P1-1** |
| 附加：`v7_write.py:6-7,82,758`、`migrate_v6_to_v7.py:87,113,187`、`v7_cache.py:73` | — | ✅ v7 路径一致（`定稿/正文/NNNN-标题.md`） | 【实跑】grep |
| 附加：`status_reporter.py:133` | ⚠️ 硬编码 `正文`（v6） | 未验证 | 【读码】→ P2-7 |

### 6.3 文档对账（抽样）

- `08-implementation-plan.md`：`- [x]` **34** / `- [ ]` **0** ✅【实跑】；`:11`/`:13` 的 F-13、F-04/05/08/11/14 回填声明在位。
- U-1 五处闭合**代码证据逐条实锤**：`chapter_outline_validate.py:59,78,151,174,186`（五码）；`v7_write.py:567,604,637,836`（prose/后置）；`forge_sync.py:20`；`governance.py:181,191,240` ✅【实跑】。
- `04-roadmap.md`：状态回填节在位（`:6-32`），W6 `❌`、W10 `⚠️` ✅【实跑】。
- W8/R13：`context_ranker.py:169-173,272-273` 用 `_density_score` 替代 `_length_score`；两个验收用例真实存在（`test_context_ranker.py:23`、`test_memory_orchestrator.py:123`）✅【实跑】。
- `TODO-from-v8-author-review.md`：`- [x]` **24** / `- [ ]` **0** ✅【实跑】（逐条未复跑，见 V-6）。
- `docs/README.md` 状态表与 `AGENTS.md`「当前状态」口径一致（v6 退役方案为进行中入口）✅【读码】。
- v6 物理删除：13 个点名模块逐个 `Test-Path=False` ✅【实跑】。
- F-3：`05-book-directory-structure.md:6` 已改「六域总图（+ 演化事件域）」、`:11` 口径说明 ✅【实跑】。
- `doctor.py` 治理组：`gov.{inv_id}`(:991)、`gov.materials.health`(:1022)、`gov.gallery.backlog`(:1055)、`gov.suite`(:1168) → 与 P4-1「8 组」自洽 ✅【实跑】。

### 6.4 测试是否测行为而非实现

- 布局：`pytest.ini` `testpaths` = data_modules/tests + scripts/tests + mcp/tests；文件数 **91 + 53 + 1 = 145** ✅【实跑】。
- **测试锚定方案条目**（行为/契约导向的直接证据）：`test_context_ranker.py:24,48`（`R13/F-13`）、`test_context_budget.py:180`（`M5/T22 验收`）、`test_memory_orchestrator.py:124,175,224`（`R13`）、`mcp/tests/test_server.py:59` `test_tools_list_contains_fourteen_tools` ✅【实跑】。
- 判定：**以行为/契约为主**（断言外部输出、端点、CLI 映射、验收口径），未发现「实现细节强耦合」为主的证据；生成质量覆盖薄一事，README 已自述（§四.E）。
- 【子代理】Agent D 给出 93/54/1 计数与我的 91/53/1 略有差（计数口径不同），结论一致是我自己的 grep 复核。

### 6.5 安全性

- **无生产硬编码密钥**：`sk-*` / `api_key="…"` 类 grep 仅命中测试值 `test_api_client.py:244`（`"sk-test"`）、`:396`（`"rk-test"`）✅【实跑】。
- **无危险执行**：`shell=True` / `os.system` / `eval(` / `exec(` 全仓 **0 命中** ✅【实跑】。
- **路径穿越防护**：`dashboard/path_guard.py:11-28` `safe_resolve` 强制 `relative_to(project_root)`；消费点 `dashboard/app.py:21,863` ✅【实跑】。
- `security_utils.py` 覆盖 96% ≥ CI 独立闸 90 ✅【实跑】。
- 密钥运行时经环境变量（`EMBED_API_KEY`/`RERANK_API_KEY`）注入、UI 只显示 `*_present` 标志【子代理 C，未逐行复核】。

### 6.6 配置链与可复用命令前置检查（用户指定）

- `.opencode/` 存在（含 `.gitignore`/`package.json`/`openviking-config.json`，`node_modules/`），`git ls-files .opencode` 为空（未跟踪）【实跑】。
- `opencode debug config` 解析后：`instructions: ["../../../AGENTS.md"]`、`skills.paths: ["../../../.agents/skills"]` **未断链** ✅【实跑】（首次解析撞上 UTF-8 BOM + 控制字符，改用正则提取）。
- **`.opencode/command/` 与 `.opencode/commands/` 两种都支持**：opencode **1.18.30** 二进制内嵌文档原文——`| Project commands | .opencode/command/<name>.md or .opencode/commands/<name>.md |`（同理 agent(s)/skill(s)）✅【实跑】。→ 落 `/review` 用复数 `commands/` 安全。

### 6.7 子代理结论核验（"自己核验一遍"）

| 子代理 | 结论 | 我的复核 |
|---|---|---|
| A（MCP 消费者） | 「14 个工具在 skills/commands/agents/hooks **全零引用**」 | ❌ **已纠正**：实测 5 个被 commands 引用（见 P1-2）。其错误源于只搜了 `webnovel_*` 全名且遗漏 commands 目录。 |
| B（双布局路径） | 4 模块支持双布局、backup_manager 经 project_locator 间接覆盖 | ✅ 我亲读 `chapter_paths.py` / `project_locator.py` / `settings_digest.py` / `placeholder_scanner.py` / `backup_manager.py` 四段后确认；**并补出 `_local_backup` v6 盲区（其未发现）**。 |
| C（安全） | 无生产密钥、无 eval/exec、security_utils 健在；其文件清单有误（称无 hooks 目录） | ⚠️ 结论方向经我 grep 复核成立；其「无 hooks 目录」错误已由我实测 `hooks/`（4 脚本 + hooks.json）推翻。 |
| D（测试） | 以行为/契约为主 | ✅ 关键点（含 `M5/T22`、`R13`、14 工具契约测）由我 grep 复核成立。 |
| E（六域） | 未产出（会话 aborted） | 🔁 我**自行完成**（§6.1 + P2-1），含 `book-init` 临时仓实测。 |

---

## 七、建议处理顺序（供参考，不含指令）

1. **P1-1**（备份盲区）——数据面风险，改动小、可独立上。
2. **P1-2 的裁决输入**（D-2 乙剩余：rag/knowledge/meter）——决定 MCP 面收缩与文档承诺数是否同步。
3. **P1-3（W6）** 与 **D-5（R16/17/18）** 的定性——避免"9/10"长期含混。
4. P2 批量（口径/文案/快照）——低风险，可随下一次文档整理一并做。

---

*报告结束。本报告为只读产物；报告落盘位置 `docs/opencode/项目复审/` 为新增宿主分目录（沿用 `docs/zcode/`、`docs/cursor/` 约定），已按用户要求创建并写入。*
