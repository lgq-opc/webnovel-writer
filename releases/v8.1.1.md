# v8.1.1 - v6 退役收官 + 质量收口 + ZCode 装机与 MCP 修复

## 发版范围

本次发布覆盖上个正式 tag `v8.1.0` 之后的 `v8.1.0..v8.1.1`（`v8-author` 分支，137 笔）。主线三块：① v6 线退役按方案 §3.1 六条裁决收官（2026-09-18）；② 2026-09-14/15 质量审阅（双宿主夜审 + 六个 pilot 批次）全量收口；③ ZCode 装机面登记丢失重建与 MCP 加载根因修复（2026-09-18/20）。给作者的一句话：写章工具更诚实了——字数、状态、备份不再给你错信儿；旧 v6 拆除完毕但完全不影响 v7 写链；插件在 ZCode 里装得上、连得上了。

## 给作者看的变化

- **决策卡如实显示推导出的目标字数**（此前推导结果不落卡，要自己再算一遍），并抽出唯一事实源，杜绝两处口径漂移。
- **字数契约成为真 section**（U-14 幽灵 section 已核实并消除）：决策卡里看到的字数约定就是写链实际消费的那份。
- **`--no-git` 书仓不再被违背**：`backup` 不再自动 `git init` 覆盖作者的选择；settle 在无 git 书仓失败时给出可执行诊断，不再抛 git 原始报错。
- **本地备份清单补齐 v7 六域**（此前备份只剩 `大纲/`，正文/设定集等不在清单里）。
- **风格样本库落点随书仓形态**：v7 书仓不再被副作用建出 `.webnovel/` 目录。
- **查询口径修正**：钩子类型/钩子强度死映射删除，钩子口径统一归决策卡；`status` 在 v7 仓如实报「不支持」，不再输出 v6 口径的误导值。

## 是否需要改旧项目

- **v7 书仓：不需要任何迁移。** 写链、数据面、命令面全部不变，升级即用。
- **v6 旧仓：仍走既定迁移流程**（`migrate_v6_to_v7.py`）。注意：已删除的 v6 context 链命令（`extract-context` 等）如被你自己的脚本引用，需改指 `webnovel.py v7-write pack`（写前降级的 v7 等价物）；v6 写链本身维持冻结、仅保留 doctor 体检，不再新增。

## 给维护者

- **v6 退役收官（方案 §3.1 六条裁决全部落地）**：context 链（`extract-context` / `context_manager` 装配链）级联删除；孤儿模块与无消费者旋钮清理；`continuity_shared` 拆为共享模块；RAG 正式下线；dashboard 对纯 v7 仓启动时明示不支持；`knowledge` 名册级定位定为正式口径；doctor 的 v6 体检保留。
- **MCP 工具面 14→12**（D-2 乙）：撤 `rag_search`（v7 无向量库）与 `context`（已被 `v7-write pack` 取代）；`knowledge` 按书仓形态分流。
- **质量审阅收口**：夜审 ZCode/OpenCode 双侧报告合入；Phase 2 三增量独立核验；引用盲区复查（19 个已删模块名 0 残留）；guard 复合命令逐段判定（R-1/R-2，白名单不再被破坏性后缀段搭车）；死命令文案清理（O-1）；data-agent.md 回归修复；D-5 结案（R16 superseded / R17 趋势入口已接 / R18 死旋钮已清）；reader_signals 回填与 R13 排序两份设计单按 0915 评审必改落地。
- **ZCode 装机与 MCP 修复（本版直接动因）**：ZCode 更新曾致插件登记面（installed_plugins / known_marketplaces / enabledPlugins）全部丢失，重建手册落 runbook **§6**（含从 `zcode.cjs` 逆向的三登记确切 schema 与守卫探针验收口径）；修复 `${user_config.*}` 无 `default` 时宿主展开即 throw 致 MCP server 加载失败的根因（`bookProjectRoot` 补空串 default）；确认 3.12.3 起宿主已读内联 mcpServers，无需根 `.mcp.json`。
- **CI 与测试基建**：workflow paths 补入 skills/agents/evals（守卫此前在它最该跑的场合不触发）；测试子进程编码契约统一（N-1，中文 Windows 假失败修复）。
- **测试与校验**：全量 pytest 1510 项通过（exit 0）；validate_plugin_package / validate_release_notes / sync_plugin_version 三校验通过。

## 验证

- 全量 pytest：1510 项收集全绿（exit 0，无失败），覆盖率 82.51%（≥80% 门槛）。
- 四校验脚本：validate_plugin_package OK（0 errors）、validate_release_notes OK、sync_plugin_version --check 三处版本一致（8.1.1）。
- MCP 探针：装机副本 `mcp/server.py` tools/list 计 12 工具；守卫 hook 五例探针（直删/复合命令/伪造写链 rc=2、单独写链 rc=0、Write 受保护 rc=2）5/5；ZCode 重启后 MCP 面板已连接，`webnovel_where` 实调业务级应答正常。
